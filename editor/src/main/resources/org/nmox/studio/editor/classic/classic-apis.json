{
  "jquery": {
    "display": "jQuery",
    "entries": [
      {"name": "$", "sig": "$(selector[, context])"},
      {"name": "jQuery", "sig": "jQuery(selector[, context])"},
      {"name": "$.ajax", "sig": "$.ajax(url[, settings])"},
      {"name": "$.get", "sig": "$.get(url[, data][, success][, dataType])"},
      {"name": "$.post", "sig": "$.post(url[, data][, success][, dataType])"},
      {"name": "$.getJSON", "sig": "$.getJSON(url[, data][, success])"},
      {"name": "$.getScript", "sig": "$.getScript(url[, success])"},
      {"name": "$.ajaxSetup", "sig": "$.ajaxSetup(options)"},
      {"name": ".load", "sig": ".load(url[, data][, complete])"},
      {"name": ".serialize", "sig": ".serialize()"},
      {"name": ".serializeArray", "sig": ".serializeArray()"},
      {"name": ".on", "sig": ".on(events[, selector][, data], handler)"},
      {"name": ".off", "sig": ".off(events[, selector][, handler])"},
      {"name": ".one", "sig": ".one(events[, selector][, data], handler)"},
      {"name": ".trigger", "sig": ".trigger(eventType[, extraParameters])"},
      {"name": ".triggerHandler", "sig": ".triggerHandler(eventType[, extraParameters])"},
      {"name": ".ready", "sig": ".ready(handler)"},
      {"name": ".click", "sig": ".click(handler)"},
      {"name": ".dblclick", "sig": ".dblclick(handler)"},
      {"name": ".hover", "sig": ".hover(handlerIn, handlerOut)"},
      {"name": ".focus", "sig": ".focus(handler)"},
      {"name": ".blur", "sig": ".blur(handler)"},
      {"name": ".change", "sig": ".change(handler)"},
      {"name": ".submit", "sig": ".submit(handler)"},
      {"name": ".keydown", "sig": ".keydown(handler)"},
      {"name": ".keyup", "sig": ".keyup(handler)"},
      {"name": ".find", "sig": ".find(selector)"},
      {"name": ".closest", "sig": ".closest(selector)"},
      {"name": ".parent", "sig": ".parent([selector])"},
      {"name": ".parents", "sig": ".parents([selector])"},
      {"name": ".children", "sig": ".children([selector])"},
      {"name": ".siblings", "sig": ".siblings([selector])"},
      {"name": ".next", "sig": ".next([selector])"},
      {"name": ".prev", "sig": ".prev([selector])"},
      {"name": ".filter", "sig": ".filter(selector)"},
      {"name": ".not", "sig": ".not(selector)"},
      {"name": ".eq", "sig": ".eq(index)"},
      {"name": ".first", "sig": ".first()"},
      {"name": ".last", "sig": ".last()"},
      {"name": ".has", "sig": ".has(selector)"},
      {"name": ".is", "sig": ".is(selector)"},
      {"name": ".each", "sig": ".each(function(index, element))"},
      {"name": ".map", "sig": ".map(callback)"},
      {"name": ".add", "sig": ".add(selector)"},
      {"name": ".end", "sig": ".end()"},
      {"name": ".index", "sig": ".index([element])"},
      {"name": ".addClass", "sig": ".addClass(className)"},
      {"name": ".removeClass", "sig": ".removeClass([className])"},
      {"name": ".toggleClass", "sig": ".toggleClass(className[, state])"},
      {"name": ".hasClass", "sig": ".hasClass(className)"},
      {"name": ".attr", "sig": ".attr(attributeName[, value])"},
      {"name": ".removeAttr", "sig": ".removeAttr(attributeName)"},
      {"name": ".prop", "sig": ".prop(propertyName[, value])"},
      {"name": ".val", "sig": ".val([value])"},
      {"name": ".text", "sig": ".text([text])"},
      {"name": ".html", "sig": ".html([htmlString])"},
      {"name": ".append", "sig": ".append(content[, content])"},
      {"name": ".prepend", "sig": ".prepend(content[, content])"},
      {"name": ".appendTo", "sig": ".appendTo(target)"},
      {"name": ".before", "sig": ".before(content[, content])"},
      {"name": ".after", "sig": ".after(content[, content])"},
      {"name": ".remove", "sig": ".remove([selector])"},
      {"name": ".detach", "sig": ".detach([selector])"},
      {"name": ".empty", "sig": ".empty()"},
      {"name": ".clone", "sig": ".clone([withDataAndEvents])"},
      {"name": ".replaceWith", "sig": ".replaceWith(newContent)"},
      {"name": ".wrap", "sig": ".wrap(wrappingElement)"},
      {"name": ".css", "sig": ".css(propertyName[, value])"},
      {"name": ".data", "sig": ".data(key[, value])"},
      {"name": ".removeData", "sig": ".removeData([name])"},
      {"name": ".width", "sig": ".width([value])"},
      {"name": ".height", "sig": ".height([value])"},
      {"name": ".offset", "sig": ".offset()"},
      {"name": ".position", "sig": ".position()"},
      {"name": ".scrollTop", "sig": ".scrollTop([value])"},
      {"name": ".show", "sig": ".show([duration][, complete])"},
      {"name": ".hide", "sig": ".hide([duration][, complete])"},
      {"name": ".toggle", "sig": ".toggle([duration][, complete])"},
      {"name": ".fadeIn", "sig": ".fadeIn([duration][, complete])"},
      {"name": ".fadeOut", "sig": ".fadeOut([duration][, complete])"},
      {"name": ".fadeToggle", "sig": ".fadeToggle([duration][, complete])"},
      {"name": ".fadeTo", "sig": ".fadeTo(duration, opacity[, complete])"},
      {"name": ".slideDown", "sig": ".slideDown([duration][, complete])"},
      {"name": ".slideUp", "sig": ".slideUp([duration][, complete])"},
      {"name": ".slideToggle", "sig": ".slideToggle([duration][, complete])"},
      {"name": ".animate", "sig": ".animate(properties[, duration][, easing][, complete])"},
      {"name": ".stop", "sig": ".stop([clearQueue][, jumpToEnd])"},
      {"name": ".delay", "sig": ".delay(duration[, queueName])"},
      {"name": "$.each", "sig": "$.each(collection, callback)"},
      {"name": "$.map", "sig": "$.map(array, callback)"},
      {"name": "$.grep", "sig": "$.grep(array, function[, invert])"},
      {"name": "$.extend", "sig": "$.extend([deep, ]target[, object1][, objectN])"},
      {"name": "$.merge", "sig": "$.merge(first, second)"},
      {"name": "$.inArray", "sig": "$.inArray(value, array[, fromIndex])"},
      {"name": "$.trim", "sig": "$.trim(str)"},
      {"name": "$.parseJSON", "sig": "$.parseJSON(json) — deprecated 3.0, use JSON.parse"},
      {"name": "$.parseHTML", "sig": "$.parseHTML(data[, context][, keepScripts])"},
      {"name": "$.when", "sig": "$.when(deferreds)"},
      {"name": "$.Deferred", "sig": "$.Deferred([beforeStart])"},
      {"name": "$.proxy", "sig": "$.proxy(function, context)"},
      {"name": "$.noConflict", "sig": "$.noConflict([removeAll])"},
      {"name": "$.isEmptyObject", "sig": "$.isEmptyObject(object)"},
      {"name": "$.contains", "sig": "$.contains(container, contained)"},
      {"name": "$.now", "sig": "$.now()"}
    ]
  },
  "mootools": {
    "display": "MooTools",
    "entries": [
      {"name": "$", "sig": "$(id)"},
      {"name": "$$", "sig": "$$(selector)"},
      {"name": "typeOf", "sig": "typeOf(obj)"},
      {"name": "instanceOf", "sig": "instanceOf(item, object)"},
      {"name": "Class", "sig": "new Class(properties)"},
      {"name": "Element", "sig": "new Element(tag[, properties])"},
      {"name": "Element.implement", "sig": "Element.implement(methods)"},
      {"name": "Request", "sig": "new Request(options)"},
      {"name": "Request.JSON", "sig": "new Request.JSON(options)"},
      {"name": "Request.HTML", "sig": "new Request.HTML(options)"},
      {"name": ".send", "sig": ".send([options])"},
      {"name": ".addEvent", "sig": ".addEvent(type, fn)"},
      {"name": ".addEvents", "sig": ".addEvents(events)"},
      {"name": ".removeEvent", "sig": ".removeEvent(type, fn)"},
      {"name": ".removeEvents", "sig": ".removeEvents([type])"},
      {"name": ".fireEvent", "sig": ".fireEvent(type[, args[, delay]])"},
      {"name": ".getElement", "sig": ".getElement(selector)"},
      {"name": ".getElements", "sig": ".getElements(selector)"},
      {"name": ".set", "sig": ".set(property, value)"},
      {"name": ".get", "sig": ".get(property)"},
      {"name": ".erase", "sig": ".erase(property)"},
      {"name": ".adopt", "sig": ".adopt(elements)"},
      {"name": ".inject", "sig": ".inject(el[, where])"},
      {"name": ".grab", "sig": ".grab(el[, where])"},
      {"name": ".dispose", "sig": ".dispose()"},
      {"name": ".destroy", "sig": ".destroy()"},
      {"name": ".empty", "sig": ".empty()"},
      {"name": ".clone", "sig": ".clone([contents][, keepid])"},
      {"name": ".tween", "sig": ".tween(property, value)"},
      {"name": ".morph", "sig": ".morph(properties)"},
      {"name": ".highlight", "sig": ".highlight([start][, end])"},
      {"name": ".fade", "sig": ".fade([how])"},
      {"name": ".setStyle", "sig": ".setStyle(property, value)"},
      {"name": ".getStyle", "sig": ".getStyle(property)"},
      {"name": ".setStyles", "sig": ".setStyles(styles)"},
      {"name": ".addClass", "sig": ".addClass(className)"},
      {"name": ".removeClass", "sig": ".removeClass(className)"},
      {"name": ".toggleClass", "sig": ".toggleClass(className)"},
      {"name": ".hasClass", "sig": ".hasClass(className)"},
      {"name": ".store", "sig": ".store(key, value)"},
      {"name": ".retrieve", "sig": ".retrieve(key[, default])"},
      {"name": ".each", "sig": ".each(fn[, bind]) — Array method"},
      {"name": ".delay", "sig": ".delay(delay[, bind[, args]]) — Function method"},
      {"name": ".periodical", "sig": ".periodical(interval[, bind[, args]]) — Function method"}
    ]
  },
  "prototype": {
    "display": "Prototype.js",
    "entries": [
      {"name": "$", "sig": "$(id...)"},
      {"name": "$$", "sig": "$$(cssRule...)"},
      {"name": "$F", "sig": "$F(element)"},
      {"name": "$A", "sig": "$A(iterable)"},
      {"name": "$H", "sig": "$H(object)"},
      {"name": "$R", "sig": "$R(start, end[, exclusive])"},
      {"name": "$w", "sig": "$w(string)"},
      {"name": "Class.create", "sig": "Class.create([superclass][, methods...])"},
      {"name": "Object.extend", "sig": "Object.extend(destination, source)"},
      {"name": "Element.extend", "sig": "Element.extend(element)"},
      {"name": "Event.observe", "sig": "Event.observe(element, eventName, handler)"},
      {"name": "Event.stop", "sig": "Event.stop(event)"},
      {"name": "Form.serialize", "sig": "Form.serialize(form[, options])"},
      {"name": "Form.reset", "sig": "Form.reset(form)"},
      {"name": "Ajax.Request", "sig": "new Ajax.Request(url[, options])"},
      {"name": "Ajax.Updater", "sig": "new Ajax.Updater(container, url[, options])"},
      {"name": "Ajax.PeriodicalUpdater", "sig": "new Ajax.PeriodicalUpdater(container, url[, options])"},
      {"name": ".show", "sig": ".show()"},
      {"name": ".hide", "sig": ".hide()"},
      {"name": ".toggle", "sig": ".toggle()"},
      {"name": ".visible", "sig": ".visible()"},
      {"name": ".addClassName", "sig": ".addClassName(className)"},
      {"name": ".removeClassName", "sig": ".removeClassName(className)"},
      {"name": ".hasClassName", "sig": ".hasClassName(className)"},
      {"name": ".toggleClassName", "sig": ".toggleClassName(className)"},
      {"name": ".observe", "sig": ".observe(eventName, handler)"},
      {"name": ".stopObserving", "sig": ".stopObserving([eventName[, handler]])"},
      {"name": ".update", "sig": ".update([newContent])"},
      {"name": ".insert", "sig": ".insert(content)"},
      {"name": ".up", "sig": ".up([cssRule][, index])"},
      {"name": ".down", "sig": ".down([cssRule][, index])"},
      {"name": ".each", "sig": ".each(iterator[, context])"},
      {"name": ".map", "sig": ".map(iterator[, context])"},
      {"name": ".detect", "sig": ".detect(iterator[, context])"},
      {"name": ".findAll", "sig": ".findAll(iterator[, context])"},
      {"name": ".invoke", "sig": ".invoke(methodName[, args...])"},
      {"name": ".pluck", "sig": ".pluck(property)"}
    ]
  },
  "backbone": {
    "display": "Backbone.js",
    "entries": [
      {"name": "Backbone.Model.extend", "sig": "Backbone.Model.extend(properties[, classProperties])"},
      {"name": "Backbone.Collection.extend", "sig": "Backbone.Collection.extend(properties[, classProperties])"},
      {"name": "Backbone.View.extend", "sig": "Backbone.View.extend(properties[, classProperties])"},
      {"name": "Backbone.Router.extend", "sig": "Backbone.Router.extend(properties[, classProperties])"},
      {"name": "Backbone.history.start", "sig": "Backbone.history.start([options])"},
      {"name": "Backbone.sync", "sig": "Backbone.sync(method, model[, options])"},
      {"name": ".get", "sig": ".get(attribute)"},
      {"name": ".set", "sig": ".set(attributes[, options])"},
      {"name": ".has", "sig": ".has(attribute)"},
      {"name": ".unset", "sig": ".unset(attribute[, options])"},
      {"name": ".save", "sig": ".save([attributes][, options])"},
      {"name": ".fetch", "sig": ".fetch([options])"},
      {"name": ".destroy", "sig": ".destroy([options])"},
      {"name": ".toJSON", "sig": ".toJSON([options])"},
      {"name": ".isValid", "sig": ".isValid([options])"},
      {"name": ".on", "sig": ".on(event, callback[, context])"},
      {"name": ".off", "sig": ".off([event][, callback][, context])"},
      {"name": ".once", "sig": ".once(event, callback[, context])"},
      {"name": ".trigger", "sig": ".trigger(event[, args...])"},
      {"name": ".listenTo", "sig": ".listenTo(other, event, callback)"},
      {"name": ".stopListening", "sig": ".stopListening([other][, event][, callback])"},
      {"name": ".add", "sig": ".add(models[, options])"},
      {"name": ".remove", "sig": ".remove(models[, options])"},
      {"name": ".reset", "sig": ".reset([models][, options])"},
      {"name": ".at", "sig": ".at(index)"},
      {"name": ".where", "sig": ".where(attributes)"},
      {"name": ".findWhere", "sig": ".findWhere(attributes)"},
      {"name": ".sortBy", "sig": ".sortBy(iteratee)"},
      {"name": ".pluck", "sig": ".pluck(attribute)"},
      {"name": ".create", "sig": ".create(attributes[, options])"},
      {"name": ".render", "sig": ".render()"},
      {"name": ".setElement", "sig": ".setElement(element)"},
      {"name": ".delegateEvents", "sig": ".delegateEvents([events])"},
      {"name": ".undelegateEvents", "sig": ".undelegateEvents()"},
      {"name": ".navigate", "sig": ".navigate(fragment[, options])"},
      {"name": ".route", "sig": ".route(route, name[, callback])"}
    ]
  },
  "underscore": {
    "display": "Underscore.js",
    "entries": [
      {"name": "_.each", "sig": "_.each(list, iteratee[, context])"},
      {"name": "_.map", "sig": "_.map(list, iteratee[, context])"},
      {"name": "_.reduce", "sig": "_.reduce(list, iteratee[, memo][, context])"},
      {"name": "_.filter", "sig": "_.filter(list, predicate[, context])"},
      {"name": "_.reject", "sig": "_.reject(list, predicate[, context])"},
      {"name": "_.find", "sig": "_.find(list, predicate[, context])"},
      {"name": "_.where", "sig": "_.where(list, properties)"},
      {"name": "_.findWhere", "sig": "_.findWhere(list, properties)"},
      {"name": "_.pluck", "sig": "_.pluck(list, propertyName)"},
      {"name": "_.sortBy", "sig": "_.sortBy(list, iteratee[, context])"},
      {"name": "_.groupBy", "sig": "_.groupBy(list, iteratee[, context])"},
      {"name": "_.countBy", "sig": "_.countBy(list, iteratee[, context])"},
      {"name": "_.shuffle", "sig": "_.shuffle(list)"},
      {"name": "_.sample", "sig": "_.sample(list[, n])"},
      {"name": "_.first", "sig": "_.first(array[, n])"},
      {"name": "_.last", "sig": "_.last(array[, n])"},
      {"name": "_.rest", "sig": "_.rest(array[, index])"},
      {"name": "_.flatten", "sig": "_.flatten(array[, depth])"},
      {"name": "_.uniq", "sig": "_.uniq(array[, isSorted][, iteratee])"},
      {"name": "_.union", "sig": "_.union(*arrays)"},
      {"name": "_.intersection", "sig": "_.intersection(*arrays)"},
      {"name": "_.difference", "sig": "_.difference(array, *others)"},
      {"name": "_.zip", "sig": "_.zip(*arrays)"},
      {"name": "_.object", "sig": "_.object(list[, values])"},
      {"name": "_.keys", "sig": "_.keys(object)"},
      {"name": "_.values", "sig": "_.values(object)"},
      {"name": "_.pairs", "sig": "_.pairs(object)"},
      {"name": "_.invert", "sig": "_.invert(object)"},
      {"name": "_.pick", "sig": "_.pick(object, *keys)"},
      {"name": "_.omit", "sig": "_.omit(object, *keys)"},
      {"name": "_.defaults", "sig": "_.defaults(object, *defaults)"},
      {"name": "_.clone", "sig": "_.clone(object)"},
      {"name": "_.extend", "sig": "_.extend(destination, *sources)"},
      {"name": "_.has", "sig": "_.has(object, key)"},
      {"name": "_.isEqual", "sig": "_.isEqual(object, other)"},
      {"name": "_.isEmpty", "sig": "_.isEmpty(collection)"},
      {"name": "_.isArray", "sig": "_.isArray(object)"},
      {"name": "_.isObject", "sig": "_.isObject(value)"},
      {"name": "_.isFunction", "sig": "_.isFunction(object)"},
      {"name": "_.isString", "sig": "_.isString(object)"},
      {"name": "_.isNumber", "sig": "_.isNumber(object)"},
      {"name": "_.isUndefined", "sig": "_.isUndefined(value)"},
      {"name": "_.debounce", "sig": "_.debounce(function, wait[, immediate])"},
      {"name": "_.throttle", "sig": "_.throttle(function, wait[, options])"},
      {"name": "_.once", "sig": "_.once(function)"},
      {"name": "_.memoize", "sig": "_.memoize(function[, hashFunction])"},
      {"name": "_.delay", "sig": "_.delay(function, wait[, *arguments])"},
      {"name": "_.template", "sig": "_.template(templateString[, settings])"},
      {"name": "_.random", "sig": "_.random(min[, max])"},
      {"name": "_.escape", "sig": "_.escape(string)"},
      {"name": "_.now", "sig": "_.now()"},
      {"name": "_.chain", "sig": "_.chain(object)"}
    ]
  },
  "knockout": {
    "display": "Knockout.js",
    "entries": [
      {"name": "ko.observable", "sig": "ko.observable([initialValue])"},
      {"name": "ko.observableArray", "sig": "ko.observableArray([initialArray])"},
      {"name": "ko.computed", "sig": "ko.computed(evaluator[, targetObject])"},
      {"name": "ko.pureComputed", "sig": "ko.pureComputed(evaluator[, targetObject])"},
      {"name": "ko.applyBindings", "sig": "ko.applyBindings(viewModel[, rootNode])"},
      {"name": "ko.applyBindingsToNode", "sig": "ko.applyBindingsToNode(node, bindings[, viewModel])"},
      {"name": ".subscribe", "sig": ".subscribe(callback[, callbackTarget][, event])"},
      {"name": ".extend", "sig": ".extend(extenders)"},
      {"name": "ko.toJSON", "sig": "ko.toJSON(viewModel[, replacer][, space])"},
      {"name": "ko.toJS", "sig": "ko.toJS(viewModel)"},
      {"name": "ko.mapping.fromJS", "sig": "ko.mapping.fromJS(data[, mappingOptions][, target])"},
      {"name": "ko.components.register", "sig": "ko.components.register(name, configuration)"},
      {"name": "ko.bindingHandlers", "sig": "ko.bindingHandlers.<name> = { init, update }"},
      {"name": "ko.extenders", "sig": "ko.extenders.<name> = function(target, options)"},
      {"name": "ko.utils.arrayForEach", "sig": "ko.utils.arrayForEach(array, action)"},
      {"name": "ko.utils.arrayFilter", "sig": "ko.utils.arrayFilter(array, predicate)"},
      {"name": "ko.utils.arrayMap", "sig": "ko.utils.arrayMap(array, mapping)"},
      {"name": "ko.contextFor", "sig": "ko.contextFor(node)"},
      {"name": "ko.dataFor", "sig": "ko.dataFor(node)"},
      {"name": "ko.cleanNode", "sig": "ko.cleanNode(node)"},
      {"name": "ko.isObservable", "sig": "ko.isObservable(instance)"}
    ]
  }
}
