package org.netbeans.lib.terminalemulator;

/** A stand-in with the platform Term's exact name, so the hierarchy match can be proven without the module. */
public class Term extends javax.swing.JComponent {
}
