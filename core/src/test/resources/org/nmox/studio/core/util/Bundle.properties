# Test-only bundle. Bundles.optional's two MISS branches are different code
# paths — no bundle at all (the catch) and a bundle without the key (the
# ternary) — and without this file every test in BundlesTest took the first
# one, so the ternary's fallback arm went unexercised and a mutant lived.
Bundles_presentKey=a value from the bundle
