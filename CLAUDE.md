# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

NMOX Studio is a NetBeans Platform-based IDE for modern web development, with first-class polyglot support (JS/TS, Java, C/C++, Python, Ruby, Rust, Go, PHP, shell + configs), NPM integration, project templates, and build tools. It's built as a multi-module Maven project with the NetBeans Rich Client Platform (RCP).

**Status**: shipping (v2.69.8), **Apache-2.0** since v1.207.0. **The 2026-08-29→09-02 three-lens run (v2.48.0–v2.56.0, nine releases)**: **the competitive lens** (docs/engineering/competitive-lens.md — the rival's relief list as a living backlog: v2.48.0 Edit with ORACLE (single-fence parse → BEFORE/AFTER preview → stale-buffer-guarded one-undo apply, refuse-not-truncate cap in the record ctor); v2.49.0 Go to Symbol project-wide (NmoxSymbolProvider feeds the platform jumpto dialog ⌥⇧⌘O AND its ⌘I bridge — the walk found the leading-sigil miss and the bridge's removeNonJavaChars query folding, both mirrored; the windows editor floor answered by the coverage seam never the floor); v2.49.1 the review (trailing-newline glue, depth cap); v2.50.0 BATCH the Tests window ⌥⌘2 (TestIndex speaks RunFocusedTestAction's own patterns — one vocabulary) + Draft Commit Message with ORACLE (git.diff consent kind, the chip's boot-law gate taught its lawful sites); v2.51.0 BATCH Pull Requests on the chip (user's own gh = keychain-only by never holding a token) + Dependabot the split way (slf4j-api 1.7→2.0 REFUSED as the v2.21.6 OSGi-placement pin)); **the futures lens** (docs/engineering/futures-2031.md — six bets argued from real trajectories, every unit paying today: v2.52.0 WIT/Component-Model citizenship (87 grammars, own outline family) + the everyday-2031 vocabulary (Temporal, navigator.gpu, startViewTransition, LanguageModel); v2.53.0 import maps (exact-then-LONGEST-prefix, bare-only, ⌘-click + ⌃Space)); and **the Agent Port (MCP)** (F4: v2.54.0 READ-ONLY BY CONSTRUCTION — loopback-witnessed Streamable HTTP, per-start bearer, any-Origin→403, McpReadOnlyLedgerTest walking the package; v2.55.0 the best-for-developers pass — structuredContent under outputSchema, readOnlyHint annotations, diagnostics(file), one-shot ide_context; v2.56.0 all three primitives — Resources as URI-views over tools, Prompts folding live state). Rhythm change after David's "all our time waiting on gates": worktree pipelining + batch mode; laws: full verdict lines only (-q ate a mutant twice), JSONObject hash order needs a deterministic distinguisher, rebase --onto for pipelined branches past a squash, audit background tasks (a hung rm -i fed git add -A). **The 2026-08-23→25 platform-and-polish run (v2.35.0–v2.38.4, ~26 releases in three days)**: **the platform advanced** (v2.35.0 — RELEASE300→RELEASE310 on David's GO, every dossier gauntlet measured PASS; v2.35.1 the update boundary OBSERVED: the Plugin Installer names the missing specs and refuses to advance while the headless --update-all half-installs — never script updates across a platform jump); **experiments became the learning front door** (v2.36.0/.1 — every experiment born with an EXPERIMENT.md that OPENS on aim, edit-file claims parity-gated against the real generators, install-at-creation so the FIRST Run succeeds, the shelf teaching count+disk-cost+lifecycle); **the zero-defect campaign** (v2.36.2–.5 — the law sweep found ELEVEN wrong-room IRC answers + FOUR unhealed studio parsers + the infra dup-id node swallow, all healed at parse; the hostile-input lens caught JsonTypes emitting broken TS for quote-carrying keys); **the scrum suite completed** (v2.37.0–v2.38.2 — sprints with burndown/report/close + roll-over pre-fill + velocity, the Standup opening "Sprint 8 · day 3 of 14"); **a11y and i18n both directions** (v2.37.4/.5 — the A11y and I18n Kit wizards joining the family + the IDE name sweep + the Turkish-I sweep with CaseFoldLocaleGateTest); **the output gauntlet** (v2.37.1 — all 15 templates run for real, Docker recipes built+smoke-run); **ledger 82 delivered** (v2.37.8 — suffixless Angular templates get the template editor via the two-half seam, the walk catching each half alone failing); **the story tutorial** (v2.38.3 — docs/a-day-at-meridian.md, one build through every area, eleven shots captured live; its own walk found the DevTools DOM pane DEAD — the Turkish-I sweep had rewritten .toLowerCase() inside injected JS, "Can\'t find variable: java" — healed + InjectedJsPurityTest/GeneratedJsPurityTest gating the cross-language sweep leak at every JS emitter); and **every studio walked clean on the new platform** (Block v2.37.7, API v2.38.2, Contract v2.38.4 with the 36-day address-book row surviving the move). The ship pipeline hardened alongside: scripts/ship-gate.sh lives in-repo, parameterized, fetch-never-checkout, with batches pipelined branch-atop-branch. **The 2026-08-23 overnight tail (v2.34.2–v2.34.4)**: the Kitchen Sink catches up (v2.34.2 — class-intelligence + env-keys + API-export stations, every claim test-matched; the count gate's DOUBLE blind spot closed failing-first: kitchen-sink.md joins the census AND the matcher learns bare "NN grammars", the stale 85 named by file+line); the night audit (v2.34.3 — zero-boot-spawns re-verified on the shipped app ~340 releases after v1.38.0; tools floor 0.75, the one honest margin); and the IRC Libera walk (v2.34.4 — TLS/join/echo//lastlog CLEAN ~25 releases on; the find: a malformed /filter add answered into the NETWORK-STATUS transcript while the user watched a channel — command feedback now lands WHERE the command was typed, FilterFeedbackInPlaceTest with the revert mutant by name; the live session shot became the user guide's first IRC illustration; the gate-template's own stale docs-landed literal ("## [2.26.0]" proving nothing) caught and repinned to the current entry). **The 2026-08-22/23 night (v2.33.1–v2.34.1)**: the day review (v2.33.1 — JsonTypes' javadoc claimed | null unions the code never emitted, the v1.189.0 law's third strike, probed live before fixing: a null beside a non-null sibling VANISHED into `bio: string`; findRoute's exact pass never stripped query strings); batch 10 (v2.34.0 — Test in API Studio on a route line drafts the request (verb+path via the v1.292.0 rule, {{base_url}} url, From-the-editor collection with same-name reuse — the fetch→route jump's server-side twin, walk-proven with the draft selected and its status line) + Copy TS Types on .json files (walk-proven: nested interfaces named after the file) + the day's update gauntlet PASS (stock 2.30.1 → 2.33.1 in-app, eleven jars byte-verified, clean reboot)); and the night close (v2.34.1 — the review: CopyTsTypes read disk BYTES, not the open BUFFER, so unsaved edits typed stale content — the buffer wins now; app.all's refusal documented and pinned (a draft needs ONE method); openWithDraft verified CLEAN against the documented synchronous-load contract). **The 2026-08-22 day (v2.31.0–v2.33.0)**: the full-stack wishlist (v2.31.0, David's ask — env keys first-class with completion + jump and secret-truncating hints, the fetch-path → Express-route jump, Copy fetch beside Copy curl); **the illustrated docs** (v2.32.0 + v2.32.1, David's call "illustrate everything with screenshots" — the eleven forge tabs regenerated at current bytes, EIGHT staged feature shots captured live from the assembled app and visually verified (swatches through tokens/hsl/oklch, class/env completion popups with provenance, the Rename Class dialog, the clocked Task Board with its live ticker, the Standup report, the Motion pane PLAYING with the banner caught mid-marquee, DB Studio's grid on a real six-row SQLite session), the user guide's chapters all illustrated incl. five forge shots that had rotted out of the story unreferenced and the env-keys section v2.31.0 never wrote; ImageRefsTest gates dead refs AND orphan images, failing-first with 11 orphans named; capture laws recorded — window-id capture excludes popups, hide-the-host + full-screen + crop is the popup recipe); and **the wishlist's second helping** (v2.33.0 — all three of v2.31.0's recorded wishes granted: Copy TS types beside Copy fetch (pure JsonTypes codec, sorted deterministic emission, merged array elements with optionals, honest unknowns), Run Script on package.json's scripts lines (pure NpmScripts caret rule — a dependency named like a script never runs, nested scripts objects refused — through the v1.103.0 trust-gated NpmService path), and param-aware fetch→route matching (/api/users/123 finds :id routes, exact always wins, query strings ignored); five mutants by name under the full-verdict protocol). **The 2026-08-21 evening + 2026-08-22 (v2.30.0-v2.31.0)**: batch 8 (v2.30.0 - the class arc reaches JavaScript: querySelector/closest/matches + classList family completion and ⌘-click to the rule; the walk caught the MID-TOKEN ACCEPT stranding token tails in BOTH the class item and the v1.330.0 var( item - tail-fold fix re-walked to a clean .hero; the floor trip answered by coverage, never the floor); the arc review (v2.30.1 - the split-editor caret mismatch in Rename Class fixed via the documentBelongsTo identity rule); and the full-stack wishlist (v2.31.0, David's ask - env keys first-class with completion + jump and secret-truncating hints, the fetch-path → Express-route jump on the v1.292.0 rule, Copy fetch beside Copy curl). **The 2026-08-21 day shift — the class-intelligence arc (v2.27.0–v2.29.1, batch mode)**: batch 5 (v2.27.0 — class="…" completion from the project's real stylesheets with per-item provenance, ⌘-click class→rule; CssClasses the CssTokens sibling; the walk found the PLATFORM's CSL goto answering class clicks with linked-sheets-only resolution — HyperlinkOperation decompiled, position 10 taken with the reason in place, the modifier-click leg recorded automation-unverifiable with the hover consultation pinned live by an in-app probe); batch 6 (v2.28.0 — the REVERSE jump: ⌘-click a .selector → the class's first markup usage with an honest count; the quieter boot: all 82 completion registrations positioned, PositionedRegistrationsTest deriving the law from the generated layer, fresh boot ZERO Ordering warnings naming our rows); docs truth (v2.28.1); and **batch 7, the capstone** (v2.29.0 — Rename Class… project-wide: whole-token spans across stylesheets and markup from the pure core, the five-rung refusal ladder speaking at every rung (invalid name, rename-onto-existing per v1.284.0, census-at-cap because a PARTIAL rename is corruption, unsaved changes), FileObject-stream writes so open editors reload themselves; walk-proven — 4 spans in 3 files with the prose 'hero' untouched in the bytes, the collision refusal speaking its exact sentence; and the mutation harness itself caught lying: a -q grep faked two survivors that unfiltered reruns proved dead — proof protocol: full verdict lines only). **The 2026-08-21 day shift (v2.27.0–v2.28.0, batch mode)**: batch 5 (v2.27.0 — the class attribute knows your stylesheets: class="…" completion from the project's real CSS/SCSS/Less/Sass and the family's <style> blocks with per-item provenance, ⌘-click class→rule; CssClasses the CssTokens sibling, classes-only with the #id/hex-twin reason in the javadoc, the bounded walk extracted on its second consumer; the walk found the PLATFORM's CSL goto answering class clicks with linked-sheets-only resolution — HyperlinkOperation decompiled (top mime, HyperlinkProviders position order, first claim wins), position 10 taken with the reason in place, the hover consultation pinned live by an in-app probe, the modifier-click leg recorded automation-unverifiable (ledger-76 class)); and batch 6 (v2.28.0 — the REVERSE jump: ⌘-click a .selector in any stylesheet → the class's first class="…" usage in the project's markup with an honest count, whole-token and class-attr-only both mutation-pinned; plus the quieter boot: all 82 of the editor module's completion registrations gained positions, PositionedRegistrationsTest derives the law from the GENERATED layer (org-nmox rows only — the CSL processor emits platform rows we cannot position, blessed in place), fresh boot ZERO Ordering warnings naming our rows). **The 2026-08-20/21 batch-mode night (v2.24.0–v2.26.1, David's call: several units per gate)**: batch 2 (v2.24.0 — Emmet speaks CSS inside HTML style regions, ⌥⌘E on bgc:tomato in a style attribute or <style> block expanding as CSS with the abbreviation slice CLIPPED to the region so it can never swallow markup, plus the docs fold); batch 3 (v2.25.0 — the style-region family reaches Vue SFCs, Svelte, and Angular templates through ONE shared HtmlStyleRegions.MARKUP_MIMES list with MarkupFamilyParityTest failing the build if any surface misses any family mime; walk-proven on a real .vue first try); batch 4 (v2.26.0 — the batch arc's own review: tokens DECLARED in .vue/.svelte style blocks feed project-wide var( completion (walked live: --vue-brand from App.vue offered with its swatch in a sibling stylesheet), Emmet's colon form (bgc:tomato) joins the dialect the honest way — the review caught v2.24.0's changelog EXAMPLE was never in the dialect and made the claim TRUE with the written-declaration law pinned by a space distinguisher (w100 → width:100px vs w 100 refused); the gate template HARDENED after a near-miss: batch mode broke the old script's worktree assumptions and it would have tagged uncommitted wip — caught mid-flight, hand-tagged on the verified sha, the template now captures the merge sha from the PR record and proves docs/main-green/tag against THAT sha with no worktree access); and the README render fix (v2.26.1, David's catch live on GitHub — the by-audience paragraph sat INSIDE the front page's 2x2 screenshot table so GitHub absorbed each wrapped line as a table row and stranded the caption row as literal pipes; structure repaired keeping every word, and MarkdownTableShapeTest gates BOTH defect shapes (glued-paragraph, orphan-row) over README + docs/*.md, failing-first proven with 4 defects named by line — the class content greps can never see: every word survives, only the SHAPE breaks). **The 2026-08-20 evening — the BATCH mode begins (David's call: several units per gate)**: swatches and the picker reach HTML (v2.22.0 — pure HtmlStyleRegions holds the boundary that made it non-trivial: <style> blocks and style attributes swatch and pick, prose tomato stays prose, walk-proven on a fixture built to tempt false positives); the first batch (v2.23.0 — design tokens complete their reach into HTML BOTH directions: var( completion + ⌘-click + swatches-through-tokens inside style regions with CROSS-REGION resolution and prose refusal, and tokens declared in HTML <style> blocks feeding the project-wide scan, region-parsed); and the second batch (v2.24.0 — Emmet speaks CSS inside HTML style regions, ⌥⌘E on bgc:tomato in a style attribute or <style> block expanding as CSS with the abbreviation slice CLIPPED to the region so it can never swallow markup — the inline-template discipline's third home — plus this docs fold, because batching docs into feature gates is the point). **The 2026-08-20 late shift (v2.21.4–v2.21.6)**: the Block Studio walk all-clean (v2.21.4 — palette→codegen→loopback preview byte-proven over HTTP→save-to-source→stop-deregisters; its gate ate the day's one CI flake, rerun-greened on the same sha before the tag); **the sweep made true** (v2.21.5 — the every-studio claim had overlooked Contract Studio and Infra Designer; both walked clean, incl. the address book's 31-day-old deployment proving .nmoxweb3.json persistence across ~200 versions and the Infra illegal-wire refusal speaking its exact v1.271.0 message — a wrong shipped claim corrected by making it TRUE); the day's own update gauntlet PASS (stock 2.19.1 → 2.21.5 in-app across fifteen releases, eleven jars byte-verified, clean reboot); and **the RELEASE310 arc COMPLETE** (v2.21.6 — the NO-GO root-caused by a standalone Felix 7.0.5 probe naming the missing requirement verbatim: jgit imports org.slf4j [1.7.0,3.0.0) and RELEASE300 satisfied it only by ACCIDENT of cluster placement (transitive slf4j-api auto-wrapped as an extra/modules bundle; 310 routes it into testng's ext/ instead — no bundle, no export); the netigso suspect CLEARED, the "wedge" revealed as a modal Warning dialog; ONE explicit application-pom slf4j-api dependency pins placement on both platforms and the 310 assembly boots clean — ledger 84 is GO-READY, the bump a property flip with its gauntlets riding the PR; the new failure class recorded: an unbundled transitive dep serving an OSGi import is unpinned behavior — declare it explicitly). **The 2026-08-20 afternoon (v2.21.1–v2.21.3)**: the shift-close docs (v2.21.1); **the DBA walk** (v2.21.2 — ~90 releases after v1.266.0, CLEAN on every leg: placeholder discipline, the hostile grid, the PK-gated edit loop proven to disk, CSV '=SUM neutralization, Manage rename; ONE find fixed + gate-pinned — the connection CRUD's Add and Edit were the silent two while Remove spoke (CrudGesturesSpeakTest); the mutation lesson: the first mutant deleted a PRE-EXISTING offer-path status and survived — enumerate the matches before you mutate); and **the API walk, all-clean** (v2.21.3 docs — request create, {{repo}} resolving through the environment to a live GitHub 200 with today's own pushed_at, security grade B with honest absents, Copy curl byte-exact with vars resolved, history persisted across sessions); and **the Block Studio walk, all-clean** (v2.21.4 docs — palette→codegen→loopback preview byte-proven over HTTP→save-to-source on disk→stop-deregisters; the day's THIRD clean walk); and **the sweep made true** (v2.21.5 docs — v2.21.4's every-studio claim had overlooked Contract Studio and Infra Designer; both walked clean: 27 foundry artifacts + EIP-170 verdicts + the address book's 31-day-old Counter deployment proving .nmoxweb3.json persistence across ~200 versions, and the Infra wire gestures with the illegal direction refusing out loud — a wrong claim corrected by making it TRUE). **The 2026-08-20 day shift ran on (v2.20.1–v2.21.0)**: docs truth (v2.20.1); **the day review** (v2.20.2, hours after the split — .sass-declared design tokens were unscanned AND the naive fix would corrupt them (the until-;-or-} value pattern needs a per-dialect line bound, now chosen by every call site incl. the document-local paths); a //-commented token had REGISTERED as live in scss since tokens shipped (v1.330.0 — blankComments only knew /* */; the dialect that made // primary surfaced it; whitespace-guarded so https:// values survive); ng new --style=sass projects gained the styles↔component switcher (NgSwitch always knew .sass — only the popup registration was missing, the v1.313.0 class one mime over); the dialect-divorce review lens recorded: after a mime split sweep scanners' extension lists, dialect-broken parse assumptions, and registrations one mime over); **the RELEASE310 runtime probe** (v2.20.3 — RED: the assembled 310 app fails org.eclipse.jgit under Netigso ("state remains INSTALLED after start()") and wedges before module turn-on, with jgit bytes AND the cluster census IDENTICAL to 300 — the platform's OSGi host regressed; NO-GO until netbinox is root-caused; the v2.19.4 refusal vindicated by measurement — compile-green is NOT a boot guarantee); and **learning space #92, Sass (indented)** (v2.21.0 — the catalog's first stylesheet-dialect space, teaching the dialect through the product's own gestures with the refusals named as features; picker-walked live; 92 spaces across five doc homes). **The 2026-08-20 day shift closed at FIVE more releases (v2.19.2–v2.20.0)**: docs truth ×2 (v2.19.2 the day addendum; v2.19.3 the night close — the 2.10.0→2.19.1 update gauntlet PASS recorded + Known-Issues truth); **the Dependabot split** (v2.19.4 — junit/org.json/mongodb in, netbeans RELEASE300→RELEASE310 REFUSED structurally with dependabot.yml ignoring org.netbeans.{api,modules,cluster} and ledger 84 opened; the gate's docs-landed check byte-proves the RELEASE300 pin); **the device-author walk** (v2.19.5 — the v2.0.0 JSON-device loop walked in the shipped app from the doc alone, CLEAN: shelf first try, trust prompt on a virgin path with Keep Safe default and the honest refusal LCD, live-edit refusal dropping the file whole with the teaching log line; one docs fix — a trust grant covers SUBFOLDERS); **the RELEASE310 dossier** (v2.19.6 — ledger 84's measured half same-day: all four decompiled assumptions byte-identical 300→310, artifacts on Central, full verify GREEN under -Dnetbeans.version=RELEASE310 with every floor holding; GO remainder is runtime gauntlets, the call is David's); and **indented Sass, first-class** (v2.20.0 — the oldest Known-Issues line closed: text/x-sass with the canonical indented grammar (86 grammars) and the ride-along audit that IS a dialect divorce's real work — swatches/picker/tokens/completion/spellcheck///toggle/Compile-to-CSS follow, Emmet/Prettier/outline/stylelint deliberately out with test-pinned reasons; walk-proven end to end incl. the firstLine fix for a crashing sass quoted by its stack path). **The 2026-08-19 day closed at FOUR more releases (v2.17.1–v2.19.1)**: docs truth (v2.17.1); the polish pass (v2.18.0, David's ask — three lenses over the boot log, the newest surfaces, and every user-visible string: the editor layer's duplicate Editors/text folders stopped warning on every boot (LayerFolderDuplicateTest the law), the Task Board's refused gestures now SPEAK and WRITE NOTHING (a refused edge-move no longer rewrote the checked-in board file; the clock's blip-drop and one-clock-per-board silences message; walk-proven on the status line with the refused move's mtime byte-identical), two Enter-kills got v1.98.0 safe defaults (SONAR kill, Remove Dependency), and the README's front-page counts (78→85 grammars, 88→91 spaces, 58→60 manifests) were rotting under a green DocsCountGateTest — the gate lost its three blind spots); **the senior-RCP pass** (v2.19.0, David's ask — ledger 83 closed with the platform's own seam: the multi-mime LSP providers (deno/eslint/stylelint/ngserver/clangd) implement MultiMimeLanguageServerProvider and register through static SINGLETON factory methods, because LSPBindings' reuse map is keyed by provider INSTANCE and a class registration instantiates once per mime folder — the second mime presented a different instance and a second server copy started (two deno lsp / two tsserver / two ngserver per project, measured v1.356.0); the tsserver pair partitions by WORKSPACE KIND (TypeScriptServer multi-mime ts+js in plain workspaces; AngularJavaScriptTsServer js-only in Angular ones) because the ts/js authority split is workspace-conditional (ledger 81) while getMimeTypes() is static — a naive merge would have rebound tsserver beside ngserver, the proven double-rename; live A/B same day: shipped 2.18.0 ran TWO deno lsp for one .ts+.js workspace, the new bytes run ONE); and the night review (v2.19.1 — the conversion had missed CssServer's three-mime class registration because the gate checked the MECHANISM (classes implementing MultiMime) not the OUTCOME: MultiMimeSingletonGateTest now derives its population from the GENERATED layer, failing-first proven, the hand-kept factory list deleted; the walk law recorded: an LSP init failure (missing typescript install) produces a transient process population that fakes ANY process count — verify zero init failures in the log before counting). **The 2026-08-15/17 stretch closed at EIGHT more releases (v2.10.0–v2.17.0)**: WeeChat tranche 2 (v2.10.0 custom /filter + /lastlog, Libera-proven hidden-on-screen/kept-on-disk; v2.10.1 floors + the 2.x update-center gauntlet v2.2.1→v2.10.0 in-app; v2.10.2 the night review — local echo honors your filters, FilterWiringGateTest makes every append site consult the verdict); the Kitchen Sink tutorial (v2.11.0, David's ask — docs/kitchen-sink.md, 21 do/see stations, the refusals-are-features close); **DHTML lives** (v2.12.0, David's ask — the Motion pane in the Browser's DevTools: a keyframe timeline strip with clamped diamond drags, the paused-negative-delay scrub, Play/Stop preview via one injected style tag, Apply-to-Source through the shared `writeBackWith` refusal ladder with same-named blocks replaced at their LAST occurrence, seven DHTML-classic presets that are parser-proven fixtures; walk-proven incl. the 50% scrub freezing a full-width block at translateX(0) exactly); **MooTools, first-class** (v2.13.0, David's ask — learning space #91, the Classic Web (MooTools) template on the kit's vendored compat build, completion 44→105 with the floor gate raised, the honest project-level EOL chip that exposed and fixed `eolMessage()` hardcoding "jQuery" for any flagged library); **Vue and Svelte, top-notch** (v2.14.0, David's ask — the kit-AND-loader dead-chord cure: a grammar alone doesn't make a mime, and neither does a kit without a DataObject loader (the walk's Emmet differential found the loader layer beneath the kits); the Volar 3.x wall — it speaks `tsserver/request` to a client-hosted tsserver no generic LSP client can be — answered by pinning @vue/language-server@2 with `{typescript:{tsdk}, vue:{hybridMode:false}}` through the generalized init-options injector (the deno seam), headless-probe-proven then live-proven with a real template diagnostic; Vue typing/vocab parity with @/: sigils, Svelte 5 dotted runes, Emmet in both SFC mimes across all five keymap profiles); the **orphan-process audit** (v2.15.0, its own session — `CommandExecutor` keeps a live-set of every spawn and one exec-level shutdown-hook reaper TERMs every live tree, so long-running spawns outside the rack die with the JVM on clean quit AND SIGTERM; the repo-scan gates became worktree-safe in the same release); the arc review (v2.16.0 — **the Motion preview belongs to the element that started it**, the v1.172.0 law's third appearance in day-old code: pure MotionTargetGuard at Play/Scrub/Stop, mutation ×3 incl. the wiring gate; shipped through a same-day tag collision with the sibling session (renumbered+rebased), a GitHub partial outage (the explicit force-with-lease law), and a windows editor-floor coverage flake — the tag went on only after the merge commit proved green); and the day-shift close (v2.17.0 — the editor JaCoCo floor obeys its WINDOWS-binding measurement (0.68 there, floor 0.67, the v1.299.0 rack call) so the gate stops flaking on noise, the retarget law walked live in shipped 2.16.0 as the exact bug scenario, and the v2.15.0 reaper observed live taking a serve process down under SIGTERM). **The 2026-08-13/14 run closed at ELEVEN 2.x releases (v2.1.0–v2.9.0)**: the device gallery ships installed-and-active (v2.1.0, David: examples "should not be a step"); the WeeChat-power IRC tranche (v2.2.0 smart join/part/quit filter + Ctrl+J hotlist + op toolkit + /alias, v2.2.1 the Libera walk's find — a `WHEN_IN_FOCUSED_WINDOW` binding is DEAD in a docked TopComponent, the docked-TC key law's second strike); all five platform keymap profiles carry every NMOX chord (v2.3.0 — Emacs/Eclipse/IDEA/NetBeans55 get the same shadows as NetBeans, KeymapProfileParityTest the law, plus the QuickSearch-nested-in-Keymaps registration fix); and the **Task Board grew into a sprint suite**: the Board Overview dashboard face (v2.4.0 — WIP/flow/aging from stated definitions; the walk caught bare `JComponent.getAccessibleContext()` returning NULL), blockers-with-owners + derived epic legend + retro notes (v2.5.0; SpotBugs caught `abs(hashCode)`), the time clock (v2.6.0 — Clock In/Out sessions as [start,end] pairs, ONE clock board-wide, <60s blips dropped, midnight-clipped TIME report proven live), the senior Java pass (v2.7.0 — apiclient's WorkspaceFilePulse promoted to `core.util.FilePulse` before a second copy grew, Card de-telescoped 9→4 args, the board's own recorded v1 limit closed: live external-edit reload via FilePulse + isForeign, proven <4s in the shipped app), the junior-RCP comments pass (v2.7.1 — 638/638 class javadoc held from v1.202, so the gap was PACKAGES: 22 package-info neighborhood maps + guide Flow 5 + JuniorDocsGateTest), **the Standup** (v2.8.0 — one button turns the board's own records into the daily report: Yesterday/Today from done stamps + day-clipped sessions, Blockers from the register, Commits from a bounded fixed-argv `git log`; empty sections OMITTED; pure StandupReport so every rule is a unit test; walk-proven incl. pbpaste round trip), and the arc review (v2.9.0 — `fromJson` never enforced its own open-session claims: a keep-both merge of the checked-in file could leave a stray open pair NO gesture can reach while TIME and the Standup count it to NOW forever; healOpenSessions closes strays at their OWN start, latest start keeps the clock; the pattern: **every runtime invariant over a checked-in file needs a parse-time heal** — this parser now has two, duplicate ids v1.325.0 and open sessions v2.9.0). **v2.0.0 is THE PROGRAMMABLE RACK**: the fleet is no longer fixed — any `*.json` in `~/.nmox/devices.d/` becomes a real device on the shelf (knobs, buttons, LEDs, ports, patch cables, saved into the patch, ⌘I-reachable, flight-recorded), with no Java, no plugin build, and no restart. The decision was grounded not invented: the extensibility arc had recorded *every shipped catalog gets a user-writable drop-in sibling*, six had one by v1.305.0, and the seventh — the DEVICE catalog, the thing the product is named for — could only be extended by authoring a NetBeans plugin. The architecture made it cheap: because `ExtensionDevice` (v1.55.0's shape-B host) already gates every exec on workspace trust, derives colour from role, and demands accessible names, JSON devices inherited the whole security model on day one — **when the host enforces the laws instead of trusting plugins, a new plugin LANGUAGE costs almost nothing**. The format is the strictest drop-in because its commands EXECUTE: argv only (no shell metacharacters), bare tool names (never a path), no knob-built tool names, no unknown `{{vars}}`, declared ports only — and any breach skips the file WHOLE with its reason logged against the filename. `DeviceFileDocsTest` feeds every documented example to the real parser (worked examples are fixtures, not prose) and asserts the tutorial's deliberately-refused example really is refused. Additive by construction: 1.x patches, presets, workspaces and SPI plugins load unchanged. **The 2026-08-12/13 elevation arc closed at FOUR releases (v1.357–v1.360), from David's one-line directive ("elevate web development") to a click-proven capability**: the in-app Browser became SOURCE-AWARE — Pick element (crosshair in the live page → DOM-tree selection via child-index paths over the one-way bridge), Open Source (element → the HTML file at the producing LINE; URL→file only through vouched channels — file:// + the serving registry — with canonical containment; element→line by id or Nth-same-tag with comments/script bodies neutralized line-true; remote pages and script-generated elements refuse honestly), and Edit Style… (instant inline preview, then the declaration lands in the SOURCE stylesheet — the rule chosen by the page's own cascade answer, last match wins; compiled-output siblings, inline <style>, unserved sheets, and unsaved buffers refuse with reasons). The v1.359 arc review found three in day-old code — the pick poll ran FOREVER after a successful pick (programmatic setSelected fires no ActionListener: cleanup living only in an action handler is skipped by every programmatic path), a duplicated selector edited the DEAD first rule (the cascade honors the last), and page-controlled selectors gained the Java-side skip-not-truncate clip — and the v1.360 walk click-proved the loop in the installed app (pick → h1#headline ringed; Open Source → index.html:9 exact; Edit Style… → tomato repaint + "Saved to style.css (.tagline)" + gray→tomato replaced in place on disk). Also that stretch: v1.355 docs truth + v1.356 the toolchain walks (deno lsp spawn + injector frame ACCEPTED by the real server + zero tsserver on .ts AND .js; rust-analyzer via the rustup toolchain path; gopls via ~/go/bin; zero orphans ×3; ledger 83 records one-LSP-server-per-mime-per-project with decompiled evidence that safe dedup needs a client change or an LSP multiplexer). **The 2026-08-12 run closed at SIX releases (v1.349–v1.354)**: the Angular ledgers' do-it-all close (rename single-authority via mime suppression — the platform rename collects edits from EVERY bound server, decompiled; identifier ⌘B; resolver truth), then David's four directed passes — Deno v1.350 (deno lsp answers NOTHING without initializationOptions.enable=true, frame-injected at the one launch seam since the platform client has no seam; deno lint/fmt/test lanes; space #90), Rust v1.351 (rustup's rust-analyzer proxy exists-but-dies until the component installs, so only a --version exit 0 proves the server and failure re-probes for restart-free self-heal; clippy/fmt lanes), Go v1.352 (gofmt -l exits ZERO with unformatted files so GLOSS CHECK's verdict reads the OUTPUT; vet/golangci lanes), and the senior DevOps artifact walk v1.353 (every Dockerize Dockerfile BUILT against a real daemon — the Go recipe's `go build -o /name ./...` refused any module with a second package, so only the toy shape ever built; SPA try_files fallback live-proven; rust BuildKit cache mounts 16s→1s; base images to golang 1.26/rust 1.95/python 3.14/php 8.5/nginx 1.29) — closed by the arc review v1.354, which found the day's own pattern repeated: the JS-mime provider never yielded to deno lsp (the ledger-81 double-authority reborn one mime over — when a provider yields a mime, grep for its SIBLING registrations), the marker walks were unbounded (a stray ~/deno.json could hijack every project under $HOME; walks now stop at the repo's .git, marker-checked-first), and PURITY ignored the ROSETTA override that GLOSS honored (kind outranks a stray manifest, in BOTH lanes). **The overnight closed at TEN releases + two live walks (v1.335–v1.345)**: the IRC walk proved v1.322's engine-side logging live against Libera and its one finding (the reopened scrollback's silent gap) was fixed same-night as v1.344 (a dim marker line per restored transcript, structurally discriminated, never through the message path); the API Studio walk came back clean. **The 2026-08-10/11 overnight shift closed at EIGHT releases (v1.335–v1.342)**: floors+update-gauntlet, then the Emmet family completing itself — CSS abbreviations (exact-match, refusals untouched), the + combinator (all-or-nothing chains), deterministic lorem, and climb-up ^ (past-root and out-of-group climbs refuse, never clamp) emptied the v1.329 grammar's deliberately-out list; the v1.342 arc review ran PROBES-FIRST (six compositions printed open, judged, then pinned as EmmetArcProbeTest) and came back CLEAN — the arc's first no-finding review. **The 2026-08-11 overnight run continued (v1.338–v1.340)**: v1.338 the CSS + combinator (all-or-nothing chains at the line's indent); v1.339 deterministic lorem (canonical passage, pinnable by tests — real Emmet randomizes; decorated lorem refuses whole; markup outs down to climb-up ^); v1.340 this docs close. **The 2026-08-11 overnight tail (v1.335–v1.337)**: v1.335 the floors ratchet (ui line floor 0.56→0.57) + the update-center gauntlet v1.323→v1.334 (a stock portable self-updated IN PLACE through the real update center across the marathon's twelve releases, all 11 module jars byte-verified, clean reboot; the 'Updates not found' log TAIL is the second refresh converging, not a failure); v1.336 Emmet speaks CSS — the v1.329 grammar's recorded out closed as CssEmmet, an exact-match subset (keyword table + numeric family + colors + !important; fuzzy matching deliberately out because a wrong guess MUTATES the stylesheet; a token after ':' never expands), live-proven on all three stylesheet surfaces incl. the shipped-app .less walk, mutation-proven ×3; its ship gate died on the **CSS** literal (regex metachars, the gate-literal law's fifth strike) after merge, resumed by hand — docs gates now grep -Fq; v1.337 this docs close. **The 2026-08-10/11 six-release iteration (v1.329–v1.334, David's ask: the go-to product for web devs, designers, engineers)**: v1.329 Emmet expansion (⌥⌘E in HTML + Angular templates; written-down grammar subset, bare words expand only when they ARE elements, refusals leave text untouched); v1.330 design tokens (var( completion with color-swatch icons from a bounded mtime-cached project scan, ⌘-click usage→declaration, swatches resolving THROUGH the token document-locally); v1.331 the Classic Kit artifact walk — the SECOND all-clean walk ever (webpack 5.109/grunt 1.6/gulp 5 all ran the generated configs incl. ES6; never-clobber + npm-merge live; vendored bytes = the official pin), closing the kit work-list (PWA→Standards→Classic); v1.332 the shipped-app walk on the mime surfaces the dev proofs skipped — Emmet live on text/x-ng-template, tokens live on .scss, and the find: the AUTO-PAIR TRAP ({text} abbreviations refused because the caret sits before the auto-closed brace; abbreviationAt now folds trailing closers with the replacement span extended past the caret); v1.333 the arc review — five findings in ~1000 fresh lines: the token hyperlink REGRESSED the v1.234 hover law (per-⌘-move getText copy; same document-property version-cache cure, gate-pinned), the token cache hoarded stale versions per save (now keyed by path), completion accept rides CompletionEdits (public for its second package), Emmet's caret waits for the atomic edit to land, and the var( auto-popup got a plausible-cause startsWith fix recorded as UNVERIFIED with ⌃Space the proven path; v1.334 docs truth + this positioning. **The 2026-08-09/10 run (v1.319–v1.326) closed with the Task Board arc — build, walk, review, walk again — and every stage found what the one before it blessed**: v1.320's learner walk of space #89 died on its FIRST click (the STATIC lane pinned a port this box held; now core FreePorts probes and the announce reads the port the server's banner NAMES), v1.321's review found the gate law's mirror (call sites with green SEAM tests and no gate — every seam fix needs TWO proofs: the seam diverges AND the call site exists), v1.322 was a debt sprint (ledger 66 closed, floors ratcheted), v1.323 shipped the **Task Board** (⌥⌘1 per-project kanban, `.nmoxtasks.json` the sixth studio file, WIP limits advisory, plain-render + never-clobber + off-EDT laws carried from day one), v1.324 walked it installed (a planted `<html><img>` card title painted as characters; ⌘I unverifiable by this automation — a known-good control term also returned empty — so the reach is pinned at unit level instead of a fabricated fix), v1.325's review found Delete Column calling the model OUTSIDE the one mutate() path that repaints/saves/checks foreign edits (enumerating all twelve mutation call sites showed eleven lawful and one bare — the exception is the bug; now a build-failing gate) plus a one-shot keyboard move and merge-duplicate ids deleting BOTH cards, and v1.326's walk of the review found the layer beneath: **Popups.selectOnTrigger is inert on a drag-enabled JList** (proven by asserting the one side effect only that listener produces — an empty-space right-click must CLEAR the selection; it didn't), fixed via popupTargetList hooking getPopupLocation, the method Swing itself calls inside the popup path — and the repo-wide popup gate that caught the change on its first run now knows both lawful forms, because the law is the outcome, not the mechanism. Process scar worth keeping: v1.325.0 shipped WITHOUT its changelog entry (`reset --soft` + `commit` commits only STAGED content, and the working-tree grep in the ship gate could not tell) — the entry is restored, and docs-landed gates now read COMMITTED content (`git show HEAD:…`), which caught a wrong literal on its first use. **The 2026-08-08/09 run (v1.309–v1.318) moved verification into the INSTALLED build**: the Angular loop was walked end to end in the shipped app — template, four-file switcher, routes outline, ng serve → auto-opened Browser at [::1]:4200 → DevTools Angular pane reading the live tree — and each walk paid: a CSS one-liner rule invisible to the Navigator for ~300 releases (CSS_SEL demanded the brace end the line), the Node-floor wall (`ng serve` hard-refuses node < 20.19/22.12 while npm install sails through — now translated at the CommandExecutor pump, the v1.264.0 idiom's second wall), the PWA kit precaching 2 of its own 5 icons, and conflict markers that rode SIX releases in CHANGELOG.md and PlainTables.java because every docs gate reads substrings and a substring survives a marker above it (the repo-wide shape gate found the second instance the first time it ran). The run also closed two arcs: the **six-seam drop-in family** (learn-catalog.d / templates.d / presets.d / api-library.d / dockerize.d / doctor.d) and the **html-render security class** (PlainTables at construction across tables, IRC, and cell renderers — escape-or-disable depends on whether a renderer MEANS its markup). **The persona-lens arc (v1.210–v1.215) found bugs ~200 releases of senior review had not**, because a persona uses the product where a reviewer reads it: a junior hit a shipping TextMate grammar that crashed on their FIRST project (v1.210.0); a mid-level dev found the IDE's own Run button passing an EMPTY LAMBDA for output — so it never registered a serving, silently disabling the ⇄ chip, ⌘I Live Servers, VITALS targeting and API Studio's {{baseUrl}} offer, a whole chain the product had already paid for (v1.212.0); and an information architect measured that **24 of 49 ordinary search terms returned NOTHING** — `bundle size` missed "Bundle-Size Gate" on the hyphen, `logs` missed "Log Follower" by a plural, and `ai` matched TAIL and ANVIL by substring luck (v1.215.0: one term-based matcher for all eight search surfaces, plus a controlled device vocabulary; 0 of 49 empty after). The lesson worth keeping: **an empty callback and an unsearchable name are invisible to code review — there is no wrong logic to spot — and visible immediately to anyone trying to USE the thing.** Angular is the framework bet (v1.214.0 Angular Language Service; David's call: stop investing in React). **The 2026-07-30/31 run: fifteen releases, and the IDE became somewhere you can live** (v1.194–v1.208). The **in-app browser** (v1.199.0: JavaFX WebView lit by an FX-bundled runtime, ⌥⌘4, Hacker News as home since v1.204.0) grew **DevTools** (v1.206.0 — WebView ships no inspector and no remote-debug protocol, so we own the engine: Console/DOM/Network/Storage/Vue panes over `executeScript` + an injected bridge held by a STRONG ref (JSObject.setMember holds weakly); a Vue production build exposes no component tree and the pane says exactly that). A **full IRC client** (v1.204.0 freenode-by-default on ⌥⌘3; v1.205.0 SASL/IRCv3 caps/tab completion/highlights/clickable URLs→Browser/logging//list/WHOIS/ignore/network editor — Escape is consumed above a docked TopComponent so clearing is Ctrl+U). **Svelte became a full vertical** (v1.207.0: comment toggle, typing, Svelte-5 rune/block/directive completion, a Vite+Svelte 5 template, a `svelteserver` probe, and a DevTools Svelte pane that maps live DOM back to source via `__svelte_meta` — Svelte compiles components away, so source mapping IS the honest ceiling). David's **coverage-and-comments** ask landed as v1.202–v1.203 (behaviour tests across every module, floors ratcheted, plus docs/engineering/codebase-guide.md — the beginner's walk through the five RCP ideas). David **relicensed the project to Apache-2.0** (v1.207.0: LICENSE + a root NOTICE attributing the NetBeans Platform, README/pom/SBOM/docs aligned, a 5-test gate that now covers the docs; permissive-to-permissive, deliberate, NOT legally required — releases ≤1.206.0 stay MIT), and v1.208.0 put LICENSE+NOTICE inside all five installers. **The arc review (v1.208.0) found two security bugs and one silent corruption in four-day-old code, and both security bugs were the same mistake: trusting a platform default.** A raw `SSLSocket` validates the chain but NOT the hostname, so IRC's TLS accepted any CA-valid certificate for any domain and would have handed the SASL/NickServ credentials to an on-path attacker; a Swing `JLabel` starting with `<html>` RENDERS it, so a page naming a component `<html><img src="http://evil/…">` made the IDE's own JVM fetch that URL. The corruption: the markup comment toggle, on a line like `<!-->` that is shorter than its own marker pair, ate the newline and the head of the next line. All four fixes were then **click-verified in the shipped 1.208.0 app, two of them behaviourally** — an empty beacon log proving no fetch ever happened, and a ticking page that stops the instant the Browser tab closes (it used to run until the IDE quit) — plus a real TLS connect to freenode (`TLSv1.3-TLS_AES_256_GCM_SHA384`) proving the hardening doesn't break real use. Standing counts: 53 devices, 85 grammars, 88 learning spaces, 58 manifests. **The night shift 2026-07-26/27: five more releases, the reviews kept paying** (v1.185–v1.189: the night review caught the Image Kit's scan — a depth-12, 500-file disk walk — running ON THE EDT in day-old code (v1.185.0, the v1.33.1 class; probes now ride the kit's RP, `ImageKitActionSafetyTest` source-gates the ordering); **Postman environment import** (v1.186.0: the v1.177.0 refusal became the feature and the refusal message now points at the menu item; `"type":"secret"` values NEVER cross into the committable .nmoxapi.json — counted, keychain named; same-name imports merge putIfAbsent); **the night gauntlet byte-proven in the installed app** (v1.187.0: Image Kit pressed 17.8 MB → 164 KB + 341 KB webp with originals untouched and the UI responsive — the EDT fix observed live; env import's planted secret absent from disk; the brew-races-homebrew-job trap recorded — check the cask VERSION before gauntleting); **Insomnia v4 import** (v1.188.0: the third migration source — flat resources[] restructured via parentId chains into "Folder / Request" names, `{{ _.name }}` → `{{name}}` everywhere, secrets law at the third border, WebSocket/gRPC counted out; import family now curl/.http/OpenAPI/Postman-collections/Postman-environments/Insomnia/HAR in, .http out); **the Insomnia review** (v1.189.0: the env-merge comment claimed base-favoring while the code favored resources[] order — proven failing-first with a sub-env-first fixture, fixed by ancestry bucketing; method lesson: *a comment claiming an ordering property is a test not yet written*)). **The day shift 2026-07-26: nine releases, the migration story complete** (v1.176–v1.184: the Explain-anything tutorial + full CHANGELOG link-block regeneration (v1.176.0, 217 versions, the block had dead-ended at v1.129.0); **Postman Collection v2.x import** (v1.177.0: folders→"Folder / Request" names, {{vars}} verbatim — they ARE our syntax, bearer/basic auth keychain-side incl. collection-level inheritance, honest refusals for v1/env-files/multipart/scripts); **HAR import** (v1.178.0: XHR/fetch only when Chrome types the capture, Cookie headers dropped-and-counted, captured Bearer/Basic lifted keychain-side, dupes collapsed, over-cap bodies refused-not-truncated); **.http export** (v1.179.0: the v1.166.0 dialect both ways — auth deliberately NOT exported, per-request keychain comments, parse(render(c)) pinned with auth as the one written exception); **floors ratchet** (v1.180.0: all ten JaCoCo minimums to measured-minus-margin, none lowered); **the import-arc review** (v1.181.0, two finds fixed: a captured non-Bearer/Basic Authorization survived HAR import as a plaintext row — a HAR is a RECORDING so it now follows the Cookie rule, and a ###-carrying body split the .http round trip into TWO requests, proven failing-first — now omit-and-say); **the migration gauntlet PASS in the installed app** (v1.182.0: Postman/HAR/export click-verified live with byte-level secret scan finding none of three planted secrets; new Migrating-from-Postman tutorial); **Image Kit (Web)** (v1.183.0, David-directed: File ▸ Image Kit (Web)… presses a project's images — pure-Java .min.jpg re-encode with quality/downscale choices, .webp siblings via the user's own cwebp, kit laws on pixels (never-clobber, under-10%-savings discarded as already-tight, alpha flattens white), live-proven 17.8 MB → 347 KB; Doctor probes cwebp); the import family is now curl/.http/OpenAPI/Postman/HAR in, .http out. Process: one Maven-Central-403 release flake healed by rerun --failed; the multi-line-pr-create deriver bug caught twice then killed by mandatory bash -n). **The AI-arc review: the same hazard, in the new consumer** (v1.175.0: a re-aim left DB Studio's error tab up with Explain armed on the PREVIOUS project's SQL — the v1.172.0 API Studio class, found by asking whether consumer 2 survived what consumer 1 failed; run + re-aim now share one clearResultTabs(), mutation-proven; the law "a result belongs to the workspace that produced it" is stated at both clearing sites). **DB Studio explains its errors** (v1.174.0, the OracleAsk seam's SECOND consumer, accepted with no seam changes: Explain… under a failed statement sends the SQL + driver error + engine kind and nothing else — a failed statement has no rows, so the connection/password never appear, asserted STRUCTURALLY not by word-grep; literals deliberately unmasked because errors are often about them, and the consent line says so). **The Explain gauntlet: live-proven, one find** (v1.173.0: a real GitHub 404 explained by the real API in the shipped app — redaction kept x-ratelimit-remaining while dropping credentials, and the v1.172.0 re-aim fix confirmed live; the find — the conversation window opened and said "Thinking…" BEFORE consent, and a decline orphaned it, so the SPI adapter now asks first while the per-send gate stays; mutation-proven). **The API-arc review: one find, fixed** (v1.172.0: a project re-aim left Explain… armed with the PREVIOUS project's response — cosmetic staleness before v1.171.0, a cross-project disclosure path after it; applyWorkspace now clears the response and everything Explain could send, source-gated + mutation-proven; the seam gained a neutral default question; bounded-disclosure/OpenAPI-servers/empty-subject lenses CLEAN). **The AI surface reaches your API responses** (v1.171.0: Explain… on the response pane opens an ORACLE conversation about the response on screen; NEW core.spi.OracleAsk soft-dependency seam carrying TEXT ONLY — no rack dep in apiclient, null lookup = no button; the disclosure is redacted where the data lives (credential headers dropped AND counted, query values masked with names kept, 4k code-point-safe body prefix), ResponseDisclosure pure + 6 tests; the flow earns its OWN consent via the generalized requestKindConsent, 3 tests pinning no cross-flow leak). **OpenAPI 3 joins the Import… menu** (v1.170.0: a JSON spec becomes a collection — one request per operation, {petId}→{{petId}}, servers[0]→{{baseUrl}}, required query params enabled, JSON examples as bodies; YAML/Swagger-2/schema-only/security refused-or-noted honestly; OpenApiCodec petstore-pinned; the import trio complete: curl/.http/OpenAPI. PLUS the update-center gauntlet PASS: a v1.160 install updated itself in-app to 1.169 — digests, MIT license, 11 NBMs, clean restart, About showing 1.169.0 under the logo). **Night docs truth** (v1.169.0, docs only: plan.md carries the v1.164–v1.168 overnight API-surface arc + the branch-frozen and verify-catches lessons; standing counts current at 78 grammars). **The night-arc review: two finds, fixed** (v1.168.0: HttpFileCodec's startsWith("<") refused every XML body — the dialect file-ref is "< path", now checked as such, SOAP-body test-pinned; the .http file read moved off the EDT per the v1.108.0 Load-Patch law; rest of v1.164–v1.167 CLEAN). **The import surface, gauntleted** (v1.167.0: curl paste → masked keychain Bearer + no header row, byte-exact Copy curl, .http → named collection + env var, all click-verified live; two finds fixed — method-duped names, toolbar overflow hiding Delete → one Import… menu button). **.http request files are citizens** (v1.166.0: Import .http… reads the REST Client dialect into a collection — ### splits with names, bare-URL GETs, body-aware comments, {{vars}} verbatim (they ARE API Studio's syntax), @vars join the active environment never clobbering, same secrets-law Authorization lift as curl, < ./file refs refused honestly; vscode-restclient grammar vendored MIT sha256-pinned as the 78th grammar, grammar-only by design, # toggle; HttpFileCodec 7 tests). **API Studio speaks curl, both ways** (v1.165.0: Import curl… turns a pasted command into a saved request — shell-style tokenizer, Authorization-header lift into the keychain-backed Auth field per the secrets law, honest refusals for multipart/@file/ANSI-C, unknown flags noted never guessed; Copy curl emits the exact command Send would run with {{vars}} resolved; CurlCodec pure core, 17 tests incl. field-level round trip). **The night review: the plans obey their own law** (v1.164.0: five of v1.163.0's new preflight plans violated plansOnlyWhatExists with file-assuming commands — RACKET main.rkt / ELM src/Main.elm / RUBY rake / PHP phpunit / DART test-dir now probe before planning; parity fixtures carry the conditioning files; rest CLEAN or blessed). **The ship gate and the IDE lanes speak every toolchain** (v1.163.0: PREFLIGHT plans checks for ~30 more kinds — commands mirrored from the rack lane tables, builds release-hardened where offered, make/cmake/task-runners honestly out; WebProjectCommands fills nine silently-null kinds (Bun/Deno/Erlang/Clojure/Scala/Haskell/OCaml/Crystal/PHP) so F6/F11 stop greying; NEW PreflightLaneParityTest — same-tool coverage between IDE lanes and the ship gate, the v1.161.0 starvation class made structural, mutation-proven on FOUNDRY; both v1.161 kinds ALSO live-gauntleted in the assembled app: aiken check OK 0.513s, clarinet check OK 0.436s, kind chips + trust prompts verified on real scaffolds). **The kind-lattice review: one find, fixed** (v1.162.0: the v1.161.0 precedence inversion starved PreflightPlan — a Clarinet repo's npm ship checks vanished when CLARITY became primary; the plan now speaks clarinet check + the harness tests, Aiken repos gain aiken check, and the failure pattern (*a precedence flip starves consumers keyed on the old primary kind — grep every switch(kind) after one*) is in plan.md; Debug grey/Dockerize/WAYPOINT verified CLEAN or blessed; plan.md currency at v1.162.0). **Contract chains are project citizens** (v1.161.0: aiken.toml/Clarinet.toml/tact.config.json are recognized manifests (58) with three new ProjectKinds — AIKEN lanes speak aiken build/check, CLARITY builds via clarinet check with tests riding the npm simnet harness, TACT stays npm-carried with ROSETTA able to dial npx tact; the one precedence inversion (CLARITY outranks NODE — the harness package.json must not shadow the contract toolchain) is mutation-proven; IDE lanes + CI export (verified setup-aiken@v1, honest clarinet NOTE) + append-only knob law held). **The About screen wears the logo** (v1.160.0, branding only: Help ▸ About now shows a generated 500×180 logo lockup — the app-icon mark + wordmark — via branded about.png, which Splash.loadContent(true) prefers over the splash it used to reuse; BrandingArtGenerator emits it, BrandingResourcesTest gates existence/PNG-signature/banner-shape mutation-proven, live-verified in the assembled app). **The tour renders on GitHub** (v1.159.0, docs only: docs/tour.md — the visual tour in GFM so the repo browser renders it natively, twelve verified relative-path screenshots, live transcript as a fenced block; tour.html stays as the phosphor-styled browser version; README + user guide lead with the .md). **A Beginner’s Guide to Smart Contracts** (v1.158.0, docs only: docs/smart-contracts-beginners-guide.md — five contract shapes with the kit’s chains as evidence, refusal idioms in real grep-verified kit code, a ten-minute first contract on three paths (Clarity/Aiken/Foundry), honest mainnet-care closing; linked from README + user guide + the worked escrow example). **The visual tour joins the docs** (v1.157.0, docs only: docs/tour.html — the demo page in the repo, NMOX Phosphor design, relative-path screenshots, 16 KB standalone; linked from README + user guide). **The README leads with the tour** (v1.156.0, docs only: counts strip + hero + a four-frame tour with real entry points (Ask ORACLE / ⌥⌘6 / ⌥⌘5 / ⌥⌘7) + the house laws in the opening paragraph — the demo page’s first impression, translated to GitHub markdown). **The contract languages are editor citizens** (v1.155.0: the kit scaffolded .clar/.ak/.tact files the editor rendered as plain text — now Aiken/Tact/Clarity have vendored sha256-pinned grammars (FunC’s stays out: GPL+archived), CSL languages with the Lexers.find guard, comments (// // ;;), typing, spellcheck, keywords, and brace outlines for Aiken/Tact with Navigator registrations (Clarity’s Lisp shape skipped with reason); editor 442 green; 77 grammars). **The night-arc review: one find, fixed** (v1.154.0, lenses over v1.150–v1.153: the npm-harness chains (Clarity/Tact) shipped no .gitignore — git add . would stage node_modules; both now write one, law test-pinned; parity forms/tool hints/casing/space schemas verified CLEAN; plan.md lineage current through 11 chains / 88 spaces). **TON joins the Contract Kit** (v1.153.0, the 11th chain, the honest way in: FunC’s grammar is GPL+archived so Tact (MIT) carries TON — an npm-local loop where the refusal is a BOUNCED transaction asserted with success:false; live-proven raw and templated (2/2 each, real @ton/sandbox chain); space #88 with folder-local INSTALL; parity gate mutation-proven; 11 chains, 88 spaces). **Night docs truth** (v1.152.0: README/user-guide/plan.md carry the AI arc + the ten-chain kit family + 87 spaces; plan.md records the night — the editor face live-verified, Cardano closed, TON honestly RED). **Cardano joins the Contract Kit** (v1.151.0, the 10th chain: an Aiken vault validator with BOTH paths tested — the refusal declared `test ... fail`, because on Cardano the refusals ARE the contract; live-proven raw AND templated on aiken 1.1.23 (2/2 each); learning space #87 with aiken check as GO; parity gate learned aiken.toml’s dependency-table pin form, mutation-proven; Doctor probes aiken; TON honestly RED — GPL+archived grammar, Tact/MIT route recorded as premise; 10 chains, 87 spaces). **Ask ORACLE picks its depth** (v1.150.0: Fast (Haiku) / Deep (Sonnet) combo in the ask dialog, remembered as a preference, model fixed per conversation so the transcript never lies about who answered; junk prefs fall back to Haiku; and the night gauntlet recorded — the editor face click-verified LIVE in the shipped app end to end, incl. the first-send code consent and a history-resolving follow-up, zero orphans on exit). **The AI-arc review: three finds** (v1.149.0, two lenses over v1.146–v1.148: caps cut mid-emoji → code-point-safe truncate at all three caps; double-VIEW + multi-lane RP could race record() → per-conversation lock in converse + VIEW fronts the open window; and the review’s own lesson — the first lock test let the mutant SURVIVE because record() self-heals structure, so the rendezvous rewrite pins request-body truth {1,3} vs {1,1} and kills it by name; key-wipe/consent-EDT/escape/publication verified CLEAN). **EXPLAIN holds a conversation** (v1.148.0, the symmetry release: VIEW opens a completed EXPLAIN as a conversation — seeded from the exchange that already happened, zero extra calls, PROMPT PARITY law mutation-proven (forFailure’s opening turn IS assemblePrompt byte-for-byte); engine unified to ONE gated path (answer = sugar over converse), consent seam generalized so code and failure flows each wire their own grant; live-proven — “which file, per your diagnosis?” resolved through replayed history to src/date.js:4). **Ask ORACLE holds a conversation** (v1.147.0: the answer dialog is now a conversation window — follow-ups on the same selection ride the full turn history to the Messages API; subject fixed so consent covers the whole conversation, ten-exchange cap surfaced honestly in the transcript, key+consent gates on EVERY send mutation-proven on the converse path, live-proven multi-turn — "the numeric code inside that error" came back 100, resolvable only through replayed history; OracleConversation pure model + 9 tests). **Ask ORACLE reaches the editor** (v1.146.0, the answer to “what is missing”: select code in ANY editor → right-click → Ask ORACLE About Selection… → answer in a dialog; the code flow earns its OWN one-time consent because the failure-flow consent promises source never leaves the machine; sends only the capped selection + file name + language + question; key/consent gates mutation-proven, engine seam-tested (9 tests), live-proven against the real API — the model correctly explained the Clarity counter’s owner-only reset; /Editors/Popup registration byte-verified in the generated layer). **Docs truth for the kit family** (v1.145.0: README beyond-the-EVM section carries all nine chains + the Aptos sniff + the eight Web3 space walks, the mangled Contract-Kit-in-Classic-Kit sentence fixed, user-guide names all nine chains and the sniffed-not-dialed dialect, plan.md currency pass at v1.144.0 with standing counts 53 devices / 74 grammars / 86 spaces / 9 chains). **The kit-arc review: two finds, both fixed** (v1.144.0, two lenses over v1.139–v1.143: space #86’s linux install hint was a do-nothing curl|echo pipeline + an unverified winget id — now the catalog’s honest idiom; ContractKitAction’s javadoc listed seven chains two ships late — now points at the enum so it can’t rot; dialect sniff/casing/never-clobber inheritance/Doctor/parity all verified CLEAN; Clarity grammar recorded as a premise). **Clarity joins the Contract Kit** (v1.143.0, the 9th chain: Clarity on Stacks — Bitcoin-anchored, decidable, interpreted on-chain; kit scaffolds the owner-gated counter with the vitest/simnet harness, kebab-cased per SIP-003, telemetry=false always; live-proven both ways with clarinet 3.22.0 — the proof caught the ASCII-only source rule and the required settings/Devnet.toml (shipped with clarinet’s published public devnet accounts, the anvil-unlocked class); learning space #86; parity gate reads npm pins too, mutation-proven; Doctor probes clarinet; 9 chains, 86 spaces). **Move speaks both dialects** (v1.142.0: the v1.137.0 premise closed — ProjectInspector.moveDialect sniffs AptosFramework in Move.toml (Sui-first default), and every Move lane (IGNITION/FORGE/VERITAS/CRATE + IDE Run/Build/Test) routes through one dialect-aware helper pair, so Aptos projects get aptos move compile/test; live-proven before the code with aptos 9.5.0; Doctor probes aptos; mutation-proven). **A tech-debt sprint** (v1.141.0, debt only: the kit never-clobber write-law extracted from its ClassicKit+ContractKit copies to one KitFiles home; AnchorDevice’s reach-in to StellarDevice.toolOnPath rehomed to the shared CommandDevice base; ContractKit’s dead Chain.tool field wired into an honest PATH hint; and a NEW KitCatalogParityTest that fails the build when the Contract Kit’s crate pins drift from the learning-catalog’s — same starters, two homes, now in lockstep; all mutation-proven). **Bitcoin contracts, the honest way** (v1.140.0: Bitcoin's real contracts are spending conditions, not deployed programs — the Contract Kit gains an 8th chain, Bitcoin (Script/Miniscript), scaffolding a 2-of-2 timelocked-recovery vault as a Miniscript policy compiled to consensus-sane Script; learning space #85; live-proven both ways against real miniscript 13.1 — the proof caught the compiler feature-flag before ship; regtest chain + Clarity/RSK pointers in the notes, keys in Core's own wallet; 85 spaces). **The Contract Kit ships** (v1.139.0, David-directed: File ▸ Contract Kit (Web3)… — seven chains (Foundry/Soroban/Solana/CosmWasm/ink!/Cairo/Move), name-templated live-proven starters + native test + CONTRACT-NOTES.md; all seven scaffolds ran green against their real toolchains post-templating incl. the full Foundry notes flow (forge install → PASS, gas 30444); idempotent + never-clobber both mutation-proven; no keys by construction; position 120 in the kit family). **The Web3 arc closes clean** (v1.138.0, arc review #2 over v1.135–v1.137: no real bug — the V/fpm checklist + build-time gates caught everything; ROSETTA generic, INSPECTOR greys by default, precedence is NODE-outranks, Move CI NOTE valid YAML; the arc v1.130–v1.138 spans STELLAR/ANCHOR devices + Cairo/Move verticals + six live-proven contract spaces + the Multi-Chain Bench). **Move (Sui) is a full language vertical** (v1.137.0, the second smart-contract language: Move.toml → ProjectKind.MOVE, the damirka MIT grammar sha256-pinned on text/x-move, CSL + typing + spellcheck + keywords + brace outline (lockstep held), every lane speaking sui move (build/test, run→build since contracts have no run verb, CHECK greys), move-analyzer LSP, Doctor probes sui, CI emits an honest NOTE; learning space #84 live-proven BEFORE the code — sui 1.76.0 ran the counter test green; detection mutation-proven; Sui-first, Aptos-dialect sniff documented as next; 74 grammars, 84 spaces). **The docs tell the multi-chain story** (v1.136.0: README device roll-call + a beyond-the-EVM section, user-guide studio chapter, plan.md currency for the whole arc incl. the review finding and the Move premise). **The Web3 arc review: one find, fixed** (v1.135.0, the same-day review's third run and third single-bug catch: STELLAR attributed exits to the knob's CURRENT position — turn it mid-run and a test exit announced LOCAL NET UP + URL + READY for a network never started, the v1.93.0 serving-truth class; the launched verb is now captured at launch (BUILD included) and onFinished consults only that, knob-lies regression test + mutation-proven; ANCHOR/preset/Cairo/spaces verified CLEAN). **Cairo is a full language vertical** (v1.134.0, the Web3 arc's own-language tranche: Scarb.toml → ProjectKind.CAIRO, the official Apache-2.0 grammar sha256-pinned on text/x-cairo, CSL + typing + spellcheck + keywords + brace outline (lockstep gate held), every AUTO lane + IDE lane speaking scarb (execute/build/test, deps-at-build, CHECK greys), scarb serves the LSP, Doctor probes it, CI export emits setup-scarb; learning space #83 live-proven BEFORE the code — scarb 2.20.0 ran fib with a gas estimate; detection mutation-proven; 73 grammars, 83 spaces). **The Web3 arc has its preset** (v1.133.0: Multi-Chain Bench — STELLAR + ANCHOR + ANVIL on one MONITOR, MAESTRO trigs firing the Soroban/Solana no-network test lanes; preset gate green). **The Web3 arc's third tranche: CosmWasm + ink!** (v1.132.0: learning spaces #81/#82 — a CosmWasm contract tested against mock_dependencies (cosmwasm-std 3) and the canonical ink! flipper (ink 5, off-chain #[ink::test]), both live-proven green on real crates first try; chain work routed to SOLDER, keys in each CLI's own keyring; Cairo/Move recorded as own-language premises for future verticals; 82 spaces). **Solana joins the rack** (v1.131.0, Web3 tranche 2: ANCHOR the Solana console, 53rd device — START boots solana-test-validator, a REAL long-runner whose JSON RPC banner (pinned live against Agave 4.1.2) drives URL/READY and a TRUTHFUL serving gate that dies with the process; ACTION dials anchor build/test; anchor-lang currency vs crates.io caught Anchor at 1.x; keys stay in the solana CLI's own config — the ANVIL boundary; learning space #80 live-proven, pure-logic program test green on real solana-program 4; Doctor +solana +anchor; rack 1164). **Stellar smart contracts are rack citizens** (v1.130.0, the Web3-expansion arc opens: STELLAR the Soroban console, 52nd device — BUILD = stellar contract build, ACTION dials cargo test (SDK testutils, no network) + the quickstart local net (Docker, RPC URL out; deliberately NO serving gate — the container outlives the start process, v1.93.0 law); soroban-sdk currency vs crates.io; detection via hasSorobanSdk (root Cargo.toml or the contract-init workspace layout — no new ProjectKind, a Soroban project IS a Cargo project); no-private-keys by construction (the CLI owns its identities, the ANVIL boundary); learning space #79 live-proven — cargo test green on real soroban-sdk 27, stellar contract build (CLI 27.0.0) → real WASM; the live proof caught the sdk-23 pin rot + the wasm32v1-none setup step before ship; Doctor probes stellar; rack 1154). **Housekeeping** (v1.129.0: Dependabot maven-group merged through the gate — org.json 20260719 via the one root property, gate held; the CHANGELOG's Keep-a-Changelog link block regenerated after dead-ending at v1.2.1, all 169 versions linked; the frozen Device SPI re-proven — examples/uptime-device builds clean against today's core, 73 releases on). **Two days of new tests are locked in** (v1.128.0: all ten JaCoCo floors ratcheted to measured-minus-margin — core .78, editor .57, tools .59, project .73, ui .13, rack .69, apiclient .81, dbstudio .85, web3 .87, infra .73; no floor lowered, no exclusion changed; a test-shedding regression now fails the build). **plan.md tells the truth again** (v1.127.0: the read-this-first plan was 31 releases stale and self-contradictory — gap #5 still called FileTreePanel a raw JTree ten days after ledger 36 closed; currency pass at v1.126.0 adds the security-and-robustness arc + empty-ledger state + the two new failure patterns, and strikes the stale gap with the lesson attached). **The day review found one real bug and the rest CLEAN** (v1.126.0, two lenses over v1.119–v1.125: the infra op lock was a boolean the node-popup Destroy-in-cloud could overlap — a second queued op saw the first one's cleanup lift the canvas lock and re-enable every op button mid-destroy; opsInFlight is now a DEPTH, unlock + deferred re-aims only at zero, gate-pinned + mutation-proven; DB reload seam/TLS dialog/DapProxy reap/Prettier drain/profile reaper/HttpBodies probe semantics/forge dispose contract all verified CLEAN). **Every tutorial is illustrated** (v1.125.0, forge v2: DocsShots gains a dialog-shot phase — invoke the action like a menu click, wait out the modal's secondary event pump, paint the dialog's root pane at 2x (not the window: the native title bar isn't Swing-painted), dispose so the blocked action returns CLOSED with nothing created; New Learning Space picker + Standards Kit wizard shots land 13/13; mapping test covers both maps, script gates the new files). **One capped HTTP-body read for the whole product** (v1.124.0, ledger 56: core.http.HttpBodies read/readUtf8 → Capped(text, byteLength, truncated) — cap + one probe byte, never the tail, counting-stream proven + cap mutation fatal; all seven former ofString sites route through it while truncation POLICY stays at each site — API Studio flags, JSON-RPC/CouchDB refuse, display-only consoles shrug; per-module gates re-pinned + a cross-module core gate fails the build on re-inline; the ledger now holds NO actionable open items — 51/45 deferred with standing reasons). **The editor debug/format remainder is closed** (v1.123.0, ledger 55 — all six items: the DapProxy loopback socket pair is reaped on the client pump's clean EOF (one FD pair leaked per debug session; the v1.37.0 half-close law intact), Prettier's timeout kills the process TREE via killTreeAndWait with a daemon drain that reads a capped 8 MB prefix then discards to EOF — and output past the cap is REFUSED outright, because truncated text written into the document would destroy the file's tail; a malformed startDebugging is parsed BEFORE the success ack (honest failure, no child dialed), the free-port probe binds loopback, the completion identifier harvest lexes a 200k-char caret window instead of the whole file per keystroke, and force-quit Chrome profile dirs ride a shutdown-hook live-set; 3 mutation proofs, editor 442; ledger 56 (HTTP-read helper unification) is now the ONE actionable open item — 51/45 stay deferred with standing reasons). **CouchDB speaks TLS** (v1.122.0, ledger 54's last item: ConnectionSpec gains a secure flag — delegating 8-arg ctor keeps every call site, absent key in an old .nmoxdb.json loads false, no migration; the dialog offers Use TLS (https) for CouchDB only, CouchBackend.baseUrl picks the scheme; 54 FULLY closed). **The infra canvas locks during live cloud ops** (v1.121.0, ledger 53b — the full-block design: runExclusive arms opInFlight + canvas.setLocked + disables Deploy/Sync/Destroy, a red banner names the lock, deleteSelection/wire-connect/importData all refuse while locked, re-aims defer until the op finishes; EDT-confined flag, source-gated; 53 FULLY closed). **Infra saves bind to their file + model mutations cross to the EDT + only a REAL 404 severs deploy linkage** (v1.120.0, ledger 53 c/d/e: boundDesignFile force-saved before a re-aim load, DigitalOceanClient.onModel invokeAndWaits every cloud-worker model write, deletedInCloud demands the HTTP 404: prefix — impostor 404s in droplet names/retry-after no longer clear doIds; Ledger53RemainderTest). **DB Studio reloads off the EDT** (v1.119.0, ledger 54 M5: reloadWorkspace + the .env connection offer read disk on the RP, newest-wins reloadSeq, EDT apply — the web3 v1.100.0 idiom; ReloadOffEdtGateTest mutation-proven). **Workbench detection rides 4 lanes** (v1.118.0, ledger 61: one hung mount can't wedge Project Explorer's whole detection sweep). **DB Studio connections refuse local-infile + zero the password on close** (v1.117.0, ledger 54 L3+L5: MySQL/MariaDB connects set allowLoadLocalInfile=false so a hostile server can't LOAD DATA LOCAL INFILE a client file; DbClient.close() Arrays.fills its password clone — close is disposal, the backend is discarded, no reopen re-reads it). **A giant LOB can't OOM the DB Studio grid** (v1.116.0, ledger 54 M4+L4: JdbcCore.cell caps each cell at 64k chars — CLOB/NCLOB read only a getSubString prefix, BLOB/binary render [N bytes] never stringified, oversize text marked honestly; PeekQueries builds the Mongo find name via JSONObject.quote so a name with quotes/backslashes stays valid JSON). **Ledger 62 closed same-day** (v1.115.0: wizard Finish scaffolds via AsynchronousInstantiatingIterator — off the EDT with wizard progress; getDisplayName caches by package.json mtime — one stat per paint, malformed JSON falls back to the dir name; runCommand(File,String) dispatches its lockfile probes on the RP). **Menu enablement never walks the disk on the EDT + Explorer scripts can't pin the NPM lane** (v1.114.0, the first full tools review: isActionEnabled resolved the project kind via uncached detectKind — dozens of listFiles per menu paint — now a 3s-TTL KindCache, one scan per window; NpmService.runCommand drained stdout to EOF before waitFor so a double-clicked `npm run dev` pinned the throughput-3 RP forever — runs now route through CommandExecutor with the trust gate still ahead of the spawn; the visible NPM Explorer coalesces re-aim storms with a 300ms trailing timer, the v1.33.2 law applied to the showing case; review verified all five prior fix families HOLD; 3 LOWs → ledger 62). **The core LOW ledger is empty** (v1.113.0, ledger 57/58/59 closed: atomic rewrites re-apply an existing target's POSIX perms after the move instead of silently narrowing to the temp's 0600; Versions.compare reads segments by leading digits so "1.24.0-rc1" compares instead of throwing, huge runs clamp; GitFacts.readFirstLine reads a 4KB prefix — a crafted multi-MB .git file costs 4KB, and a cap-filling no-newline line degrades to null rather than showing a truncated branch name). **Zero unbounded reads remain** (v1.112.0, ledger 60 closed: the pump's readLine() buffered one logical line without limit — a no-newline flood grew a String until OOM; readLineBounded keeps exact terminator parity but truncates past 200k chars with an honest marker and drains the tail so the pipe keeps moving; with HTTP capped in v1.104.0 and process capture capped in v1.106.0, every read path in the product is now ceilinged). **The recent-files trail never touches the paint thread** (v1.111.0, the first dedicated project-module review: RecentFiles.record ran a stat + pref rewrite + synchronous flush on the EDT on EVERY tab switch — all now ride a dedicated single-thread nmox-recent-files RP; Workbench refresh rendered via list()'s up-to-20 stats on the EDT (one hung mount = frozen refresh) — now renders from listRaw(), a pure pref parse, while pruneAsync sweeps vanished files off-EDT and re-requests one coalesced refresh only when something dropped, converges-not-storms test-pinned; the review verified v1.33.x detection/coalescer/no-repost laws + listener symmetry all HOLD; its LOW → ledger 61). **The lexer never recurses into itself** (v1.110.0, a live-caught crash: every CSL language config resolved its lexer via `Language.find(own mime)`; when consulted before the TextMate provider claimed the mime, the find fell back into the CSL kit and re-entered `getLexerLanguage` → StackOverflowError — which once struck inside `Exceptions$OwnLevel`'s static initializer and turned every later benign notification into a red error for the whole session; new `Lexers.find` ThreadLocal cycle guard returns null on re-entry instead, all 50 configs ride it, bare `Language.find` in a config fails the build, overflow mutation-proven). **Screenshots are a product capability** (v1.109.0: DocsShots — boot with `-J-Dnmox.shots.dir` and the app cycles each suite tab, paints the main window to 2x PNGs by pure Swing painting (no OS screen-recording permission), and exits; scripts/docs-shots.sh = rebuild-first + throwaway userdir/cachedir + update-checks suppressed; forge owns docs/images/tabs, curated staged shots in docs/images untouched; 11 of 13 tutorials illustrated; learning-space tutorials now install via the REPL's INSTALL button / SOLDER in-app, never a terminal handoff). **Rack-panel mouse handlers are owner-scoped + Load/Export don't block the EDT** (v1.108.0, the first dedicated rack UI-layer review: RackPanel installed its shared-device mouse handlers with a bare `instanceof DeviceMouse` guard — but that inner class is shared across panels, so a second RackPanel over the same rack skipped its own wiring + leaked the old panel; guard now identity-aware `dm.owner() == this` + removeNotify detaches; and Load Patch + Export CI did blocking disk I/O on the EDT while the adjacent Save used SAVE_RP — both now ride SAVE_RP, Load via new RackIO.readDocument read+parse-off-EDT then fromJson on-EDT; CablePatchGesture/RackTopComponent/palette/all controls verified CLEAN; source-gated). **The flight recorder writes off the hot path** (v1.107.0, the first dedicated rack-engine review: FlightRecorder.record() did its per-event journal append + rotate under `synchronized(this)` — the monitor every device's output-pump thread AND every EDT reader funnels through — so one slow/full disk could stall every pump at once; append+rotate now ride a dedicated single-thread FIFO JOURNAL_RP off the monitor, awaitJournalIdle() barrier + source gate; and RackIO.load parsed JSON BEFORE fromJson cleared devices, so a corrupt .nmoxrack.json left the previous project's devices mounted on a switch and got clobbered by the next save — now .bak's the file + resets to known-empty; the review verified CommandExecutor has NO OOM accumulator and the rest of the engine core CLEAN, its one LOW → ledger 60). **The process-probe is bounded in time AND memory** (v1.106.0, the first ui review + the core-review HIGH it leaned on: EnvironmentDoctor's per-probe timeout was illusory — it drained output to EOF via `transferTo(nullWriter())` BEFORE `waitFor(4s)`, so a tool holding its pipe open hung the whole Doctor sweep + leaked the reader thread; now delegates to `ProcessSupport.runBounded`, whose OWN output accumulator was uncapped (HIGH: a runaway/hostile child flooding stdout grows the heap unbounded → OOM kills the IDE — the shared primitive every module's spawns route through) — each capture now held at a 4 M-char/~8 MB ceiling while the drains keep reading to EOF (no pipe deadlock), `BoundedResult` gains a `truncated` flag, 20 MB-flood mutation-proven; plus "Discard experiment" (irreversible tree delete) defaulted Enter/Space to YES → full `NotifyDescriptor` ctor with NO_OPTION initialValue (v1.98.0 idiom), source-gated; the core review verified HttpClientFactory/JsonUtil/SelfWriteTracker/ToolLocator/AtomicFiles/TerminalPhosphor/the frozen Device SPI CLEAN, its 3 LOW findings → ledger 57–59). **Device background work is drainable in tests** (v1.105.0: the DYNAMO RUNNER-knob flake that blocked two releases — a knob-change listener schedules an async reload via RackDevice.offEdt on the single-threaded DEVICE_BG RP, which the test settle never drained (it flushed EDT + router only); new RackDevice.awaitDeviceBgIdle() FIFO barrier + settle draining device→EDT→router; fixes the whole offEdt-race class, no production change, rack 1154 green). **Every HTTP response read is bounded** (v1.104.0, the ofString sweep: a repo-wide hunt for the unbounded-buffering bug fixed in apiclient/web3/dbstudio found the last four `BodyHandlers.ofString()` sites — rack HttpDevice (the HTTP console, user-pointed URL, 1 MB), rack OracleClient (Anthropic Messages, 8 MB), infra DigitalOceanClient (cloud API behind a secret token, 8 MB), ui UpdateCheck (boot-time GitHub check, 2 MB) — each now streams through a capped `ofInputStream`+`readNBytes` reader; a hostile/misconfigured/redirected endpoint can no longer OOM the IDE; source-gated per module; ZERO `ofString` remains in the product's main sources). **The IDE won't run a cloned repo's code without asking** (v1.103.0, the tools half of the editor RCE class: WebProjectActionProvider Run/Build/Test/Clean (F6/F11) handed a project-controlled command — package.json scripts, make/cargo(build.rs)/gradle, npx-resolved node_modules/.bin — straight to CommandExecutor.run ungated, and NpmService.runCommand (NPM Explorer double-click / install / runScript) spawned npm scripts ungated; both now call WorkspaceTrust.requestTrust before the spawn (prompt-once, the debug-action idiom), the fixed-tool paths (npm ls -g/--version) don't route through the gate; NpmService output capped against OOM; the root cause pinned — CommandExecutor.run/ProcessSupport.builder are deliberately un-gated primitives, trust is the caller's job; mutation-proven; a missing-@Messages-key hand Bundle.properties fixed in passing; tools 78). **The editor won't run a stranger's code** (v1.102.0, the first dedicated editor review — the two most serious defects of the module-review arc, both RCE: LSP servers (`LanguageServers.launchNpm`) and Prettier-on-save ran a cloned repo's committed `node_modules/.bin/<tool>` with NO Workspace Trust gate, so opening a `.ts`/`.js` file or hitting Ctrl+S executed attacker code — now the project-local binary is used only when the workspace is trusted via the SILENT `WorkspaceTrust.isTrusted` check, else the user's own global tool on PATH; plus the DAP frame read is capped at 64 MB so a debuggee's `console.log('x'.repeat(2e9))` can't OOM the IDE, and a malformed Content-Length is a protocol error not a crash; mutation-proven, editor 434; the review verified the debug-spawn trust gate, process-tree kills, DAP half-close teardown, and the JS lexer's progress-on-malformed-input CLEAN; ledger 55 records the proxy-socket-leak + Prettier-kill-tree remainder). **DB Studio quotes, reads, and confirms safely** (v1.101.0, the first dedicated dbstudio review — seven fixes: UpdateBuilder escapes the backslash on MySQL/MariaDB so a value ending in `\` can't break out of its literal and swallow the WHERE, PostgreSQL/SQLite keep quote-doubling only; CouchBackend streams through an 8 MB capped reader so a huge `_find` can't OOM the IDE; the Apply-edits and Remove-connection dialogs default to the safe button via the v1.98.0 idiom so a reflexive Enter can't run UPDATEs or delete a keychain password; EXPLAIN refuses multi-statement text so a trailing DELETE can't execute under a read-only button; Apply treats a 0-row UPDATE as a failure instead of a silent lost edit; CSV export neutralizes formula injection; mutation-proven, dbstudio 374; the review verified Keyring-only passwords, injection-safe identifier quoting, atomic writes, and the NB-bridge-never-closes-NB's-connection law CLEAN; ledger 54 records the EDT workspace read + LOB cell cap remainder). **Contract Studio is bounded, clamped, and confirmed** (v1.100.0, the first dedicated web3 review — all six findings fixed in one release: JsonRpcClient responses stream through an 8 MB capped reader with redacted oversize refusals — the Watch pane polls arbitrary endpoints every 2s and could OOM the IDE; URI.create moved inside the redaction seam; the new pure WatchCursor clamps the eth_getLogs lane to the same 50-block window as blocks — an outage no longer widens the range unboundedly — with a generation guard so STOP→START can't tear cursors mid-tick; SEND/Deploy confirm with the v1.98.0 safe-default idiom before broadcasting to any non-loopback endpoint while ANVIL devnets stay frictionless; reloadWorkspace reads off the EDT newest-wins; fixture-server + 3 mutation proofs; the no-private-keys boundary and keyring/atomicity laws verified CLEAN). **API Studio sends are bounded, cancellable, and honest** (v1.99.0, ledger 52 closed: response bodies stream through an 8 MB capped reader — a runaway endpoint can't OOM the IDE, truncation flagged on the status line, charset honored; the pretty re-parse moved off the EDT behind size + StackOverflowError guards; sends ride their own interruptible RP with a real Cancel button so hung sends never wedge re-aim/loads/serving refreshes; componentClosed saves only when dirty so a no-op open/close can't round-trip a newer file through the unknown-key-dropping parser; split multi-header CSP grades the union — false MISS and false PASS both killed; fixture-server tests + 3 mutation proofs). **Infra destructive dialogs default to the safe button** (v1.98.0, ledger 53a: Destroy Stack/Resource and Deploy confirmations defaulted their Enter/Space to the destructive option — NotifyDescriptor.Confirmation hard-codes initialValue=OK_OPTION and setValue never moves the default button; both confirms now use the full constructor with NO_OPTION, Deploy uses DialogDescriptor with Cancel as initialValue; a reflexive keypress can no longer delete billed cloud resources; DialogSafetyTest source-gate mutation-proven; from the first dedicated infra review). **API Studio secrets are keychain-only** (v1.97.0, the first dedicated apiclient review's HIGH finding: auth tokens were serialized plaintext into the committable .nmoxapi.json, breaking the Keyring-only law every other studio honors; now each request has a stable id, its token lives in the OS keychain via ApiSecrets — dbstudio.Passwords idiom, in-memory fallback + warn-once — the Auth field is a JPasswordField, WorkspaceIO never writes the token, and a pre-v1.97.0 file's plaintext migrates on load and drops from the file on next save; the basic-auth colon is now checked AFTER {{var}} resolution so a single-var credential is no longer silently dropped; mutation-proven; two module reviews recorded findings as ledger 52 (apiclient response/close-save/grader) + 53 (infra destructive-dialog defaults + live-canvas)). **Resurrection survives an unsaved rack** (v1.96.0: kill -9 with a never-saved patch left a perfect snapshot matching nothing on relaunch; unmatchedAgainst + click-time DeviceCatalog re-creation fix it, live-proven; found by the night journey gauntlet — update center/SPI/DB/API/Contract/Standards all PASS with real tools). **Cables patch by click as well as drag** (v1.95.0: click a jack to arm — the preview follows the cursor across scrolls — click a compatible jack to connect, empty space/Escape cancels, same-side click re-arms; one pure CablePatchGesture state machine drives both gestures, every transition unit-tested; born from the real rack being wider than a default window). **ORACLE is live-proven** (2026-07-19: both consult paths ran against the real Anthropic API in the shipped app — button consult with the one-time consent dialog, and the hands-free VERITAS FAIL → EXPLAIN cable auto-consult delivering a correct diagnosis with zero faceplate interaction). **Block Studio jumps between components** (v1.94.0: double-click/F3 on an Element naming a sibling's tag switches there as a patch boundary; own/unknown tags fall through — the v5 idea backlog is empty). **Rear jacks never collide** (v1.93.1: pitch compresses uniformly for jack-heavy devices, relayout-on-add, catalog-wide ≥44px clear-air gate proven failing-first). **The serving gate never lies** (v1.93.0: launch()/launchWithEnv() return launched-for-real and all 18 SERVING/RUNNING/LIVE emitters across 14 devices are guarded — declining Workspace Trust can no longer leave a phantom high gate; trustCheck seam + ServingTruthTest failing-first). **The modern lightweights are covered** (v1.92.0: Solid/Preact/Qwik/Lit/Alpine/htmx learning spaces — 78 total, all six live-proven; Vite+Solid template; Alpine 3.14.9 + htmx 2.0.4 vendored sha256-pinned into an 8-library Classic Kit with detection + completion; ember/remix manifests — 53 recognized; v1.92.1's review added full-file vendor tripwires). **ORACLE is composable** (EXPLAIN trigger in-jack + OUT data out-jack: VERITAS FAIL → EXPLAIN auto-explains hands-free; a cable never prompts — no consent, no call, honest LCD — and consults rate-limit at 30s; OUT feeds MONITOR/PHOSPHOR). **Every declared jack is live** (ledger 50 closed: the five consoles’ STOP/ENABLE/SERVE in-jacks now route — serve/stop/enableGate per the NEXUS shape, SPECTER’s gate ties the E2E run to the dev server; ConsoleJackContractTest fails the build by name on any future dead jack). **Fifth-review hardened** (two lenses over v1.83–v1.88: SPECTER CI export always-headless via a ciCommand seam, RECORD skips its own report serving, engine configs read beside the NODE manifest, readyFired resets per finish; Block Studio preview library reads an EDT-built volatile snapshot — never live docs on the HTTP thread — leading-space TEXT refused at validate, stale ⌘I cleared, popup-outlives-re-aim guarded; ledger 50 records the console family's inert STOP/ENABLE jacks). **Block Studio's dialect is total** (TEXT with </& entity-escapes through generate and unescapes through parse — the last one-way import limitation closed, text pieces now honestly literal in the browser; Save All writes the whole workspace never-clobber with per-outcome counts). **The E2E loop closes** (E2E Loop preset — VELOCITY READY → SPECTER RUN, SCOPE follows app-then-report; a Playwright learning space whose config makes ENGINE=auto resolve; the two v1.86.0 menu-position collisions fixed and boot-verified). **E2E suites are rack citizens** (SPECTER, the 51st device: ENGINE=auto resolves playwright/cypress from config-then-deps, RUN + HEADED toggle, REPORT serves the Playwright HTML report into the ServingRegistry, RECORD aims playwright codegen at the live dev server, BROWSERS installs runtimes, registry version currency, CI-step capable; cypress-missing verbs grey with reasons; both contract gates proven failing-first). **Block Studio composes components from components** (the preview harness imports every other valid component's module from /lib/<tag>.js — read live per request — so an Element piece naming a sibling's tag renders composed; the active tag is excluded from the imports because a double customElements.define throws, mutation-proven; broken components sit the pass out, unknown /lib tags 404). **Block Studio holds a whole component library** (one .nmoxblocks.json holds any number of components — BlockWorkspace pure core, v1 files wrap verbatim as one component and the next save writes v2, wrap branch mutation-proven; toolbar switcher + unique-tag "+"/confirmed "−"; a switch is a patch boundary — force-save then fresh undo, mutation-proven; Open Component… replaces same-tag in place or joins as new; preview follows the active component; the studio finally entered the user guide + README). **The functional web is first-class** (Elm — reactor/make/elm-test lanes + elm-repl space; ReScript — rescript.json/bsconfig.json build lanes; PureScript — spago run/build/test/install; NODE outranks them beside a package.json by design; 72 grammars, 72 spaces). **Indie-stack complete** (Julia finished — Project.toml kind + Pkg lanes; Nim, D, Racket full verticals: glob/manifest detection, pinned grammars, LSP entries, all AUTO lanes, Doctor probes, nim-secret + racket REPL learning spaces; 72 spaces, 72 grammars). **V-fluent** (V, the vlang systems language, is a full vertical: `v.mod` → `ProjectKind.VLANG`, pinned vscode-vlang grammar on `text/x-vlang` for `.v`/`.vsh`/`.vv`, CSL VLanguage + keyword completion + brace-family outline, `v-analyzer` LSP, IGNITION `v run .` / FORGE `v .` / VERITAS `v test .` / CRATE `v install` (CHECK greys — no vpm outdated) / ROSETTA lists it / the IDE-native Run/Build/Test speak `v`, Doctor probes `v`, and a `v repl` learning space — every AUTO lane + the IDE lanes test-proven). **The framework-console family is complete** (KINETIC the SvelteKit console — vite verbs + svelte-check, @sveltejs/kit currency — and NIMBUS the Nuxt console — nuxi dev/build/preview/typecheck, nuxt currency — join HALO/NEXUS/VELOCITY/COSMOS/PHOENIX/ARTISAN; 50 devices). **Astro-aware** (COSMOS, the 50th device, is the Astro console — dev/build/preview/check with the serving URL out and astro version currency, mirroring VELOCITY/NEXUS). **Vite-first** (VELOCITY, the 50th device, is the Vite console — DEV/BUILD/PREVIEW with the serving URL out and vite version currency, mirroring NEXUS/HALO; IGNITION still serves Vite generically). **Project tree is platform-native** (Project Studio's file tree is a BeanTreeView over the real DataFolder node — real file-type icons, git branch annotation, the full Open/Cut/Copy/Delete/Rename/Tools/Properties node menu, lazy off-EDT children; heavy dirs stay childless, the root resolve rides a background lane with newer-aim-wins, ledger 36 closed live). **Monorepo-fluent** (WAYPOINT dials a workspace package and every Node lane runs there). **Biome-native** (biome.json flips PURITY/GLOSS auto lanes to `biome lint`/`biome format` with honest fix spelling, diagnostics, and LCD counts). **Speaks your package manager** (every Node AUTO lane — CRATE, NPM-9000, the IDE's Run/Build/Test/Clean, NPM Explorer — resolves npm/yarn/pnpm from the project's own contract: the corepack packageManager pin first, then the lockfile; pnpm-lock.yaml/yarn.lock joined the ManifestPulse set; Doctor probes pnpm+yarn; mutation-proven at every consumer). **Explains your errors, the rack way** (ORACLE, the 45th device: EXPLAIN reads the last failed run off the FlightRecorder — command, exit code, ≤5 error lines, device, project name — and asks the Anthropic Messages API what went wrong; QUERY-blue, no project mutation; verdict on a multi-line LCD, full text in a popup, HAIKU/SONNET knob; the API key Keyring-only or ANTHROPIC_API_KEY/CLAUDE_API_KEY; its OWN one-time consent for the outward data flow — WorkspaceTrust is an inward execution guard and does not cover it — spelling out exactly what's sent and what isn't; zero boot cost, no network without the button press, the key and consent gates both mutation-proven; honest LCD degradation for no-key/no-consent/nothing/offline/refusal). **Updates itself** (ledger 21 closed: Tools ▸ Plugins is live — a "NMOX Studio Updates" center registered in ui/layer.xml points at `releases/latest/download/updates.xml`, GitHub's stable redirect to the newest release; the linux release lane publishes the catalog + all 11 module NBMs as additional assets, every NBM pinned to its own tag by ABSOLUTE URL because the platform resolves relative ones against the pre-redirect catalog URL — a cached old catalog must never fetch newer bytes and fail its own SHA-512 digests; the never-shipped sample module is pruned + gated by scripts/build-update-site.sh; no fetch at boot — the platform checks post-UI on a default-weekly schedule, evidence decompiled; registration/workflow/script mutation-gated by UpdateCenterTest; NBM signing now a secret-gated release pipeline — a sign-nbms root-pom profile + base64-keystore workflow secret, jarsigner-verified, off by default, docs/engineering/nbm-signing.md). **Undo respects patch boundaries** (loading a preset or patch clears the rack's undo history at the single choke point every load routes through — ⌘Z can no longer peel a just-loaded patch apart or resurrect the previous one; a removed device drops its cables' trigger-cooldown bookkeeping; TAIL/TEMPO re-sync their faceplate on undo re-attach — each of the three sub-bugs mutation-proven; org.json's version is one root property across all eight module copies, byte-verified, gate-tested — ledger 19 and 23 closed). **Rack findings in the Action Items window** (ledger 32 closed: PURITY/TYPEGUARD diagnostics land in the platform Task List via a lazily-instantiated PushTaskScanner — severity on the window's own error/warning groups, tool name in the text, click-to-navigate, visible even for unopened files; replace-per-run semantics in a pure core, clear-on-rerun mutation-proven; recon found the squiggle half already rode HintsController — RackSquiggler untouched, now source-gated; DiagnosticsBus stays as the transport). **Modules say which release they came from** (every module manifest carries the product version as its OpenIDE spec version, driven by one root <spec.version> property the release workflow stamps from the tag alongside branding; inter-module dependencies are real ranges — core > 1.47.0 — so a mismatched module refuses to load instead of LinkageError at call time; SpecVersionGateTest byte-gates all 11 jars + the placeholder discipline, mutation-proven). **Optional means a lookup, not a caught classloader failure** (core.spi ProjectAim/LiveServings facades + rack @ServiceProvider adapters replace 31 catch(LinkageError) sites; apiclient/web3/infra dropped their rack dependency entirely; rack's exports are OpenIDE-Module-Friends-locked to the five first-party consumers — byte-verified; SoftDependencyGateTest pins the idiom out, mutation-proven). **A platform citizen** (aiming opens the project in OpenProjects + publishes a real selection from the aim-owning windows — the Team menu is the full enabled git suite with just a project aimed, and the git chip's Show Changes/Diff/Annotate are back; ledger 29 worked as its own release, guards mutation-proven, boot laws intact; since v1.48.0 Project Studio's tree publishes the selected FILE's node — per-file git verbs see a real file, cleared selection falls back to the aim, distinct-target storms coalesce — and NPM Explorer publishes the found project's node, withdrawing to a null opinion when there's none so its hand-read registry fallback keeps working, now self-echo-proof). **Never freezes, never lies** (Stop All/switch off the EDT with the orphan guarantee intact — 1.5s/device freeze measured then killed; studio saves on single-lane writers with stamp-atomic self-write discrimination; Window menu shows real accelerators, drift-gated; SBOM covers the vendored adapter). **Debugs the browser too** (Debug in Chrome on HTML/JS/TS: the vendored js-debug launches a throwaway-profile Chrome at the project's live ServingRegistry URL — or file:// for serve-less HTML — and page breakpoints hit in the IDE; recon-first, real-Chrome integration test, mutation-proven). **Tested where it ships** (windows-latest runs the full verify as a blocking CI gate — the lane's first green found two real product bugs: language servers never detected as installed on Windows, and a cross-OS DapProxy disconnect race; one evidenced POSIX-only disable). **Usable without a mouse or a screen** (the widget library speaks Swing accessibility: knobs are SLIDERs with keyboard arrows + focus rings, buttons/toggles answer Space/Enter, LEDs/LCDs/VU meters report state; every control on all 51 devices exposes an accessible name — CI-gated by DeviceContractTest, 59 fixed to get there). **Knows its branch** (a git chip on the status line — HEAD read from disk, dirty count bounded + boot-law-gated; one click to the platform History browser; Team menu carries the rest). **Platform-native inside** (a five-lens senior-RCP idiom review: file CRUD rides DataObject so open editors follow deletes/renames, all five workspace writers are atomic — temp sibling + ATOMIC_MOVE via core AtomicFiles — the Rack shutdown-hook leak is a live-set reaper, Docker Panel respects being closed, studio workspace loads moved ctor→componentOpened without breaking the zero-boot-spawns law, API Studio/NpmService on named RequestProcessors, DockerClient a real @ServiceProvider; deep deferrals written as ledger 29–36, deliberate divergences blessed in writing). **Says what it does** (every advertised keyboard shortcut opens the window it names — studios on ⌥⌘6–9, Workbench ⌥⌘0, Open Folder ⌥⌘O; the four that collided with the platform Keymaps profile were found by pressing them). **Starts honest** (window in 1.4–2.7s; zero processes spawned at boot — hidden default-open tabs defer their work to first show, JFR-verified). **Breakpoints everywhere** (JS/TS debug out of the box via vendored js-debug + a DAP session multiplexer; Python/Go unchanged; all three now gate on Workspace Trust). **Senior-review hardened** (a six-lens RCP architecture audit fixed the two oldest surfaces' latent bugs: real process timeouts everywhere via ProcessSupport.runBounded, the EDT mutation half — New Project/Experiments/file-tree delete/session snapshots all off the paint thread, per-open studio listener lifecycles, corrupt workspace files kept as .bak instead of clobbered, cloud tokens in the OS keychain, dead v0.x code deleted — net −3,000 lines), **everything wired together** (the Serving Registry — serve devices announce URLs live; status-line `⇄ serving` chip, ⌘I "Live Servers", VITALS/BEACON auto-target, API Studio {{baseUrl}} offers, Contract Studio auto-connects to ANVIL; manifest edits pulse exactly the devices that read them — NPM-9000/DYNAMO/CRATE/HALO/ARTISAN re-sync without re-aiming; Contract Studio auto-rescans on new build artifacts; DB Studio offers connections for running Docker database containers; all four studio workspace files reload on external edits with never-clobber dirty guards; every listener bounded + equality-guarded + storm-tested), polyglot editing (72 TextMate grammars incl. Gleam, CoffeeScript and the config layer with Apache configs (.htaccess/httpd.conf/.vhost), LSP providers, toolchains for Bun/Deno/Rust/Go/BEAM+ (Elixir/Erlang/Gleam)/PHP/.NET/Dart/Zig/Scala/Haskell/OCaml/Crystal with per-lane monorepo dirs; Navigator outline for 58 mimes; classic-library-aware completion — jQuery/MooTools/Prototype/Backbone/Underscore/Knockout APIs appear when the project's deps or script tags carry them), **the classic web as first-class** (script-tag sites with no manifest open as STATIC projects and serve via IGNITION; bower.json/Gruntfile/gulpfile/webpack.config are project manifests — 51 recognized; jQuery 1.x/2.x get honest EOL chips; Classic Kit wizard extends any codebase with vendored-or-npm jQuery/MooTools/Prototype/Backbone/Knockout + webpack/grunt/gulp/bower scaffolds, never clobbering), the Reason-style task rack (51 devices incl. SPECTER the E2E console, WAYPOINT the workspace selector, ORACLE the AI error explainer, the REPL, the ARTISAN Laravel console, the ANVIL local EVM chain, the GOVERNOR gas gate, and the DYNAMO Grunt/Gulp task runner — see docs/devices.md, generated + CI-gated; undo/redo ⌘Z on every structural edit; cross-lane QUORUM join, readiness ENABLE gates, per-lane REFLEX GLOB routing; quality gates: VITALS Lighthouse floor with perf/a11y/both/best/seo/all GATE, VERITAS coverage floor + named-failure re-runs, GAUNTLET throughput floor, PRISM bundle budget, BEACON cert/uptime sentinel; SOLDER any-command (exports to CI), TAIL log follow + TEMPO clock (both resurrect), HELM ssh runner; presets, CI export, session resurrection, PREFLIGHT ship gate), safe project switching + .env everywhere + Experiments (~/.nmox/experiments lifecycle), Quick Search (⌘I — reaches projects, rack devices, live servers, API Studio requests, and infra nodes)/status line/keymap surfaces, the Workbench home base, the Docker control panel, the multi-cloud infra designer (DigitalOcean/Hetzner/Cloudflare — sync live resources from all three, drift Refresh, Destroy-stack with cost framing, cloud-init user_data, copy-ssh-from-node, deploy log), **API Studio** (Postman-style tab with per-response security-header grades), **DB Studio** (⌥⌘7 database suite: SQLite/PostgreSQL/MySQL/MariaDB/MongoDB/CouchDB with bundled drivers, Keyring-only passwords, kind-aware SQL/JSON console, per-statement result grids with in-grid row editing — PK-gated, Apply previews the exact UPDATEs, honest read-only reasons — CSV/JSON export, persistent history + saved queries, .env connection offers, EXPLAIN button, ⌘I-reachable; suite tabs Workbench→Rack→DB→Web3→Infra→API→Docker all open by default on first launch), **Contract Studio** (⌥⌘6 Web3 suite: Foundry/Hardhat artifact tree, ABI-driven Interact with decoded returns/reverts, Watch pane streaming blocks + decoded event logs, Oversight pane with gas table + EIP-170 size verdicts + deployment address book in .nmoxweb3.json; NO private keys ever — sends via devnet unlocked accounts, secret RPC URLs Keyring-only; Solidity grammar/outline/LSP; foundry.toml toolchain), **standards enforced for real** (.editorconfig on save; Standards Kit wizard for robots/sitemap/manifest/security.txt/humans), the **PWA Kit wizard** (Java2D icon forge incl. maskable set, readable service worker, offline page, idempotent index.html wiring), **Learning Spaces** (New Learning Space… — 78 built-in tutorials across languages/frameworks/libraries, each generating sample code + a walked tutorial + a rack pre-wired with a real in-rack REPL you type into), one Options category (General/Rack/Format sub-panels, incl. the startup update-check toggle), installers for all three OSes with bundled runtimes, Homebrew cask, and a tag-triggered release workflow with SBOM.

## Working Method (read before coding — human or AI)

The long-form culture lives in CONTRIBUTING.md ("How this house
works"), docs/engineering/plan.md (the living plan + failure
patterns), and docs/engineering/codebase-guide.md (the five RCP ideas
+ house laws with their origin incidents). The operational
distillation:

**The unit shape.** Recon first (decompile/probe the platform, never
folklore) → build with tests in the same change → **walk it in the
ASSEMBLED app** (`application/target/nmoxstudio/bin/nmoxstudio`,
throwaway `--userdir`, `-J-Dplugin.manager.check.updates=false`) →
review the fresh code with hostile lenses within a day or two → docs
truth (CHANGELOG + README + user guide in the same ship). Most real
finds come from the walk and the review, not the first write.

**The proof protocol.** Interesting tests are mutation-proven: break
the code the way the test exists to catch, watch the NAMED test fail,
restore, watch it pass. Only full verdict lines count
(`Tests run: N, Failures: N`) — a missing verdict is a failed run.
Commit work BEFORE any mutation; a bare `git checkout --` restore has
eaten uncommitted fixes repeatedly. Test each OR-branch alone;
equivalent mutants get documented and structurally pinned.

**The standing laws** (each paid for by a shipped bug): refusals
speak — nothing fails silently; secrets are keychain-only; no spawn
of project-controlled code without Workspace Trust BEFORE the spawn
(SpawnSiteTrustLedgerTest classifies every site); every read bounded;
writes atomic + never-clobber (corrupt files → `.bak`); no disk/process
work on the EDT (named RP, newest-wins, EDT apply); a result belongs
to the workspace that produced it (re-aim clears everything armed);
every runtime invariant over a checked-in file gets a parse-time
heal; every control carries an accessible name (contract-tested).

**Gates gate outcomes, not mechanisms.** Derive gate populations from
generated artifacts (the layer, the jar, the census) so unanticipated
cases fail the build. Docs-landed checks read COMMITTED content
(`git show HEAD:…`), grep -F for literals (regex metachars have faked
green five times), and the next version number comes from the
CHANGELOG head — never from memory.

**Shipping.** Every change rides `mvn clean verify` (tests + SpotBugs
+ find-sec-bugs + JaCoCo floors — a new finding is usually a real
bug, fix it, don't exclude it) → PR → three-OS blocking CI →
squash-merge → tag → the release workflow publishes 19 assets incl.
the update-center catalog. `scripts/ship-branch.sh` (in-repo since v2.68.1:
rebase from the FORK SHA CAPTURED AT FORK TIME, the docs-collision
resolver, line-anchored marker checks, the title as a quoted argument,
every exit code checked) opens the PR; `scripts/ship-gate.sh` (in-repo,
parameterized) automates PR-to-tag with the stale-rollup grace,
fetch-never-checkout, and the tree-identity fast path; `scripts/post-ship.sh`
(in-repo since v2.68.2) waits for the assets, fast-forwards main, hops the
cask, deletes the merged branch. `scripts/update-gauntlet.sh <from-tag>`
(in-repo since v2.69.2) is the close proof: a stock portable of an older
release updates itself in-app to the newest and boots clean, first-boot
half included. In chained
scripts, guard mvn's exit code EXPLICITLY — `set -e` has let a red
verify through. Never tag unverified; a published tag is never moved. One working
tree, one chain: never run two ship chains (or a chain plus
interactive git) in one checkout — a chain commits onto whatever
branch HEAD points at by then (measured: a docs chain committed onto
a sibling feature branch and pushed an empty one). Chains assert
`git rev-parse --abbrev-ref HEAD` before every commit and push;
parallel releases get separate worktrees. And a multi-line `grep -F`
pattern is an OR of its lines — every enforced literal is ONE line.

## Build and Run Commands

### Prerequisites
- **Java 25+** (the bundled runtime and CI are JDK 25 LTS since v1.253.0; the compiler still TARGETS 21 bytecode on purpose — see the law at maven.compiler.target)
- **Maven 3.6+**

**IMPORTANT**: Ensure JAVA_HOME points to JDK 21+ or use `--jdkhome` when running:
```bash
# Set Java for build
export JAVA_HOME=/Library/Java/JavaVirtualMachines/jdk-23.jdk/Contents/Home
# or use: export JAVA_HOME=$(/usr/libexec/java_home -v 21)

# Alternative: use --jdkhome when launching IDE
./application/target/nmoxstudio/bin/nmoxstudio --jdkhome /Library/Java/JavaVirtualMachines/jdk-23.jdk/Contents/Home
```

**Java Module Access**: The IDE is configured to open necessary Java modules for NetBeans Platform compatibility with Java 9+. These flags are automatically included in the default configuration.

### Building

```bash
# Clean build without tests (fastest)
mvn clean package -DskipTests

# Full build with tests
mvn clean test package

# Build specific module
mvn clean package -pl editor

# Build specific module with dependencies
mvn clean package -pl editor -am
```

### Running the IDE

```bash
# Using convenience script
./run.sh

# Or manually after build
cd application/target/nmoxstudio/bin
./nmoxstudio

# Run directly with Maven (development mode)
mvn nbm:run-platform
```

### Testing

```bash
# Run all tests
mvn test

# Run tests for specific module
mvn test -pl tools

# Run specific test class
mvn test -pl tools -Dtest=NpmServiceTest

# Skip tests during build
mvn package -DskipTests

# Generate test coverage report
mvn test jacoco:report
```

### Convenience Scripts

The project includes several shell scripts:
- `./build.sh` - Clean build
- `./run.sh` - Build and run the IDE
- `./gui-test.sh` - GUI-specific tests (macOS)
- `./setup-and-run.sh` - Interactive setup menu
- `scripts/boot-smoke-test.sh` - Headless boot of the assembled app (the CI gate)

The comprehensive suite is `mvn verify` — tests plus the SpotBugs,
find-sec-bugs, and JaCoCo gates. (The v0.x-era test-everything.sh /
quick-test.sh scripts checked classes that never shipped and were
removed in v1.22.0.)

## Module Structure

The project is organized as a multi-module Maven build using NetBeans Platform:

```
NMOX-Studio/
├── core/                    # Cross-cutting platform touches (Terminal phosphor theme)
├── editor/                  # File type support, JavaScript lexer, syntax highlighting, completion
├── tools/                   # NPM integration, build tools
├── rack/                    # Reason-style task rack: drag-drop device wiring, control surfaces
├── infra/                   # Node-RED-style multi-cloud infra designer (DO/Hetzner/Cloudflare)
├── apiclient/               # API Studio — Postman-style request/collection/test suite
├── dbstudio/                # DB Studio — SQL + document database suite (5 engines, bundled drivers)
├── web3/                    # Contract Studio — Web3/EVM smart contracts (Keccak/ABI/JSON-RPC cores)
├── project/                 # Project templates, scaffolding, project explorer UI
├── ui/                      # Main windows, actions, welcome screen, startup logic
├── branding/               # Application theming, splash screen, icons
├── application/            # Final packaging and distribution assembly
└── NMOX-Studio-sample/    # Sample module template
```

### Active Modules (in build)

| Module | Purpose | Key Components |
|--------|---------|----------------|
| **core** | Cross-cutting platform touches + shared SPI | `TerminalPhosphor` (phosphor Terminal theme on first run); `spi.device` (the frozen public **Device SPI** third-party rack devices implement); `spi` facades (ProjectAim/LiveServings soft-dependency seams); `ProcessSupport`/`ToolLocator`/`AtomicFiles`/`HttpClientFactory` |
| **editor** | File editing and language support | `JavaScriptLexer` (regex-aware), `JavaScriptDataObject`, `TypeScriptDataObject`, `WebFileSupport` (HTML), 72 TextMate grammars incl. HTML/CSS/SCSS/Less + the config layer, JS/HTML/CSS completion providers, LSP providers, `OutlineModel`/`StructureNavigatorPanel` (Navigator outline for 58 mimes), `ConfigFileResolver` (dotfile MIME), `debug/DapDebugAction` + `debug/dap/` (DapFrames/DapProxy/JsDebugServer — flattens js-debug's child-session protocol for the platform's single-session DAP client; adapter vendored at `src/main/release/jsdebug/`) |
| **tools** | Development tools and integrations | `NpmService`, `WebProjectFactory`, `NpmExplorerTopComponent` |
| **rack** | Reason-style virtual task rack + project lifecycle | `RackTopComponent`, `Rack`/`RackDevice` model, 52 task devices (incl. WAYPOINT workspace selector, ORACLE AI error explainer, ROSETTA mixed-repo selector, QUORUM lane-join barrier, IGNITION polyglot runtime, INSPECTOR debug launcher, ARTISAN Laravel console, the REPL, ANVIL local EVM chain, GOVERNOR gas gate, DYNAMO Grunt/Gulp runner, HARBOR docker, BLACKBOX flight recorder, SONAR port scanner, PREFLIGHT ship check, PHOSPHOR terminal), cross-lane coordination (QUORUM join, `RackDevice.enableGate` readiness gates on long-runners, REFLEX per-lane GLOB routing), patch-cable wiring, `FileWatcher`, `RackIO` persistence, `RackService`, `ProjectStudioTopComponent` (templates, file CRUD, package.json editor, presets) |
| **apiclient** | API Studio (Postman-style) | `ApiClientTopComponent` tab, collections/requests tree, request builder (params/headers/body/auth/tests), response viewer, `{{var}}` environments, `.nmoxapi.json` per-project persistence; `ApiClient`/`TestRunner`/`WorkspaceIO` engine |
| **dbstudio** | DB Studio (database suite, ⌥⌘7) | `DbStudioTopComponent` (connections+schema tree, kind-aware SQL/JSON console, per-statement result grids + history), `DbBackend.create()` dispatch — `DbClient` JDBC (SQLite/PG/MySQL/MariaDB, bundled drivers), `MongoBackend` (Extended-JSON runCommand), `CouchBackend` (pure HTTP Mango); `SqlSplitter`, `DocumentGrid`, `Passwords` (Keyring-only), `.nmoxdb.json` specs, `DbSearchProvider` (⌘I) |
| **web3** | Contract Studio (⌥⌘6) | `Web3StudioTopComponent` (Interact/Watch/Oversight, network manager, DA-6), `Keccak256`/`AbiCodec` (spec-vector-pinned), `JsonRpcClient` (Transport seam, URL redaction), `ArtifactScanner` (Foundry out/ + Hardhat artifacts/), `GasReportParser`, `ContractSizeCheck` (EIP-170), `Web3WorkspaceIO` (.nmoxweb3.json, secret networks never serialize URLs), `RpcSecrets` (Keyring), `Web3SearchProvider` (⌘I) |
| **infra** | Multi-cloud infra designer (DO/Hetzner/Cloudflare) | `InfraDesignerTopComponent`, `NodeKind` catalog, `FlowCanvas`, `DeployPlanner`, per-provider clients, `CloudTokens` (Keyring), cost estimation, `.nmoxinfra.json` persistence |
| **project** | Project management | `ProjectExplorerTopComponent`, `WebProject`, wizards |
| **ui** | Core UI components | `MainWindow` (the Welcome launchpad), `UpdateCheck`, actions, Options panels |
| **branding** | Application identity | Splash screen, icons, custom branding |
| **application** | Final assembly | Distribution package creation |

## Architecture

### NetBeans Platform Concepts

NMOX Studio leverages the NetBeans Rich Client Platform (RCP):

1. **Module System**: Each module is an OSGi-like bundle (NBM) with explicit dependencies
2. **Lookup System**: Service discovery and dependency injection via `Lookup.getDefault().lookup(Service.class)`
3. **TopComponents**: Dockable window panels for UI
4. **DataObjects**: File type handlers registered via MIME types
5. **Nodes**: Tree/explorer view representations
6. **Actions**: Commands registered via annotations

### Key Architecture Patterns

**Service Registration**:
```java
@ServiceProvider(service = SomeService.class)
public class SomeServiceImpl implements SomeService {
    // Implementation
}
```

**Service Lookup**:
```java
SomeService service = Lookup.getDefault().lookup(SomeService.class);
```

**TopComponent (Window)**:
```java
@TopComponent.Registration(mode = "editor", openAtStartup = false)
public class MyPanel extends TopComponent {
    // UI implementation
}
```

**DataObject (File Type)**:
```java
@MIMEResolver.Registration(displayName = "JavaScript", resource = "mime-resolver.xml")
@DataObject.Registration(mimeType = "text/javascript", ...)
public class JavaScriptDataObject extends MultiDataObject {
    // File handling
}
```

### Module Dependencies

- **core** has minimal dependencies (base NetBeans APIs only)
- **editor** depends on core + editor/lexer APIs
- **tools** depends on core + project APIs + external libs (JSON)
- **ui** depends on core + windowing APIs
- **application** depends on all active modules

## Development Workflow

### Adding a New Feature

1. Identify the appropriate module (or create new one)
2. Add dependencies to module's `pom.xml`
3. Implement using NetBeans Platform APIs
4. Register services/components via annotations
5. Add unit tests
6. Update layer.xml if adding UI components or actions
7. Test in IDE with `mvn nbm:run-platform`

### Common Tasks

**Add a new file type**:
- Create DataObject class in `editor/`
- Add MIME resolver XML
- Implement EditorKit or use existing
- Register with `@DataObject.Registration`

**Add a new tool window**:
- Create TopComponent in appropriate module
- Use `@TopComponent.Registration` annotation
- Implement UI with Swing components

**Add a new service**:
- Define interface in module API
- Implement with `@ServiceProvider`
- Consume via Lookup

**Add NPM functionality**:
- Extend `NpmService` in `tools/`
- Add UI in `NpmExplorerTopComponent`

### Testing

The project uses JUnit 5 with AssertJ assertions and Mockito for mocking:

```java
@Test
@DisplayName("Should parse npm error when command not found")
void shouldParseNpmErrorWhenCommandNotFound() {
    // Given
    String errorOutput = "npm: command not found";

    // When
    NPMError result = parser.parseError(errorOutput);

    // Then
    assertThat(result.getUserMessage()).contains("NPM not found");
}
```

Integration tests can use NetBeans Platform test harness in the `test` scope.

## Key Implementation Details

### JavaScript Syntax Highlighting

Location: `editor/src/main/java/org/nmox/studio/editor/javascript/`

The JavaScript lexer (`JavaScriptLexer.java`) provides token-based syntax highlighting using NetBeans Lexer API. Tokens are defined in `JavaScriptTokenId.java` and language hierarchy in `JavaScriptLanguageHierarchy.java`.

**Token categories**: keywords, identifiers, operators, literals, comments, whitespace

**Integration**: Registered via `@LanguageRegistration` and `layer.xml`

### NPM Integration

Location: `tools/src/main/java/org/nmox/studio/tools/npm/`

- **NpmService**: Executes NPM commands, parses package.json
- **WebProject**: A full platform `Project` — its Lookup carries `Sources`, an `ActionProvider` (Run/Build/Test/Clean → `WebProjectCommands` per toolchain, executed via the rack's `CommandExecutor` with a `ProgressHandle`), `RecommendedTemplates`, and a `ProjectOpenedHook` that aims the rack
- **WebProjectFactory**: `@ServiceProvider(ProjectFactory.class)` — recognizes 16 manifests (package.json, Cargo.toml, go.mod, pom.xml, …) as projects
- **NpmExplorerTopComponent**: UI for browsing and running NPM scripts

Projects are recognized by any supported manifest (package.json and 15 others); the IDE-native Run/Build/Test/Clean and the rack are one mechanism.

### Project Templates

Location: `rack/src/main/java/org/nmox/studio/rack/projectstudio/ProjectTemplates.java`

All 14 project templates (React/Vue/Svelte/Solid via Vite, Angular, vanilla, PHP, Classic Web, …) live in Project Studio's wizard — the one New Project surface (File ▸ New Project… and the Welcome tab both open it). The old platform `@TemplateRegistration` wizard (`WebProjectWizardIterator` + resource templates) was unreachable since the v1.11 chrome pass evicted the platform action, and was deleted in v1.246.0.

## Known Issues and Technical Debt

See `docs/engineering/tech-debt.md` for the CURRENT ledger (the
`docs/hack/` copy is v0.x archaeology). Key items:

1. **Built-in templates live in code** (ProjectTemplates.java) - deliberate since v1.293.0: their pins carry gate-enforced version ceilings. User templates ARE data-driven now: ~/.nmox/templates.d drop-ins join the wizard (see docs/project-templates.md)
2. **Startup** - window in 1.4-2.7s with ZERO processes spawned at boot (measured v1.38.0, JFR-verified; the module system scanning ~519 cluster jars is ~90% of it - the deliberate feature-set price)
3. **JS/TS use a custom lexer pipeline** while the other languages ride TextMate+CSL; two code paths to maintain
4. ~~.sass shares the SCSS grammar~~ - CLOSED v2.20.0: indented Sass has its own mime + the canonical indented grammar (86 grammars)

## Version History

- **v0.1**: Basic JavaScript/TypeScript support, NPM integration, project templates
- **v1.0.x**: task rack, polyglot editor (TextMate+CSL), LSP everywhere, infra designer, Workbench, packaging + release pipeline (and the real branded splash)
- **v1.1.0-v1.3.0**: Docker control panel + HARBOR, BLACKBOX/SONAR awareness devices, session resurrection, PREFLIGHT ship gate
- **v1.4.x-v1.10.x**: bundled JRE installers, config-layer grammars, Navigator outline, WebProject SPI, LSP health + installer, self-policing build (SpotBugs/find-sec-bugs/JaCoCo floors), Java 21 + CI matrix + SBOM + Dependabot, CI release gates (boot smoke, test audit, rendering probe)
- **v1.11.0**: daily driver — safe switching guard, .env everywhere, Experiments, Quick Search/status line/keymap, crisp chrome (VCS-museum eviction, honest About, menu-bar name)
- **v1.12.0-v1.13.0**: SOLDER/TAIL/VITALS + overallSuccess verdict hook; testing rigor (named failures, re-run failed, coverage + throughput floors)
- **v1.14.0**: infra truth — drift Refresh + Destroy stack with cost framing
- **v1.15.0-v1.17.0**: Bun + Deno first-class; HELM/BEACON/PRISM; DO deep (cloud-init, ssh-from-node, deploy log) + designer fit/zoom
- **v1.18.0-v1.19.0**: PING REST console + dialog-idiom sweep; API Studio module (collections, {{var}} environments, assertions, .nmoxapi.json)
- **v1.20.x**: standards with gusto — .editorconfig honored on save, Standards Kit wizard (robots/sitemap/manifest/RFC 9116 security.txt/humans), security-header grades on every API Studio response, WCAG as a VITALS gate; dialog-stacking fix
- **v1.21.0**: PWA Kit wizard — Java2D icon forge (maskable set), readable service worker (app-shell/network-first), offline page, idempotent index.html wiring
- **v1.22.0**: the Snow Leopard release — no new features; charset/EDT/leak/race fixes, ProcessSupport+ToolLocator promoted to core, shared HttpClient + JsonUtil, JOptionPane eviction, test backfill + floors, docs truth pass
- **v1.23.0**: the completeness release — Experiments manager (promote/discard UI), Hetzner drift/destroy/cloud-init + any-provider live deploys, VITALS gates all four Lighthouse categories, Ship Gate + Dev Intelligence presets, BEAM outlines, wizard validation, window shortcuts
- **v1.268.0**: renaming a Block Studio component follows its references — the organize sweep's third instance, sharper: F2-rename EXISTED but silently orphaned every sibling Element naming the old tag and let two components collide on one tag (double customElements.define throws in the preview, the v1.85.0 hazard); root-tag edits now ride BlockWorkspace.renameActive — invalid/colliding tags refused with zero side effects, sibling references follow with a count in the status, cross-component rename is a patch boundary per the v1.84.0 switch law; mutation-proven ×2 by name
- **v1.267.0**: docs truth — plan.md carries the persona cadence (v1.262–v1.266) + the live-verified Manage Saved Queries rename in the shipped 1.266.0; headline at v1.266.0 (docs only)
- **v1.266.0**: the DBA persona walk (real SQLite store: quotes/unicode/NULLs/formula name) — saved queries become MANAGEABLE (Manage… → Rename/Delete; rename refuses collisions, delete safe-default; pure WorkspaceEdits, mutation-proven) and the console placeholder becomes a HINT (cleared on focus, restored to blank on blur, RUN refuses placeholder text — the walk's first query executed the untouched `SELECT …;` as statement 1 and reported a failure the user never wrote); SQLite Test says \"database opens\"; the rest of the studio verified CLEAN live (edit→preview→apply→re-run 99→149, CSV bytes with `'=SUM` neutralization, safe defaults); ledger 75 records the narrow-pane toolbar clip; blessed: 30-char name default + CSV literal NULL
- **v1.265.0**: the arc review (v1.259–v1.264) — the v1.263.0 data-loss class closed STRUCTURALLY: the rename fix had removed the trigger but six sites still carried the un-finally'd `loading = true … loading = false` shape (incl. applyWorkspace, the re-aim path), so any future throw would wedge the guard and silently stop edit recording again; every raise now enters a try (withLoading helper + explicit try/finally) and LoadingGuardShapeTest makes the shape a build law — raise only above a `try`, drop only inside a `finally` — both rules mutation-proven ALONE per the v1.264.0 lesson. Verified CLEAN: LoopbackUrls cost profile (zero sockets for non-local URLs, BEACON check-time only, Watch tick fixed-delay), CleartextHttp, the arrow guard, the beginner-pass gates; two humanizer LOWs blessed in writing
- **v2.24.0**: batch 2 — Emmet CSS in HTML style regions (region-gated, clip law pinned + mutation-proven) + the docs fold
- **v2.39.4**: the five-walk night (docs only) — shelf/Check-My-Work/break-the-page/Explain-with-real-send/exporter-round-trip ALL PASS live in shipped 2.39.3, six screenshots captured from the session and wired; the Explain send answered from the real API with the actual fix
- **v2.39.3**: the teacher's exporter — Export as Learning Space… (the aimed project → a drop-in: bounded text gathering with spoken skips, TUTORIAL.md + run driver + .nmox-checkpoints.json riding along, refused whole unless it round-trips the student picker's own parser, checkpoint errors refuse at EXPORT time, self-owned-only overwrite; 2 mutants + the equivalent round-trip-gate mutant documented and structurally pinned)
- **v2.69.8**: docs — the walk's proof shots illustrate the user guide (the first-boot What's New over the main window; the update notifier's balloon seen live, its first illustration since v1.25.0) (docs only)
- **v2.69.7**: the screen walk of shipped 2.69.6 — the first-boot What's New was created but NEVER VISIBLE (a modal from the UI-ready hook before the main window is active stays unmapped; MainWindowUp waits for showing+active), two dialogs wider than a 1372-pt screen (DialogFit clamps to 0.8×screen), three boot Ordering warnings naming our rows fixed + LayerPositionCensusTest over the assembled cluster (its first run found two lazily-loaded folder collisions more)
- **v2.69.6**: docs truth — plan.md's addendum for the senior-RCP/PM passes and the in-repo-pipeline night (v2.62.2–v2.69.5) + currency header at v2.69.5 (docs only)
- **v2.69.5**: CI runs the gauntlet — the Update gauntlet workflow after every Release (previous release → latest under xvfb, both halves, logs kept; weekly + on demand; informational, never blocks a release); gate-pinned
- **v2.69.4**: the gauntlet's laws gate-pinned — no --refresh on the fresh-userdir run, TERM after the tracking wait, the last="true" poll, the installed-version proof; a reintroduced loop fails the build by name
- **v2.69.3**: the night close — the gauntlet's first-boot half (timed live boot + TERM; NbPreferences persist under SIGTERM) and its two rehearsals' corrections (the headless --update-all CLI LOOPS with or without --refresh → wait on update_tracking's last="true" entry then TERM; prove the INSTALLED version); 2.67.2 → 2.69.2 PASS incl. lastSeenVersion=2.69.2; night-arc review CLEAN
- **v2.69.2**: the update-center gauntlet in-repo — scripts/update-gauntlet.sh <from-tag> (long leash, update_tracking wait, fresh-cachedir proof boot, --refresh, the live-boot recipe for first-boot UI printed not attempted); gate-covered
- **v2.69.1**: the review of the night's tooling — `cmd | tail || …` read tail's exit, not the command's, in both pipeline scripts (the masking their headers warn about); `set -o pipefail` in both, gate-pinned by ShipScriptsGateTest
- **v2.69.0**: TypeScript runs without a build (futures F7) — IGNITION's node lane accepts index.ts/main.ts/src/index.ts (JS entries first) and runs `node --experimental-strip-types <entry>` — one argv for every Node ≥22.6, no probe (buildCommand also runs for tooltips/CI on the EDT); an older Node's bad-option refusal becomes the human wall at the pump; pure NodeTypeStripping + matrix pin, three mutants; proven headlessly on Node 22.9
- **v2.68.2**: the pipeline's third half in-repo — scripts/post-ship.sh (assets wait, main fast-forward from the main checkout only, tap wait, cask hop, branch delete; every wait leashed and named); ShipScriptsGateTest covers all three scripts
- **v2.68.1**: the ship half in-repo — scripts/ship-branch.sh (fork sha as a parameter, the docs-collision resolver embedded, line-anchored marker checks, quoted PR title, every exit checked) + ShipScriptsGateTest (bash -n over scripts/, usage refusal); Working Method names both halves
- **v2.68.0**: the night debt sprint — ledger 64 closed (OpenOnServe rescans once after attaching, the snapshot-to-attach gap zero-width by construction; seam-tested, mutant by name) + ledger 67 closed (SearchTerms.score EXACT/LOOSE/NO_MATCH; device search lists whole-word hits before prefix hits — 'compose' → HARBOR first; membership pinned across five queries) + ledger 65 re-blessed
- **v2.67.2**: the notifier's first day (docs + a test comment) — the daily update check, live for the first time since v1.25.0, walked in a stock 2.67.0: the check ran, the request answers 200 with the newer tag, and Tools ▸ Plugins opens on Updates (11) → 2.67.1 with Update armed; the balloon itself owed to a screen walk; user guide's "one-click download" corrected to the Plugin Manager
- **v2.67.1**: the close (docs only) — the update-center gauntlet 2.63.0→2.67.0 PASS (eleven modules via the platform's updater, byte-verified, clean fresh-cache boot) and the proof the fix was for: a live boot of the updated install ended by an orderly quit wrote lastSeenVersion=2.67.0 — What's New's first-boot rule ran on a version the module classloader could never read before; harness law: netbeans.close reboots exit before post-UI hooks, so first-boot UI is proven by a live boot + orderly quit; an absent preference means the path never ran
- **v2.67.0**: the version the product could not read — every product-version read (Welcome footer, What's New, Report a Problem, Agent Port initialize, and UpdateCheck's daily notifier since v1.25.0) loaded the branded startup bundle through the MODULE classloader → MissingResourceException → null in every shipped build (found by the day-close gauntlet: the What's New preference never written while the Getting Started tick was); ONE reader now, core.util.ProductVersion (netbeans.productversion property → branded bundle via the system classloader → null), ProductVersionGateTest bans raw reads; two mutants + a planted-read gate proof; the day close folded in (gauntlet update-path PASS 2.63.0→2.66.0, walk debts, three pipeline laws)
- **v2.66.0**: the PM's activation wish — a Getting Started column on the Welcome that ticks itself from records the product keeps (recent projects, LAUNCH events, live servings, ORACLE consent, ~/.nmox/learn); persisted ticks never un-tick; hides when done or on request; pure GettingStarted mutation-pinned
- **v2.65.0**: Report a Problem… carries the rack's last failed run (FailureContext from the flight recorder — command in a code span, exit code + duration, fenced error lines, redacted; absent when nothing failed; no rack → the old report); one mutant by name
- **v2.64.1**: the day-shift review of the PM release — What's New's first boot moved from Welcome's componentShowing (mid-boot, and never for a closed Welcome) to the platform's @OnShowing hook (WhatsNewHookTest keeps it there); the shortcut sheet resolves actions on the EDT (in-memory layer cache, no disk); redaction's over-reach blessed as the right side to err on
- **v2.64.0**: the product manager's wishes (David's ask) — What's New in the product (CHANGELOG bundled at build, parity-gated; first boot after an update shows the unseen entries once, fresh installs record silently, dev builds nothing), Report a Problem… (redacted bounded log tail + environment in an editable dialog; nothing sent — the GitHub new-issue page is pre-filled and the user submits; URL-cap clip says so), Keyboard Shortcuts… (derived from the running keymap through the platform's own action objects, the D/A/O/S notation law rendered, Copy as Markdown); three pure cores, three mutants by name; the live Help-menu read found the platform's Report Issue + Keyboard Shortcuts Card surviving the v1.11 eviction — hidden, gate-pinned; dialogs not hand-driven this release (said so)
- **v2.63.0**: the senior-RCP pass (David's ask) — every background thread a named daemon by construction (core.util.Threads at all 22 sites; DaemonThreadGateTest reads every module and fails on any other raw thread, hook statements read to their closer); ORACLE consent + model prefs userdir-scoped via NbPreferences with a one-time legacy migration; Angular diagnostics through the platform logger at FINE; nine never-imported public packages withdrawn (rack.model restored: return types reach friends without imports — the nbm check caught it); two idioms blessed in writing
- **v2.62.2**: deps housekeeping, the split way — Dependabot #649 absorbed through the gate: mongodb-driver 5.11.0, maven-compiler-plugin 3.16.0 (target stays 21), snakeyaml 2.7; platform/slf4j/OpenJFX pins untouched
- **v2.62.1**: the day close (docs only) — the update-center gauntlet across the day's nine releases PASSED (stock 2.57.0 → 2.62.0 in-app, eleven jars byte-verified, clean boot at 2.62.0 with zero SEVERE; harness law: wait for the platform's updater to write update_tracking before the reboot, and boot with a fresh cachedir — a reboot in the updater's same second loaded the old modules) + the shipped-app walk of 2.61.1 (minimap, ghost via ⌥⌘G + Tab, sticky) in one instance
- **v2.62.0**: pull requests get their review half (competitive lens R6 CLOSED) — Review Threads… (gh api pulls/N/comments through the user's own gh; pure GitReviews caps 200 comments and clips bodies at 2,000 chars; rendered as TEXT — the html-render class honored before it could bite; read-only) + Checkout… (gh pr checkout behind the pure GitCheckoutGuard: modified/staged files REFUSED out loud, untracked allowed; then a safe-default confirm naming PR + branch; chip refreshes; a FAILED gh checkout that left staged leftovers on a proven-clean tree — measured on a shallow clone — is restored with a HEAD-preserving reset, untracked files untouched, said out loud); every spawn (9 sites) behind mayRunProcess; the spawn ledger's chip entry made exactly true; four mutants by name
- **v2.61.2**: docs truth — plan.md current to v2.61.1 (the two-lens run + the Agent Port + today's editor day as dated addenda; the probe-cluster, press-twice, status-line, built-is-not-a-verdict, commit-before-probing, and D/A/O notation laws recorded) (docs only)
- **v2.61.1**: the day-shift review of the three editor units (minimap, sticky scroll, ghost text) — one find fixed: Complete with ORACLE read the WHOLE document then clipped to its 6,000/1,500 window (the bounded-read law's exact forbidden shape); bounded at the read now, BoundedCompletionReadGateTest names the whole-document shapes; bars/listeners/consent verified CLEAN; the echoed-head trim recorded as the text channel's honest ceiling
- **v2.61.0**: Complete with ORACLE — ghost text on demand (competitive lens R4 → PARTIAL on the house's terms): ⌥⌘G sends the code around the caret ONCE (6,000 before / 1,500 after, clipped and said so) after the CODE consent with the keychain key, the single-fenced reply painted as VIRTUAL gray text through the platform's own `virtual-text-prepend` highlight attribute (editor-lib2 HighlightsViewFactory → PrependedTextView, decompiled); Tab inserts as one undo unit, any edit/caret move/click dismisses (Escape is swallowed above a docked TC — v1.205.0 law re-measured), the document untouched until Tab; chord notation law: D=⌘ A=⌥ O=⌃ on macOS, so ⌥⌘G is "DA-G" (EOF falls back to insert-selected, said out loud); pure OracleComplete + gated OracleCompleteEngine, eight tests, three mutants by name; no always-on stream — the gesture is the gate
- **v2.60.0**: sticky scroll (competitive lens R7 CLOSED) — the enclosing declarations of the top visible line pinned above the text as up to three rows of the source's own lines (editor font, past the gutter, click to jump), from the Navigator outline's items via pure StickyScope (item END lines derived per family: brace balance with comment/template + quote blanking, indentation for Python/Nim/YAML, next-item elsewhere; three mutants by name); a root Editors/SideBar North registration reaches every editor; View ▸ Sticky Scroll toggles live; bar collapses when nothing encloses the top line
- **v2.59.0**: the minimap (competitive lens R7, first half) — a silhouette strip beside EVERY editor's scrollbar via the root Editors/SideBar SPI (the error stripe's registration shape decompiled: East, non-scrollable, position 6900 inside its 7000); pure MinimapModel (2 px rows until the document is taller than the strip, then the WHOLE document scales to fit — never a second scrollbar; 120-column bar cap; 40k-line bounded shape with an ellipsis), three mutants by name; click/drag scrolls, View ▸ Minimap toggles every open editor live; listener symmetry + coalesced reshape + Document.render from day one; sticky scroll stays OPEN in the lens
- **v2.58.1**: the day-shift review + its walk's find — top-level at-rules complete at the file top and after } (propertyPrefixAt's boundary rule had made @position-try/@view-transition uncompletable where they live); the second keyword of a value completes (valueContextAt crosses earlier keywords, bounded by ;{}); and the real bug, product-wide: **a second Ctrl+Space wiped every NMOX completion item** — the platform re-queries as COMPLETION_ALL (9) while a popup shows (CompletionImpl decompiled) and all ELEVEN providers gated createTask on queryType != COMPLETION_QUERY_TYPE; found because both fixes read "No suggestions" live while their tests passed, split by a probe cluster (provider queried, core correct, auto-popup rendered the item, the explicit press arrived as 9); mask at all eleven sites + CompletionAllQueryGateTest (source law + behavioral law over every real createTask), two mutants by name; LEARN kind verified CLEAN
- **v2.58.0**: learning space #93 — WebAssembly Components (WIT), contract-first (RUN = wasm-tools component wit; the file:line refusal taught as the feature; live-proven before the entry existed) — and the walk's find: every manifest-less run space (c/cpp/pascal/odin/cobol/haxe/perl/graphql, since v1.74/75) was DEAD on RUN — SOLDER requires a manifest and detectKind saw NONE; fixed at the root with ProjectKind.LEARN (the marker IS the manifest, the rack driver IS the toolchain; the resort after STATIC), blessed laneless in both kind gates; the count gate binds docs/tutorials/*.md too (a same-named tutorial copy hid a 92)
- **v2.57.0**: the everyday-2031 CSS vocabulary — a control-termed probe of RELEASE310's css-lib (container-type/text-wrap PRESENT) found anchor positioning, view transitions, @starting-style, scroll-driven animation, field-sizing, interpolate-size ABSENT; CssFutures completes 23 properties with meanings + values + 3 at-rules across the stylesheet and markup-region families, and anchor()/anchor-size() on the spec's inset/sizing hosts (walk find: provenance read a placeholder → the host name); the windows editor floor (0.66/0.67) answered by the coverage seam, never the floor; an equivalent mutant (a colon guard that could never fire) deleted with the boundary law pinned
- **v2.56.1**: the MCP arc review — output schemas were not contracts (4/6 closed schemas emitting undeclared keys, arrays declared as bare []); McpSchemaContractTest validates every builder's real output against its declared schema; resources/read + prompts/get answer -32603 on a throw and AgentPort carries a source-pinned last-resort net; the walk validated live structuredContent against the live schema
- **v2.56.0**: the Agent Port becomes a complete MCP server — Resources (nmox:// URI-views over tools' structured output, byte-identical; unknown → -32002) + Prompts (diagnose_failure/review_setup fold live state); initialize declares all three; read-only inherited not re-argued; walk-proven by curl; the macOS rendering-probe flake confirmed by rerun on the same SHA
- **v2.55.0**: the best-MCP-server pass — every tool returns text AND typed structuredContent under a declared outputSchema (structured = single source, Texts renders), readOnlyHint/destructiveHint:false annotations in tools/list, diagnostics(file) filter, one-shot ide_context (6 tools)
- **v2.54.0**: the Agent Port (MCP), F4 — READ-ONLY BY CONSTRUCTION: Tools ▸ Agent Port (MCP)… opens a loopback-witnessed MCP Streamable-HTTP endpoint with a per-start bearer (constant-time), any Origin → 403, POST-only, body-capped; five read-only tools; McpReadOnlyLedgerTest fails the build if any mcp class names a spawn/write/trust primitive
- **v2.53.0**: import-map intelligence (futures F3) — ⌘-click a bare specifier → its mapping (exact wins, else LONGEST trailing-slash prefix), ⌃Space in an open import quote lists mapped specifiers; JSONObject hash order let a first-match mutant survive → deterministic distinguisher
- **v2.52.0**: the futures arc opens — docs/engineering/futures-2031.md (six bets) + WIT/Component-Model editor citizenship (87 grammars, own outline family, wasm-tools on Doctor) + the everyday-2031 JS vocabulary (Temporal, navigator.gpu, startViewTransition, built-in AI namespaces, five snippets)
- **v2.51.0**: BATCH — Pull Requests… on the git chip via the user's own gh (keychain-only by never holding a token, PlainTables rows) + Dependabot the split way (actions absorbed by hand with the exporter literal; slf4j-api 1.7→2.0 REFUSED as the v2.21.6 OSGi pin, now a dependabot ignore) + v2.50.0 review CLEAN
- **v2.50.0**: BATCH — the Tests window ⌥⌘2 (TestIndex over RunFocusedTestAction's own patterns; Run rides extracted runDiscovered, spawn ledger unchanged) + Draft Commit Message with ORACLE (staged diff, git.diff consent kind, never commits; the chip's boot-law gate taught both lawful spawn sites)
- **v2.49.1**: the competitive arc review — trailing-newline glue fixed (matchTrailingNewline, symmetric) + symbol-walk depth-32 cap that speaks
- **v2.49.0**: Go to Symbol, project-wide — NmoxSymbolProvider feeds jumpto's dialog (⌥⇧⌘O ×5 profiles) and ⌘I bridge from ProjectSymbols over OutlineModel; two findability finds (sigil prefix, the bridge's removeNonJavaChars folding); windows editor floor answered by moving matching to pure SymbolMatch
- **v2.48.0**: the competitive lens opens — docs/engineering/competitive-lens.md (seven relief items) + Edit with ORACLE (instruction → single fenced reply → BEFORE/AFTER preview → stale-buffer-guarded one-undo apply; refuse-not-truncate)
- **v2.47.1**: the arc review — the cross-network decimals cache dropped at switch (a "1.5" in the re-read window would have mis-scaled if the address held a different token; stale now REFUSES, the money law's failure mode) + the ABI cap in ImportedContract's ctor (every construction site enforces it, mutant by name; oversize file entries skip alone); two-decoder duplication + raw-units-by-design blessed in writing
- **v2.47.0**: any history — History… (bounded range: over-cap REFUSED never clamped, mutation-proven; decoded rows vs artifacts + canonical events; CSV quoted + formula-neutralized per the v1.101.0 law) + the fee strip on the chip (eth_gasPrice, honest absence); mainnet walk: 277 decoded USDC Transfers over 4 real blocks exported to parser-clean CSV, live gas on the chip
- **v2.46.0**: the token faces complete — address-carrying imports open ATTACHED from the tree; ERC-721 face (strip name/symbol + Owner of… with tokenURI); amount fields speak human on ERC-20 canonical writes (interpretAmount: raw passthrough, decimals-converted "1.5", unknown-decimals and exponent forms REFUSED — the money law); anvil walk: typed 1.5 → chain's own balanceOf answered 1500000000000000000 exact
- **v2.45.0**: any contract, any chain — Import ABI… (one-parser law, workspace-persisted with heal+cap+inverse, walk-proven on REAL MAINNET: USDC's live metadata + a stranger's tx decoded) + network presets (mainnet/Sepolia keyless gateways, chainId-proven live); the walk's find fixed structurally: loadGuarded's second Workspace construction site dropped imports on relaunch — a compat ctor masked the compile error; ONE parse site now, masking ctor deleted, boot path pinned (reborn bug kills four)
- **v2.44.0**: more Web3 (David's ask) — Contract Studio learns tokens: ErcStandards (ERC-20/721/1155 by complete required-signature sets), the TOKEN STRIP (name/symbol/decimals/supply via real eth_calls, TokenAmounts exact both ways — over-precise amounts REFUSED, a truncated transfer is money), Balance of… read-only, and Inspect tx… (calldata + logs decoded against workspace ABIs + canonical token events, unknowns shown raw); walk-proven on real anvil + forge (gasUsed matched the node's log to the digit); three mutants by name
- **v2.43.0**: the blessed certificate — the NBM-signing cert ships in-product as a KeyStoreProvider at TRUST level (the Apache mechanism; recon: Lookup-collected, Utilities.verifyCertificates checks signers against TRUST stores), PEM public-half bundled + parity-gated against KEYS (drift mutant dies by name); broken resource degrades to accept-once, never fails; GUI TRUSTED label collects on the NEXT update
- **v2.42.0**: the self-signed trust chain (official certs = v3.0 by decision) — NBM signing ON via the v1.58.0 seam (self-signed RSA-4096 keystore, secrets set, keychain-only passwords); releases add GPG-signed SHA256SUMS (assets 19→21, gate updated); KEYS at repo root; ledger 86 records the v3.0 remainder incl. the NBM TRUSTED-bless recon
- **v2.41.0**: the site goes public — the pages workflow deploys ui/src/main/release/website to GitHub Pages verbatim (no build step: the web serves the cluster's exact bytes, kit-parity law covering both); the hero note stops lying off-localhost (site.hero.note/.web pair in both catalogs, site.js picks the true one by host); README/user-guide point at https://nmox.github.io/NMOX-Studio/
- **v2.40.1**: the site server's review — HEAD answered with a real content length made the JDK WARN per request (browser probe HEADs would spam messages.log); both branches now HEAD-quiet, mutation-proven ×2 via the logger tap whose first version tapped the WRONG logger name (com.sun.net.httpserver is the System.Logger name, not the source-class package); containment verified against encoded traversal live
- **v2.40.0**: the product ships its own website — Help ▸ NMOX Studio Website (local) serves the bundled site (SiteServer: loopback-witnessed bind, traversal→404 live-proven, GET-only) with the ⇄ chip on the product's own story; a11y.css/i18n.js ARE the kits' output byte-for-byte (SiteShipsTest: kit-parity + data-i18n↔catalog keys; PackagedSiteGateTest: cluster-parity at integration-test phase — the plain test phase has no cluster on a clean build), EN/ES switch walked live; Welcome footer Website ⇄
- **v2.39.5**: the checkpoint tutor (ORACLE credits back) — failed Check My Work offers Explain with ORACLE… (failed checks + the learner's checked file capped, space.check consent, command-kind sends detail only); REAL-send walk-proven with both moments as guide shots; the walk's find fixed — the kind-consent boilerplate's "does not send your source files" contradicted file-carrying kinds, now "nothing beyond the line above"
- **v2.39.2**: elevation batch two — experiment Duplicate (full-tree fork, forkedFrom marker, original untouched, 3 mutants) + Explain error… (the browser.error OracleAsk flow: last located error + 7 capped source lines, consent states literally what leaves) + the gate's stale-rollup grace (a checks fail must persist a 20s re-read with nothing pending)
- **v2.39.1**: learning spaces check your work — catalog checkpoints (file kind contains+absent, command kind argv-only), File ▸ Check My Work, 4 flagship seeds, CheckpointParityTest
- **v2.39.0**: runtime errors become first-class citizens — Browser window.onerror → serving-registry resolve → DiagnosticsBus [browser] (squiggle + Action Items, cleared per page load)
- **v2.38.9**: the pipeline sheds its waits — the gate's tree-identity fast path (tag immediately when the merge tree equals the green PR head's), the docs-only CI lane (~2 min), the cached jlink runtime across all three release lanes; fetch retry + startup_failure auto-rerun; the fold-script law's structural death (set -e chains, enforcing literal checks)
- **v2.38.8**: experiments and learning spaces to 100% — spaces Promote… (guards mirrored from discard, three mutants), both promotes quiesce-before-move (the v1.290.0 family), spaces shelf parity (teaching header/age/empty-offer), both shelves in ⌘I (experiments quietly); the docs for this entry shipped via v2.38.9's repair — the gate refused DOCS-MISSING correctly both times
- **v2.38.4**: the evening review + the Contract Studio walk — the day's fresh code CLEAN under probing; GeneratedJsPurityTest extends the sweep-leak gate to the other three JS emitters (i18n helper, PWA sw ×2 strategies, Block codegen); the walk ALL CLEAN ~85 releases on (anvil auto-connect, deploy block 1, increment mined gas 43,482, CALL → 1, the address book's 36-day-old deployment surviving the platform move)
- **v2.38.3**: A Day at Meridian (David's ask — one story, every area, screenshots) — docs/a-day-at-meridian.md with eleven live-captured shots + the standing illustrations; the story's own walk found DevTools' DOM pane DEAD (the v2.37.5 Turkish-I sweep had rewritten .toLowerCase() inside DOM_SNAPSHOT's injected JS — "Can't find variable: java"); healed + InjectedJsPurityTest gates every DevScripts constant against Java-isms (the cross-language sweep leak, a named class); DevTools scenes captured from the healed build
- **v2.38.2**: batch 13 — the Standup opens with its sprint (day-of inside the window, test-pinned); sink + README riders (sprint cadence, suffixless Angular); the API Studio walk ALL CLEAN ~45 releases on (persisted workspace, live {{repo}} 200 whose pushed_at was the day's own merge, grade B honest absents, byte-exact curl)
- **v2.38.1**: batch 12 — the sprint cadence: Close Sprint offers the next sprint pre-filled (name incremented, same-length day-after window, Cancel starts nothing) + the Sprint dialog speaks velocity (last three closed, newest first, honest null); walk-proven live incl. the Sprint 8 pre-fill and the velocity line
- **v2.38.0**: batch 11 — the gate fetches-never-checks-out (the tree-collision class dies); kits reach src/index.html via shared KitFiles.entryPage; suffixless switcher parity pinned; the ALS half researched to an honest refusal (the ledger-81 double-rename class would return)
- **v2.37.8**: ledger 82 delivered — suffixless Angular templates open with the template editor (coloring, popup, ⌘B, the component jump): two-signal predicate (sibling @Component + angular.json ancestry, repo-bounded) + the two-half seam (registerEditor plain + public setMIMEType — the walk caught each half alone failing); plain html walked as the control; ALS-binding remainder recorded; scripts/ship-gate.sh parameterized in-repo (the sed-derivation rot class dies)
- **v2.37.5**: i18n both directions (David's ask) — the I18n Kit (Web) wizard (locale catalogs same-keys-gated, dependency-free data-i18n applier keeping <html lang> truthful, I18N-NOTES.md, idempotent wiring; three mutants by name) + the Turkish-I sweep (55 bare case-folds → Locale.ROOT; CaseFoldLocaleGateTest bans the bare form failing-first) + ledger 85 records the honest IDE-l10n position + two shipped-docs repairs (changelog order, README splice)
- **v2.37.4**: a11y both directions (David's ask) — the A11y Kit (Web) wizard joins the kit family (a11y.css + A11Y-NOTES.md + idempotent lang/skip-link/stylesheet wiring; zoom-disabling viewport warned never rewritten; walk-proven with the second run hash-identical, three mutants by name) + the IDE-side name sweep reaches the db/web3/infra dialogs the rack law never covered
- **v2.37.2**: the Kitchen Sink catches up (experiment front door + sprint ceremonies stations) + the a11y sweep — the v2.4.0 name law HELD on all twelve painted Overview widgets (two false finds from narrow grep windows, recorded); the real gap: the experiments dialogs' label-less inputs, now named + gate-pinned
- **v2.37.1**: the output gauntlet — everything the product GENERATES proven against today's world: all 15 templates run for real (8 installs + 6 builds + tests + five toolchain legs green; Angular env-limited with its README floor note exactly right), 4 Dockerize recipes built + 2 smoke-run as containers (SPA deep link 200 via try_files; warm-the-pulls-before-judging recorded), 25 exporter-only action pins checked live (all majors current; two stale exact pins bumped)
- **v2.37.0**: the scrum master's wishes (David's ask) — the sprint: Sprint… names a window (refusals out loud), the Overview grows the header + BURNDOWN reconstructed from done stamps (ideal line under the real one, future unplotted), Sprint Report… (Standup's review sibling: done/open/blocked/time/retro/velocity), Close Sprint… archives for velocity behind a safe default (cards untouched); mangled windows heal to no-sprint with DELIBERATELY REDUNDANT guards — the equivalent-single-mutant/double-mutant-kills lesson recorded; walk-proven incl. disk-byte truth
- **v2.36.6**: the Docker walk (docs only) — the corpus callosum ALL-CLEAN on a real daemon ~90 releases on: engine/containers/logs, the v1.35.0 offer with the PUBLISHED port pre-filled, keychain password, Test OK, SELECT version() 3ms; the balloon-fades/bell-keeps-the-link walk recipe recorded
- **v2.36.5**: the hostile-input lens — JsonTypes' quoted property names never escaped quote/backslash (a hostile key emitted syntactically BROKEN TypeScript; deep-nest refuses cleanly, 50k rows in 49ms); fixed + mutant by name; campaign lens verdicts recorded (EDT audit, keybinding census, Standards Kit walk — all CLEAN)
- **v2.36.4**: the zero-defect campaign opens (David's directive) — the honest inventory (zero TODOs, clean boot census, ledger audited); ledger 80 closed STRUCTURALLY: PopupPositionUniquenessTest from the generated layer named FIVE Emmet-vs-switcher collisions where the ledger knew one; Emmet 95→94, differentially live-proven (control 1 warning, treatment 0)
- **v2.36.3**: the sweep's audited tail — the infra graph's duplicated node id silently REPLACED the first node and its doId linkage to a live billed resource; healed at parse (both nodes AND both cloud linkages survive); rack cables + infra wires verified CLEAN; the parse-time-heal audit COMPLETE across all seven studio files
- **v2.36.2**: the law sweep — three young laws swept across every sibling: feedback-lands-where-typed found ELEVEN more IRC handlers answering into network status (incl. "Not connected" — every command typed while disconnected answered invisibly; the gate now structural: no bare network-key appends); the parse-time-heal law found FOUR unhealed studio parsers where a keep-both merge could wipe a sibling's keychain secret (api request ids, db connection ids, web3 network names) or throw the preview's double define (block tags) — all heal at parse, first-occurrence-keeps-identity; buffer-wins swept CLEAN; five mutants by name
- **v2.36.1**: the manager teaches too (David's follow-up) — the shelf header speaks count + measured disk cost + the lifecycle sentence (size walk off-EDT, lands when ready); the empty shelf OFFERS to start an experiment (default button acts); Open re-opens EXPERIMENT.md; Discard speaks on the status line; pure shelfSummary/sizeOf pinned; the missing-verdict-is-a-failed-run mutation lesson recorded; the walk's find FIXED — an aimed discard left the studio on the deleted path (the v1.290.0 law, missed ~90 releases while the learning-space sibling carried it): re-aim at ~/NMOX after the delete, mutation-pinned
- **v2.36.0**: experiments teach (David's ask — the first tool for learning a stack): every experiment born with an EXPERIMENT.md walkthrough that OPENS on aim (press this, edit this file, here's this stack's IDE intelligence), the edit-file claim parity-gated against the real generators (first run caught Solid's src/index.jsx); the install runs at creation (checkbox + package.json guard, spawn-ledger classified) so the FIRST Run succeeds; New Experiment leads the Welcome START column; the dialog fronts the 92-space catalog; the shelf shows age; three mutants by name
- **v2.35.0**: the platform advances — RELEASE300→RELEASE310 (ledger 84 closed, David's GO): the property flip after the dossier + v2.21.6 slf4j root-cause; gauntlets ALL PASS on the assembled 310 app (boot 6s/0 spawns/0 SEVERE, FX 26 browser + DevTools DOM, nine chords, 11-NBM update-site dry run, SPI example green); the update boundary MEASURED — 310 dependency floors hold ≤2.34.5 installs back honestly, installers carry them across (v1.256.0 timing law)
- **v2.34.4**: the IRC Libera walk — live TLS/join/echo//lastlog CLEAN; /filter feedback answered in the wrong room (network status while a channel was watched) — now lands where the command was typed, mutation-pinned; the session shot is the guide's first IRC illustration; the gate template's stale docs-landed literal repinned
- **v2.34.3**: the night audit — zero-boot-spawns re-verified live ~340 releases after v1.38.0; tools JaCoCo floor 0.74→0.75 (the only honest margin; editor/rack stay windows-binding by law)
- **v2.34.2**: the Kitchen Sink catches up — class-intelligence/env-keys/API-export stations, claims test-matched; DocsCountGateTest's double blind spot closed failing-first (census + bare-"NN grammars" matcher, stale count named by line)
- **v2.34.1**: the night close — CopyTsTypes prefers the open BUFFER over disk bytes (unsaved edits typed stale content); app.all's refusal documented+pinned; openWithDraft verified CLEAN (the synchronous componentOpened load contract); docs current
- **v2.34.0**: batch 10 — Test in API Studio drafts a request from a route line (v1.292.0 rule, {{base_url}} url, From-the-editor reuse; walk-proven); Copy TS Types on .json files (walk-proven, nested interfaces named after the file); the 2.30.1→2.33.1 update gauntlet PASS (eleven jars byte-verified, clean reboot)
- **v2.33.1**: the day review — JsonTypes' claimed-but-never-emitted | null unions implemented (a null sibling VANISHED into string; the v1.189.0 law); findRoute's exact pass strips query strings; Copy TS types + Run Script enter the user guide
- **v2.33.0**: the wishlist's second helping — v2.31.0's three recorded wishes granted: Copy TS types (JsonTypes pure codec, sorted emission, merged array optionals, honest unknowns), Run Script on package.json (NpmScripts caret rule, trust-gated spawn), param-aware fetch→route (:id patterns, exact wins); five mutants by name
- **v2.32.1**: the illustrated docs, part 2 — the Motion pane photographed mid-marquee + DB Studio's live grid, wired under the ImageRefsTest law
- **v2.32.0**: the illustrated docs — 11 forge tabs regenerated + 6 staged feature shots wired into the user guide; ImageRefsTest gates dead refs AND orphans (failing-first, 11 named); the popup capture recipe recorded
- **v2.31.0**: the full-stack wishlist (David's ask) — process.env./import.meta.env. completion from the real .env family (+ ⌘-click to the declaring line, values truncated — the popup reminds, never discloses), fetch('/path') → the Express route that registers it (v1.292.0 rule reused, cache.get receiver-discriminator pinned), Copy fetch beside Copy curl (btoa basic auth, JS escaping pinned); shared ProjectJumpHyperlink skeleton (fourth-copy promotion); recorded-not-granted: JSON→TS types, package.json run gestures, param-aware matching
- **v2.30.1**: the class-arc review — the split-editor caret mismatch in Rename Class… (lastFocusedComponent vs popup context) fixed with the documentBelongsTo identity rule, wiring source-gated; one LOW recorded (style-region CSS-comment attr sweep)
- **v2.30.0**: batch 8 — the class arc reaches JavaScript (querySelector/closest/matches after the dot, classList bare names; fetch('.card') stays a URL — context mutants by name); the walk's find: MID-TOKEN accepts stranded token tails in the class item AND the v1.330.0 var( item — tail-fold fix in both, re-walked; floor trip answered by itemsFor seam + hyperlink tests, floor unmoved
- **v2.29.1**: docs truth — the class-intelligence arc enters the record (docs only)
- **v2.29.0**: batch 7 — Rename Class, project-wide: the arc's capstone (right-click any class token → every whole-token span rewrites, count per file); the refusal ladder speaks at all five rungs; FileObject-stream writes reload open editors; pure-core span math, three mutants by name; walk-proven incl. the collision refusal and prose-untouched disk bytes; the -q-grep-fakes-survivors harness lesson
- **v2.28.1**: docs truth — the class intelligence enters the README and user guide (docs only)
- **v2.28.0**: batch 6 — the reverse class jump (stylesheet .selector → first markup usage, count on the status line, whole-token + class-attr-only mutation-pinned) + the quieter boot (82 completion registrations positioned; PositionedRegistrationsTest from the generated layer, org-nmox scope with the platform-row bless; boot census zero our-row Ordering warnings)
- **v2.27.0**: batch 5 — the class attribute knows your stylesheets: class completion with provenance across the markup family, ⌘-click class→rule; CssClasses core (comments/strings/url() blanked; classes-only, #id hex-twin reason); the platform find: CSL goto resolves only linked sheets — dispatcher decompiled, position 10 with reason, hover consult pinned live, click leg ledger-76
- **v2.26.2**: docs truth — CLAUDE.md v2.23.0→v2.26.1 + plan.md batch-mode addendum (docs only)
- **v2.26.1**: the README renders again (David's catch) — the by-audience paragraph sat inside the front page's 2×2 screenshot table (GitHub absorbed each wrapped line as a one-cell row; the caption row stranded as literal pipes, emphasis imbalanced); structure repaired with every word kept; MarkdownTableShapeTest gates glued-paragraph + orphan-row shapes over README + docs/*.md, failing-first proven (4 defects named by line)
- **v2.26.0**: batch 4 — the batch arc's review made true: .vue/.svelte-declared tokens feed project-wide var( completion (walked live cross-file), Emmet colon form with the written-declaration law (w100→width:100px, w 100 refused, mutants killed by name); the changelog-example-never-in-the-dialect find; the gate template hardened (merge sha from the PR record, no worktree access) after the near-miss that almost tagged wip
- **v2.25.0**: batch 3 — the style-region family (swatches, picker, var( completion, token jump, Emmet-CSS) reaches Vue SFCs, Svelte, and Angular templates via ONE shared MARKUP_MIMES list + MarkupFamilyParityTest (a missing mime fails the build, not the framework you didn't test); real-.vue walk first try
- **v2.24.0**: batch 2 — Emmet CSS in HTML style regions (region-gated, clip law pinned + mutation-proven) + the docs fold
- **v2.23.0**: batch 1 — design tokens reach HTML both directions (var( completion/⌘-click/swatches-through-tokens in regions with cross-region resolution; <style>-declared tokens feed the project scan, region-parsed); two mutants by name; walk-proven incl. the untouched prose usage
- **v2.22.0**: swatches + picker reach HTML — HtmlStyleRegions (style blocks + attributes, comments blanked, exact offsets); prose stays prose; walk-proven
- **v2.21.7**: docs truth — the late shift + gauntlet PASS + GO-READY in the headline (docs only)
- **v2.21.6**: the RELEASE310 arc complete — root-caused (Felix probe named org.slf4j [1.7.0,3.0.0) missing; cluster-placement accident, netigso cleared) and fixed (one explicit slf4j-api dep pins bundle placement both platforms; 310 boots clean); ledger 84 GO-READY
- **v2.21.5**: the sweep made true — Contract + Infra walked clean (31-day address-book persistence; the exact wire-refusal message); a wrong claim corrected by making it true (docs only)
- **v2.21.4**: the Block Studio walk all-clean (loopback preview byte-proven, save-to-source, stop-deregisters); the day's one CI flake rerun-greened on the same sha before the tag (docs only)
- **v2.21.3**: docs truth — the API walk recorded (all-clean: live {{var}} 200, grade B, byte-exact curl, persisted history) + headline currency (docs only)
- **v2.21.2**: the DBA walk — clean across every leg (placeholder, hostile grid, PK-gated edit→disk truth, CSV neutralization, Manage rename); one find: Add and Edit were the connection CRUD's silent two, fixed + CrudGesturesSpeakTest; enumerate-before-you-mutate recorded
- **v2.21.1**: docs truth — CLAUDE.md v2.20.0→v2.21.0, the shift's four entries (docs only)
- **v2.21.0**: learning space #92 — Sass (indented): the first stylesheet-dialect space, the v2.20.0 vertical completed the house way (native swatches, Compile to CSS, token completion reading the .sass file itself, refusals as features); picker-walked live; 92 spaces ×5 doc homes
- **v2.20.3**: the RELEASE310 runtime probe — RED: org.eclipse.jgit fails under Netigso and boot wedges before module turn-on, jgit bytes + cluster census identical to 300 (the OSGi host regressed); NO-GO until netbinox is root-caused; the v2.19.4 refusal vindicated by measurement (docs only)
- **v2.20.2**: the day review — .sass tokens scanned line-bounded (the shared value pattern would swallow lines), // comments blanked (a commented token registered as LIVE in scss since v1.330.0; whitespace guard keeps https://), NgSwitch's missing x-sass popup registration; the dialect-divorce review lens recorded
- **v2.20.1**: docs truth — CLAUDE.md v2.19.1→v2.20.0, the day shift's six entries (docs only)
- **v2.20.0**: indented Sass, first-class — text/x-sass + the canonical indented grammar (TheRealSyler, MIT, sha256-pinned; 86 grammars) + real CSL kit; ride-alongs audited (swatches, picker, tokens, completion, spellcheck, // toggle, Compile to CSS — which the split would have LOST); Emmet/Prettier/outline/stylelint deliberately out, reasons test-pinned; walk-proven; firstLine prefers the Error line over a crashing tool's stack path (v1.303.0 class)
- **v2.19.6**: the RELEASE310 dossier — all four decompiled assumptions byte-identical 300→310, full verify GREEN under the property override, every floor holds; GO remainder is runtime gauntlets; David's call (docs only)
- **v2.19.5**: the device-author walk — the JSON-device authoring loop CLEAN in the shipped app; the ancestor-trust sentence added to the doc (a grant covers subfolders) (docs only)
- **v2.19.4**: deps housekeeping, the split way — junit 6.1.3/org.json 20260814/mongodb 5.10.0 in; RELEASE310 refused structurally; dependabot ignores org.netbeans.*; ledger 84 opened
- **v2.19.3**: the night close — the 2.10.0→2.19.1 update-center gauntlet PASS recorded (eleven jars byte-verified, clean reboot) + Known-Issues truth (docs only)
- **v2.19.2**: docs truth — CLAUDE.md v2.17.0→v2.19.1 + plan.md day addendum (docs only)
- **v2.19.1**: the night review — the v2.19.0 conversion missed CssServer (three-mime CLASS registration, up to three vscode-css-language-server copies) because the gate checked classes implementing MultiMime — the mechanism, not the outcome; MultiMimeSingletonGateTest now derives its population from the GENERATED layer (any provider under ≥2 Editors mimes must be a methodvalue singleton with mime parity, size-floored), proven failing-first, parity mutant killed by name, the hand-kept factory list deleted
- **v2.19.0**: the senior-RCP pass — one language server per project, not per mime (ledger 83): multi-mime providers implement MultiMimeLanguageServerProvider + static singleton factory registrations (the platform's reuse map is instance-keyed; class registrations instantiate per mime folder); the tsserver pair partitions by workspace kind (plain: shared ts+js; Angular: js-only beside ngserver) preserving ledger 81; live A/B deno lsp 2→1 vs shipped 2.18.0, tsserver 1 for mixed .ts+.js; the init-failure churn measurement trap recorded
- **v2.18.0**: the polish pass (David's ask) — the editor layer's duplicate folders stopped warning every boot; Task Board refused gestures speak and write nothing (refused edge-move mtime byte-identical, walk-proven with all three clock messages live); SONAR kill + Remove Dependency get safe defaults; README counts 85/91/60 with DocsCountGateTest's three blind spots closed; ellipsis/arrow/em-dash convention sweep; the walk law: a plugin update mid-walk swaps a writable dev cluster's jars IN PLACE
- **v2.17.1**: docs truth — CLAUDE.md v2.9.0→v2.17.0 + plan.md stretch addendum (docs only)
- **v2.17.0**: the day-shift close — the editor JaCoCo floor to its windows-binding measurement (0.68 on the windows lane where POSIX-gated tests skip; floor 0.67 with the law at the pom line, the v1.299.0 rack call) ending the gate's noise flake; the v2.16.0 Motion retarget law walk-proven in shipped 2.16.0 (Stop froze the PLAYING element while another held the selection; a retargeted Play resurrected nothing); the v2.15.0 exec-reaper observed live — SIGTERM took the serve process down with the app
- **v2.16.0**: the arc review (v2.11–v2.14) — the Motion preview belongs to the element that started it (the v1.172.0 law, third appearance): pure MotionTargetGuard wired at Play/Scrub/Stop, mutation ×3 by name incl. the call-sites-exist wiring gate; scrub status honesty; Keyframes.Frame unmodifiable; shipped through a tag collision, a GitHub outage, and a windows floor flake with never-tag-unverified intact
- **v2.15.0**: the orphan-process audit (its own session) — long-running spawns outside the rack orphaned on EVERY JVM exit; CommandExecutor now registers every spawn in a live-set reaped by one exec-level shutdown hook (TERM the tree, bounded grace, KILL stragglers), covering every present and future caller by construction; POSIX-only by construction (TerminateProcess runs no hooks); repo-scan gates worktree-safe (dot-dir filter on the repo-relative path)
- **v2.14.0**: Vue and Svelte, top-notch (David's ask) — the kit-AND-loader dead-chord cure (VueEditorKit/SvelteEditorKit + DataObject loaders; a kit without a loader is never consulted); Volar 3.x's tsserver/request wall answered by pinning @vue/language-server@2 + tsdk + hybridMode:false through the generalized InitOptionsInjector; Vue auto-pairs + 60-word vocabulary with @/: sigil walk; Svelte 5 dotted runes; Emmet in both SFC mimes ×5 profiles; both servers live-proven
- **v2.13.0**: MooTools, first-class (David's ask) — learning space #91, Classic Web (MooTools) template (vendored compat build byte-for-byte), completion 44→105 (floor 100), honest project-level EOL chip exposing + fixing eolMessage()'s hardcoded "jQuery"; five doc counts to 91
- **v2.12.0**: DHTML lives (David's ask) — the Motion pane in DevTools: keyframe timeline strip (clamped diamond drags, double-click add/edit), paused-negative-delay scrub, Play/Stop via one injected style tag, Apply-to-Source through the shared writeBackWith refusal ladder (same-named @keyframes replaced at the LAST occurrence), seven parser-proven DHTML presets; walk-proven end to end
- **v2.11.0**: the Kitchen Sink tutorial (David's ask) — docs/kitchen-sink.md, 21 do/see stations across every surface, claims checked against pinned tests
- **v2.10.2**: the night review — local echo honors your filters (both echo sites consult the verdict; FilterWiringGateTest the law)
- **v2.10.1**: floors ratchet (ui +10pts) + the 2.x update-center gauntlet (v2.2.1→v2.10.0 in-app, 11 jars byte-verified, clean reboot)
- **v2.10.0**: WeeChat tranche 2 — custom /filter (hidden on screen, kept on disk, Libera-proven) + /lastlog
- **v2.9.0**: the Task Board arc review — merged session lists are healed on load: `fromJson` now enforces the two open-session invariants the runtime keeps by construction (open pair only ever a card's LAST; ONE clock board-wide) that a keep-both merge of the checked-in file can violate — a stray open pair was a phantom forever-running session, unreachable by any gesture while the TIME report and the Standup counted it to NOW; strays close at their OWN start (no invented time), the latest start keeps the clock; the parse-site comment had claimed the rule since v2.6.0 without code (the v1.189.0 law again); mutation-proven ×2 by name; mutate() coverage/standup threading/FilePulse symmetry verified CLEAN
- **v2.8.0**: the Standup — a toolbar button turns the board's own records into the daily report as markdown: Yesterday/Today from done stamps + day-clipped time sessions (midnight splits exactly like the TIME report, running clock marked), Blockers from the register (card — owner · unblock action), Commits since yesterday from a bounded fixed-argv `git log` (no repo = the section is simply absent); sections with nothing to say are OMITTED; one click copies for Slack/PR/standup; pure StandupReport + 6 tests, mutation ×2, walk-proven incl. the pbpaste round trip
- **v2.7.1**: the junior-RCP comments pass — measure-first found 638/638 public types already carry class javadoc (v1.202 held), so the gap was PACKAGES: 22 package-info neighborhood maps (what lives here / which RCP mechanism / reading order), 5 thin headers fattened, codebase-guide Flow 5 (Clock In — one click through every persistence law in five files), JuniorDocsGateTest keeping both properties as a build law
- **v2.7.0**: the senior Java pass — WorkspaceFilePulse promoted to core.util.FilePulse before a second copy grew; TaskBoard.Card de-telescoped 9→4 ctor args (optionals default, the enclosing class assigns); the Tasks window's own recorded v1 limit closed — live external-edit reload (FilePulse + tracker.isForeign, EDT apply, stop on hide), proven live <4s; ⌘I reaches epic labels + "blocked"
- **v2.6.0**: the time clock — Clock In/Out on cards ([start,end] session pairs, end 0 running; ONE clock board-wide, clock-in elsewhere closes the running session; <60s blips dropped whole); the overview's TIME report clips sessions per calendar day (midnight-spanning work splits) with running counted to now; header shows live elapsed via a label-only 30s ticker; live walk proved the clip ("today 2h 07m · week 4h 00m")
- **v2.5.0**: Board Overview tranche 2 — the blocker register (owner / unblock-action / auto-stamped since; blank action refused — a register row must be actionable; finishing IS the unblock), derived epic legend (labels in use, busiest first, hue = floorMod(hashCode,360) — SpotBugs killed abs(hashCode)), board-level retro notes saved with the board
- **v2.4.0**: Board Overview — a dashboard face on the Task Board (Overview toggle): WIP now (middle columns only), done today/week (calendar days), per-day flow bins, aging active cards, per-column stats — all from stated definitions in pure BoardStats; the walk caught bare `JComponent.getAccessibleContext()` returning NULL (painted widgets extend JPanel), pinned by OverviewRenderProbeTest
- **v2.3.0**: the keymap profiles — the platform ships five keymap profiles and every NMOX chord now rides all of them (Eclipse/Emacs/IDEA/NetBeans55 get the same Keymaps shadows and editor Keybindings as the NetBeans profile, cloned in each module's layer); KeymapProfileParityTest makes parity a build law; the QuickSearch actions were REGISTERED INSIDE the Keymaps folder (appearing as a bogus profile) — moved to the filesystem root; live Emacs walk (Ctrl+K kills, ⌥⌘3 survives)
- **v2.2.1**: the Libera walk — Ctrl+J was dead in the shipped app: a `WHEN_IN_FOCUSED_WINDOW` binding never fires inside a docked TopComponent (the v1.205.0 Escape law's second strike — the docked-TC key law); rebound on the component itself
- **v2.2.0**: the WeeChat-power tranche — the smart join/part/quit filter (activity-window per nick per channel, the WeeChat signature), Ctrl+J hotlist jump (most-urgent-first: highlights > messages, chronological within), the operator toolkit (op/deop/voice/kick/ban/unban with mode-aware enablement), /alias with persistence; recon-first: the client already carried most of WeeChat's feature set, so the tranche closed exactly the four signature gaps
- **v2.1.0**: the device gallery ships installed and active — six ready-made JSON devices exercising the whole v2 format bundled INTO the rack module (BundledDevices reads them from the classpath through the same judge, fit law, and trust gate as any drop-in), per David: examples "should not be a step — they should come installed and active"
- **v2.0.0**: **the programmable rack** — `~/.nmox/devices.d/*.json` becomes a real device on the shelf (DeviceFile the pure judge, JsonDeviceExtension a thin adapter onto the frozen SPI, UserDevices the drop-in reader, DeviceCatalog merging both extension kinds through one validate()); the host keeps every law (trust gate, colour law, port lexicon, shelf law) so the format can't express an ungated command; refusals are total and logged by filename; docs examples parsed by the build; mutation-proven ×3; the seventh and last drop-in seam, and the one that changes what the product is
- **v1.362.0**: the counts the docs quote are gated — DocsCountGateTest binds live-doc device/space/manifest counts to ground truth (historical logs deliberately out of scope); three stale counts fixed; the gate's first run rejected the docs edit that created it (hand-counted 64 manifests vs the array's real 60)
- **v1.361.0**: the documentation pass — the browser-to-source tutorial (the elevation arc's loop in one sitting, with a refusal table), README's source-aware Browser section, user-guide currency for the DOM tools and the toolchain-native lanes; every tutorial claim checked against DevToolsPanel before ship
- **v1.360.0**: the elevation walk (docs only) — the full source-aware loop click-proven in the installed 1.359.0: pick → h1#headline auto-selected + ringed, Open Source → index.html:9 (the exact producing line), Edit Style… → instant tomato repaint + "Saved to style.css (.tagline)" + gray→tomato replaced IN PLACE on disk, every other byte identical
- **v1.359.0**: the elevation-arc review — three fixes mutation-proven: the pick poll stopped on EVERY exit path (programmatic setSelected fires no ActionListener — the poll ran forever after a successful pick; pinned by a poll-count-freezes behavioral test through the fake-runner seam), duplicate selectors edit the LAST rule (the one the cascade applies), page-controlled selectors re-clipped Java-side (skipped, never truncated)
- **v1.358.0**: style write-back — Edit Style… on the picked element applies inline instantly, then lands the declaration in the SOURCE stylesheet (the page's own cascade answer picks the rule via el.matches over document.styleSheets; CSSOM-normalized selector match, replace-in-place or indent-matched insert, @media depth-tracked, whole-word properties); the refusal ladder: inline <style>, unserved sheets, compiled .css with a preprocessor sibling, unsaved editor buffers, structural characters — each with its reason while the preview stays
- **v1.357.0**: inspect-to-source — the Browser becomes source-aware: Pick element (one-shot capture-phase click listener records the child-index path; 300ms poll over the one-way bridge) + Open Source (PageSourceResolver: file:// + serving-registry origins only, loopback spellings unified, public/ docroot, canonical containment; HtmlSourceLocator: id-anchored or Nth-same-tag with comments/script/style bodies blanked line-true; refusals over guesses)
- **v1.356.0**: the toolchain walks (docs only) — deno/rust/go proven in the INSTALLED app, process-level and headless (spawn + handshake side-effects + orphan checks); ledger 83 opened (one LSP server per mime per project — measured, deferred with decompiled LSPBindings evidence)
- **v1.355.0**: docs truth — CLAUDE.md v1.344→v1.354 catch-up, README learning-space counts 88→90
- **v1.354.0**: the arc review over v1.349–v1.353 — three fixes mutation-proven: the JS-mime yields to deno lsp (the double-authority reborn one mime over), one bounded rootAbove (.git boundary + 8-level cap) shared by the LSP authority checks and Run Focused Test's runner selection, and kind-first lint resolution (a ROSETTA override owns PURITY too)
- **v1.353.0**: the senior DevOps pass — every generated Dockerfile built for real: go multi-package `-o` fix, SPA nginx try_files fallback (deep links 200, live-proven), rust BuildKit cache mounts (16s→1s, rebuilt binary run to prove it), base-image currency with a pinning test; the law: a Dockerfile that was never built is a claim, not a product
- **v1.352.0**: the Go pass — PURITY auto → go vet / golangci-lint (config opt-in), GLOSS gofmt with the exit-contract repair (gofmt -l exits 0 either way; the verdict reads the output), real go.mod learning space
- **v1.351.0**: the Rust premier pass — rustup-proxy honesty (probe caches SUCCESS only; failure re-probes so the one-click install self-heals without restart), cargo clippy/fmt lanes, focused-test truth (a no-match cargo test exits 0 — the fn must carry a test attribute)
- **v1.350.0**: the Deno pass — deno lsp becomes the TypeScript authority in deno.json workspaces via DenoInitOptionsInjector (the client's FIRST frame gains initializationOptions.enable=true — without it deno lsp publishes nothing), deno lint/fmt/test lanes, zero-config PREFLIGHT gate, learning space #90
- **v1.349.0**: the do-it-all Angular close — ledger 81 (rename single-authority: ngserver alone owns text/typescript in Angular workspaces because the platform rename collects edits from every binding — capability games proved useless, decompiled), ledger 78 (⌘B on identifiers), ledger 77 (resolver truth); the server→client frame filter rides a hand-rolled BytePipe (PipedInputStream pins reader-thread identity and dies with lsp4j's pool)
- **v1.326.0**: the Task Board walked again, and the menu underneath it — the v1.325 fixes confirmed live in the installed build; the find: `Popups.selectOnTrigger` never runs on a drag-enabled JList, so Edit…/Delete… were dead on any card not already left-clicked (proven by behaviour: an empty-space right-click failed to CLEAR the selection — the one thing only that listener does); fixed via `Popups.popupTargetList` hooking `getPopupLocation`, the popup gate taught both lawful forms; PLUS the v1.325.0 changelog entry restored (staged-content trap) and docs-landed gates moved to committed content
- **v1.325.0**: the Task Board arc review — three fixes in day-old code, mutation-proven: Delete Column ran OUTSIDE mutate() (no repaint, no save, stale header indices sent the second click at a DIFFERENT column; now gate-enforced — every board mutation must ride a mutate( line), keyboard moves kept their selection across the rebuild, and duplicate card ids (a checked-in board + a keep-both merge) stopped making one delete take two cards
- **v1.324.0**: the Task Board walk in the shipped app — chord/drag/rename/WIP-red all held and a planted `<html><img src>` title rendered as characters, never fetched; the Quick Search POPUP proved undriveable by this automation (a known-good control term returned empty too), so the ⌘I reach is pinned by a sink-seamed unit test rather than a fabricated fix
- **v1.323.0**: the Task Board — David's ask ("fully featured" kanban): ⌥⌘1, columns of cards with drag AND keyboard moves, advisory WIP limits (red header, never blocks), per-column menus, `.nmoxtasks.json` as the sixth per-project studio file with the whole studio-law family on day one (atomic writes, self-write discrimination, .bak-before-fallback, off-EDT IO, newest-wins, plain rendering, clicked-item popups, safe-default confirms, zero boot cost)
- **v1.322.0**: the debt sprint — ledger 66 closed (IRC logging survives a closed tab: engine-side IrcLogTap at client creation, 353-seeded membership, no double-log), ui/apiclient floors ratcheted, ledger 75's header finally says CLOSED; the ledger's own recorded deferral reasons did the prioritizing
- **v1.321.0**: the night-arc review — the gate law's mirror image: v1.320 shipped call sites with green SEAM tests and no gate (FreePorts/bannerPort tests passed while nothing referenced them at any spawn site), so wiring gates now pin BOTH halves; the checklist form: *every seam fix needs two proofs — the seam diverges, and the call site exists*
- **v1.320.0**: the learner walk of space #89 — the catalog's front door died on its FIRST click (the STATIC lane pinned `python3 -m http.server 8000`, the box held 8000, python refuses a busy port): core.util.FreePorts probes the first free port AND ServeUrls.bannerPort makes the announce read the port the server's banner NAMES — the probe alone would have put the ⇄ chip on a dead port; ten stale pinned-literal test assertions moved to shape+range in the same unit
- **v1.319.0**: docs truth — plan.md carries v1.316–v1.318, CLAUDE.md caught up fifteen releases (docs only)
- **v1.318.0**: the serving leg lives in the shipped app, and the Node wall speaks human — the Angular loop proven end to end in the INSTALLED build (Run → ng serve 0.8s → ⇄ chip → Browser auto-opens at http://[::1]:4200/ with the v1.259.0 loopback rewrite visibly at work → renders through the h2c-flagged FX 26 WebView → DevTools Angular pane reads the live tree, zero orphans after Stop); the wall it hit first became the grant: ng serve hard-refuses node below 20.19/22.12 while npm install sails through (npm doesn't enforce engines), so the CommandExecutor pump now translates the refusal shape Angular's CLI and Vite share (`requires … Node.js version`) once per run with the way out — the v1.264.0 EADDRINUSE idiom's second wall; EBADENGINE warnings deliberately not rescued; mutation-proven ×2 incl. a pump-WIRING gate (a predicate with green tests and no call site is a payload without a gate)
- **v1.317.0**: the Angular walk in the shipped app, and the one-line rule invisible for 300 releases — the framework bet's first end-to-end walk in an INSTALLED build (template → silent 21.2 install → all three switcher gestures → routes outline with the v1.316.0 fix observed live); the find hid one surface over: the product's OWN template stylesheet outlined as "No structure to show" because CSS_SEL demanded the brace END the line, so every single-line rule (`main { text-align: center; }`) was invisible since the outline shipped in v1.4.3; anchor removed, both writing styles outline identically, mutant restoring the anchor dies by name
- **v1.316.0**: the arc review over v1.309–v1.315 — probing the day's own fresh code found a nested `data: { path: '…' }` key on its OWN line listing as a phantom route (the single-line form was safe only because the chain matches once per line); fixed structurally with the inside-a-value-object guard; the conflict-marker gate and PlainTables verified holding
- **v1.315.0**: docs truth — plan.md carries v1.313–v1.314 (the css-prep mime recurrence, the two-signals narrowing rule, *fallbacks are the usual masker*, and why content assertions cannot see structural damage); standing header at v1.314.0 (docs only)
- **v1.314.0**: the outline sees your Angular routes — app.routes.ts outlined as NOTHING (probe-measured, the v1.292.0 Express finding in the framework the project bet on); routes now list with targets (eager component / lazy symbol / redirect), children nested, click landing on the path: line; the rule fires only where the file BOTH imports @angular/router AND annotates a Routes type (two signals that co-occur by construction beat one signal made stricter); PLUS the six-release conflict markers found in CHANGELOG.md and PlainTables.java — every docs gate reads substrings and a substring survives a marker above it, so the repo-wide ConflictMarkerGateTest now reads the SHAPE (it found the second instance the first time it ran); mutation-proven ×3
- **v1.313.0**: the Angular switcher covers the whole four-file set — Open Angular Styles / Open Angular Spec, and back from a stylesheet to its component; both decorator spellings (styleUrl/styleUrls, first entry wins), the ng new --style sibling fallback, a spec has no spec; the live walk caught styles→component registered on text/css alone while css-prep resolves .scss/.less to their OWN mimes (the v1.230.0 finding, second occurrence) — all three mimes now registered, gate-pinned; the off-EDT gate ties its count to the NUMBER of actions; mutation-proven ×2 (the first survivor re-proved *fallbacks are the usual masker*)
- **v1.312.0**: docs truth — plan.md carries the retroactive-sweep arc (v1.309–v1.311); standing header at v1.311.0 (docs only)
- **v1.311.0**: the renderer sweep the gate couldn't see — the v1.306 gate enumerated `new JTable`, so JLabel-based CELL renderers were never checked: API Studio's collections tree (imported names), its send history, and the Navigator outline (which escaped one branch and set RAW text in the other) all rendered external text as markup; fixed via PlainTables.plain at CONSTRUCTION; DB Studio's tree/saved-query renderers MEAN their markup and already escape — recorded verified-clean, not "fixed" (escape-or-disable depends on whether the renderer means its markup)
- **v1.310.0**: the oldest drop-in surface gets the path law — learn-catalog.d (v1.53.0) predated the traversal rule the newer five were born with; LearningSpace.create now resolves every sample path inside the target (resolveInside, canonical, a/../../b refused), the whole space disqualified on one bad path; laws need retroactive sweeps
- **v1.309.0**: the PWA kit persona walk (~290 releases after the kit shipped) — the service worker precached 2 of the 5 icons the kit writes, so an installed PWA offline showed broken home-screen and splash art, defeating the kit's whole promise; precache is now keyed off the same ICON_FILES constant write() uses; plus one shared KitFiles.shortName truncation (12 code points, surrogate-safe) for the manifest's short_name; method: for pure-builder surfaces, generate a real site and validate the ARTIFACT
- **v1.308.0**: docs truth — plan.md carries v1.304–v1.307 (the drop-in family complete + the html-render arc); standing header at v1.307.0 (docs only)
- **v1.307.0**: the IRC client can't be made to fetch a URL by a hostile server — nick list, channel tree, and topic label render server-chosen strings; all three route through PlainTables.plain at CONSTRUCTION (setting html.disable after setText is too late: BasicHTML installs the view on the text change — the ordering trap is test-pinned); mutation-proven ×2
- **v1.306.0**: tables show external text as text, everywhere — Swing's DefaultTableCellRenderer RENDERS a `<html>`-prefixed cell, so an `<img src>` in an API response header, DB cell, docker name, or probed tool output made the IDE's JVM fetch an attacker-chosen URL at paint time (the v1.208.0 DevTools class, standing open in every OTHER table); core.util.PlainTables is now the ONE place html.disable is spelled, and PlainTableGateTest fails the build on any production `new JTable` file that doesn't route through it or carry a written exemption
- **v1.305.0**: ~/.nmox/doctor.d probes join the Environment Doctor — the sixth and last named drop-in seam, completing the family (learn-catalog.d, templates.d, presets.d, api-library.d, dockerize.d, doctor.d); strictest validation of the six because a probe EXECUTES on Doctor open: tool names must be bare (no separators), args must be flag-shaped, malformed files skipped-with-note as " · yours" rows; mutation-proven
- **v1.304.0**: docs truth — plan.md carries the night shift (v1.300–v1.303); standing header at v1.303.0 (docs only)
- **v1.303.0**: the Doctor stops quoting error text as versions — the machine-setup persona walk (Environment Doctor's first) found cwebp's usage banner shown as "Usage:" and a CRASHING corepack pnpm shim's stack-trace path shown as its version, ✓ beside both; nonzero probes now read "found — but its version command failed (exit N)" (timeout sentinel −1 keeps an already-printed version — the wedged-pipe test caught the rule over-reaching first run), and cwebp joins the -version dialect holdouts; live-proven; mutation-proven ×2
- **v1.302.0**: the drop-in family review (v1.297–v1.301) — one find fixed: the Dockerize tab captured the recipe object at combo-refill time, so EDITING a drop-in and pressing Regenerate showed the old content; the selection now resolves by name against the fresh load (DockerRecipes.findByName), a deleted recipe falls back to the detected generator, behavioral mutation proof covers both cases; verified CLEAN: library scan on the housekeeping RP, menu completeness, first-show null-selection path; one blessing in writing
- **v1.301.0**: Dockerize speaks your house style — the fourth drop-in surface and the last named seam: ~/.nmox/dockerize.d recipes join the Dockerize tab's new combo beside the detected generator, previews swap with {{name}} substituted, Write writes what previews; ONE path-law implementation (UserTemplates.pathProblem, made public for its second consumer) and a NEW behaviorally-tested resolveInside guard in the writer — extracted structurally after a string-presence assertion let the guard mutant survive (the v1.290.0 class, fourth instance: guards belong in testable code, not private UI methods); live-proven; mutation-proven ×2
- **v1.300.0**: docs truth — plan.md carries the evening's tail (v1.295–v1.299: review finds, the request library, the ten-release update gauntlet, the floors ratchet and its windows-binding law); standing header at v1.299.0 (docs only)
- **v1.299.0**: the floors ratchet — seven JaCoCo minimums raised to measured-minus-margin (core .89, editor .67, project .87, apiclient .92, dbstudio .91, web3 .94, infra .85), locking in the persona-walk and extensibility-arc tests since v1.180.0; tools/ui unchanged (no honest margin) and rack held at .75 because the WINDOWS lane is its binding measurement (.76 there vs .78 on unix — POSIX-gated tests skip); no floor lowered, no exclusion changed
- **v1.298.0**: the update center crosses ten releases, gauntleted headlessly — a stock v1.288.0 install updated itself in-app to 1.297.0 via the real update center (updates=11, all modules; second refresh converges to zero; restart shows all eleven modules 1.297.0 Enabled, zero failure phrases; cluster jar manifests byte-verified) — the behavioral proof of the spec-version chain across the exact stretch the drop-in arc shipped; noted: a WRITABLE cluster installs in place instead of shadowing in the userdir (docs only)
- **v1.297.0**: the request library — the arc's third capture-once loop: .http/.rest files in ~/.nmox/api-library.d join API Studio's Import… menu ("· library"); .http DELIBERATELY over .nmoxapi.json (the workspace file is keychain-coupled; the dialect is the sharing format — auth absent on export, keychain-lifted on import, so the secrets law holds at both ends with zero new code); library items ride the ONE importHttpFrom seam by construction, the scan runs off-EDT, and the button disables until the menu shows (the v1.296.0 class prevented on day one); live-proven ("Imported 2 requests."); mutation-proven ×2
- **v1.296.0**: the extensibility-arc review (v1.292–v1.295) — two finds fixed: a Presets double-click stacked two popups (the off-EDT menu hop let each click post its own scan; the button now disables until the menu is up) and a verbatim .nmoxrack.json copy displayed as "Name.nmoxrack" (inner suffix stripped); verified CLEAN: the wizard's heterogeneous-selection casts, route-outline ordering, the shared single-threaded RP; blessed: Experiments stays built-ins-only, visible-row count cosmetic; both fixes mutation-proven
- **v1.295.0**: docs truth — plan.md carries the extensibility arc (v1.291–v1.294) and the direction it states: every shipped catalog gets a user-writable drop-in sibling (lazy read, skip-with-note, never the product's own trust) while gate-enforced content stays in code; candidate next seams recorded — API Studio collection drop-ins, Dockerize recipes, Doctor probes; standing header at v1.294.0 (docs only)
- **v1.294.0**: save a patch once, have it as a preset everywhere — the extensibility arc's second drop-in seam: any .json in ~/.nmox/presets.d joins the Presets menu ("· yours") and the format IS the Save Patch format, closing the capture-a-wiring-you-like loop; entries are name+file only — parse happens at APPLY time off the EDT through the Load Patch read path (corrupt fails with a message then; unknown devices degrade to v1.54.0 MISSING placeholders), the drop-in scan rides off the EDT (v1.33.1), and customs keep the built-ins' replace-confirm (v1.280.0); live-proven (Log Watch · yours applied its real TAIL → MONITOR wiring); mutation-proven ×2
- **v1.293.0**: your templates join the wizard — tech-debt item #1 ("templates live in code; should be extensible/data-driven") closed on its extensibility half: a JSON file in ~/.nmox/templates.d (name/description/files, {{name}} substitution — the product's own var spelling) appears in New Project beside the built-ins via the learn-catalog.d drop-in idiom (lazy read, filename order, malformed skipped-with-note); built-ins DELIBERATELY stay in code (their pins carry gate-enforced version ceilings); the path law refuses absolute/../backslash/drive paths by disqualifying the WHOLE template, generation re-checks resolved paths, and a custom template never gets the built-ins' pre-trust — the trust prompt survives and the optional install asks first (v1.224.0 spawn-ledger law), plus a package.json guard on every install; live-proven (listing, skip-note, substitution, trust prompt before install); mutation-proven ×3
- **v1.292.0**: the outline sees your routes — the editor persona walk opened server.js (the most-opened file in the ecosystem this IDE is built for) and the Navigator said "No structure to show"; the extractor was RIGHT (a headless probe proved it reads function/class and arrow consts fine) and useless, because an Express/Fastify/Koa file declares none of those — every handler is an inline callback, and the structure of that file IS its route table; app.get('/health', …) now outlines as `GET /health` with click-to-line, narrow by design (app-or-router receiver + real HTTP verb + string path, so app.use/res.json/cache.get stay out, comments and template literals ride the existing stripNonCode guard, and Express's own app.get('port') config getter is pinned as a WRITTEN limit); live-proven; mutation-proven ×2 — and the string-path mutant SURVIVED first because app.use is excluded by the VERB list, so no test exercised that guard until a no-quotes app.get(port) case was added (the divergence lesson, third instance)
- **v1.291.0**: docs truth — plan.md carries the night shift (v1.288–v1.290) and its standing header moves to v1.290.0: the organize sweep's last holdout and last creator, the aimed-discard review find, and the three lessons (mutation-divergence repeated one release after it was written → checklist not lessons file; hover behaviour is unverifiable by this automation — 3 MOUSE_MOVED events for a whole session, which is what ledger 76's four instrumentation rounds really established; two pipeline failures were the harness — awk on tab-separated gh output, and a full disk read as a bad cask artifact) (docs only)
- **v1.290.0**: the arc review (v1.287–v1.289) — one real bug in hours-old code: v1.289.0's Discard stopped the devices but left the studio AIMED at the deleted directory, and that is the common path (Discard sits beside Open); reproduced live — blank tree, orphan root, the deleted path still on the status line; an aimed discard now re-aims at ~/NMOX (the v1.33.1 known-good home) strictly AFTER the delete (aiming first lets the watchers and the patch autosave race it and re-create files under the tree being removed), quietly (no re-entry into the recents just cleaned) and guarded (dropping some OTHER space leaves the studio put); live-proven both ways; mutation-proven ×2 — and the second mutant SURVIVED first because the test checked the guard merely APPEARS while the panic block still carries one: it now counts guarded blocks (the v1.287.0 divergence lesson, second instance)
- **v1.289.0**: learning spaces can be discarded — File ▸ Learning Spaces… is the inverse of New Learning Space…, which had no inverse since v1.24.0: the ~/.nmox/learn dirs exist forever, so a language tried once sat in the Workbench's daily PROJECTS list for the life of the install (the v1.288.0 finding's cause), making spaces the organize sweep's LAST creator; Open aims the studio, Discard stops anything running there, deletes the tree and forgets the recents row; guarded like the Experiments manager — safe-default confirm (v1.98.0), the marker file as the contract so it can never become a general rm -rf, and a must-be-under-the-home check so a marker dropped elsewhere cannot authorize a delete; listing and tree walk both off the EDT; mutation-proven ×4
- **v1.288.0**: the Workbench forgets on request — the night walk found the home base's RECENT FILES and PROJECTS rows were the organize sweep's last holdout: learning spaces sat in the daily list for the life of the install (the ~/.nmox/learn dirs exist forever, so the prune could never touch them) and right-click offered nothing; each row now carries "Forget — the file/project stays on disk" (list-only, reopening re-records, the AIMED row exempt because it re-adds itself — a Forget there would be a lie), per-row menus so clicked-row-is-the-target holds by construction, children inherit the popup, the RecentFiles write precedes the refresh callback, and the project side rides a new ProjectAim.forgetRecentProject default method with the flush off the EDT; rest of the surface verified CLEAN live (v1.111 prune, v1.279 dedup); live-proven (elm row gone, dir intact); mutation-proven ×3
- **v1.287.0**: the night-arc review over the ten-release persona day (v1.278–v1.286) — two fixes: fit()'s truncation chopped UTF-16 units and could strand a lone high surrogate (the v1.149.0 cap class reintroduced by day-old code; now offsetByCodePoints), and the v1.286.0 template content files' two homes collapsed to one (the processor resolves from resources); the review's own lesson pinned in the test — the first surrogate mutation SURVIVED because under a uniform per-unit metric a dangling surrogate can never be the stopping point (the chop styles converge), so the test now uses a font-like metric where a lone surrogate is width 0 and the mutant diverges and dies by name; verified CLEAN: removeDevice disposes running devices, the ExplorerUtils activation lifecycle, the REFLEX filter placement; plan.md carries the full 2026-08-06 day addendum incl. the CI-recovery recipes
- **v1.286.0**: New File speaks JavaScript — the wizard on a JS project offered NO JavaScript file (ClientSide = Sass/LESS only, HTML/CSS under "Other"), and the tell was WebProjectRecommendedTemplates' privileged list naming Templates/ClientSide/javascript.js and three more paths NO shipped module ever registered, so the float-to-top feature silently pointed at ghosts since the class was written; the editor module now registers JavaScript/TypeScript/JSON File at exactly the promised paths (JS/TS empty by design, JSON starts as {} since an empty file is invalid JSON), the privileged list is rewritten to real paths incl. the platform's actual names (Templates/Other/CascadeStyleSheet.css, not css.css), and PrivilegedTemplatesExistTest fails the build on any privileged-but-unregistered ClientSide entry; live-proven (⌘N → the five types with loader icons → routes.js created); PLUS the ledger-69 trust sentinel's cleanup flush made best-effort after flaking twice in one day on fresh ubuntu runners (BackingStoreException from the finally block AFTER all assertions passed — no ~/.java/.userPrefs to lock); assertions never excused; mutation-proven ×2
- **v1.285.0**: the file tree's Cut/Copy/Delete actually work — every file's context menu has offered them since the v1.64.0 platform-tree rewrite and all three were permanently disabled: they are CallbackSystemActions that only enable when the activated component's ActionMap binds their keys via ExplorerUtils, and nothing ever bound them (220 releases of grey menu items; the v1.38.1 documented-but-never-exercised pattern, caught by the project-starter walk trying to delete a scratch file); FileTreePanel.installExplorerActions now binds copy/cut/paste/delete into the ActionMap Project Studio's default lookup exposes with activateActions on the window lifecycle, Delete keeps confirmDelete=true (the v1.98.0 safe default), and the aimed project ROOT refuses destroy/cut from its own tree; live-proven in the dev build both ways (scratch file deleted through the platform confirm; root's Cut/Delete grey); mutation-proven ×3
- **v1.284.0**: renaming a script onto an existing name is refused — the project-starter walk (a fresh Express API through the wizard) found Project Configuration's Scripts table let two rows share a name and Save's map-fold kept the LAST one: renaming test→dev destroyed the RUNNING dev script and the test script in one click, three scripts in, two out, no warning (the v1.268.0 tag-collision class, fourth home); Save now refuses by name before any write with blank rows exempt, and — same listener — commits an in-progress cell edit first, closing the half-typed-command-silently-dropped hole; mutation-proven ×3; the walk's happy path verified CLEAN live (wizard → silent npm install → pre-trusted Run → ⇄ serving → JSON in the in-app Browser)
- **v1.283.0**: the LCD tooltip registers in addNotify (devices are built off the EDT and ToolTipManager is a Swing singleton — the constructor was the wrong place), and ledger 76 records what live verification actually found: NO LcdDisplay tooltip has ever fired, in any version — RackButton's shows on the same hover, REFLEX's editable LCD has called setToolTipText since v1.0 and shows nothing, and a headless probe proves the method returns the right string, so the fault is dispatch (likely RackDevice's own registration plus its getToolTipText(MouseEvent) override) and needs a real bisect rather than another guess; the v1.282.0 truncation fix is live-proven and unaffected
- **v1.282.0**: an LCD that has to cut its text keeps the head and says it cut — VERITAS's `UNTRUSTED WORKSPACE — EXECUTION REFUSED` painted as `ED WORKSPACE — EXECUTION REFUSED` (seen live in the shipped 1.280.0): LcdDisplay's single-line paint dropped characters off the FRONT one at a time with no marker, so the word the message exists to say went first and the remainder read as content, while the multi-line branch trimmed from the END — two halves of one widget disagreeing about which end matters since v1.0; both now route through one fit helper (head kept, ellipsis appended, marker-not-a-stray-letter when nothing fits) and a truncated LCD carries the full text as its tooltip, closing the gap where assistive technology could read what sighted users could not; mutation-proven ×3
- **v1.281.0**: REFLEX stops firing for the IDE's own saves — the studios persist their workspaces beside the project (.nmoxrack.json and five siblings) and REFLEX's "code" filter includes json, so pressing Save Patch lit the EYE, pulsed the CHANGES meter, fired the CHANGED trigger and raised a Workspace Trust prompt for a click that touched no source (caught live on the very next click after the v1.280.0 confirm; API Studio and DB Studio save the same way on ordinary edits, so an armed REFLEX kept re-firing all session); new pure core.util.IdeWorkspaceFiles states the rule as the naming convention itself so a later studio is covered on day one, the drop happens BEFORE anything observable and an all-IDE batch returns without emitting, and IdeWorkspaceFilesTest reads every workspace filename out of five modules' sources so a differently-named one fails the build instead of a user's test suite; mutation-proven ×3
- **v1.280.0**: replacing the rack asks first — Presets and Load Patch both route through RackIO.fromJson, which CLEARS UNDO HISTORY by design (v1.50.0), and neither confirmed: clicking a preset on an unsaved pipeline destroyed it with ⌘Z powerless, the one irreversible gesture family the v1.98.0 safe-default sweep never reached; both now ask when the rack differs from its last persisted state (Enter = No, message says loading cannot be undone), with the baseline re-set on open/save/load/preset so the confirm never nags; mutation-proven ×2
- **v1.279.0**: the Workbench's OPEN FILES lists FILES, once each — opening a folder as a project leaves a folder-backed editor TopComponent, and the section rowed every opened TC with no folder guard and no path dedup, so the project directory listed once PER open (the neighbouring RECENT FILES already deduped, this one didn't); one `listable` predicate now decides, mutation-proven ×2
- **v1.278.0**: the Task Rack persona walk — aiming a project with NO .nmoxrack.json left the PREVIOUS project's devices mounted (autoLoadPatch had no else branch): the walk saw a REPL dialed to `elm repl` on a plain Node project, but the real damage was Save Patch writing project A's devices into project B's file and a RUNNING device keeping its process while every commandDir re-rooted to B; a patchless project now resets to the exact starter rack (shared loadDefaultRack, removeDevice disposes so running devices stop), three behavior tests on the real aim path, mutation-proven ×2
- **v1.277.0**: docs truth — plan.md carries the close-of-shift addendum for the twelve-release persona day (docs only)
- **v1.276.0**: the Browser-persona walk — the DevTools console was DEAD on every CSP-strict page (HN, GitHub): it wrapped input in window.eval, which a script-src without 'unsafe-eval' refuses, while the DOM/Network/Storage panes never noticed (their executeScript payloads are host-compiled top-level scripts, which unsafe-eval does not gate); the expression is now INLINED host-compiled with a statementScript fallback for non-expression input, mutation-proven ×2; HN reading/navigation, the iPhone preset reflow, and the DOM snapshot verified CLEAN live on FX 26
- **v1.275.0**: the day-tail review (v1.271–v1.274) — one drift hazard closed: the wire gesture's press and drop hit zones duplicated the nub tolerance inline; both now ride one nearNub predicate with the literal pinned to a single occurrence (re-inlining fails by name); WrapLayout edges, wireRefused semantics, and the Services-bridge noun fallback verified CLEAN
- **v1.274.0**: the Docker-persona walk — a real postgres container through the whole corpus callosum (panel engine/containers/logs, the v1.35.0 offer with the PUBLISHED port pre-filled, Test OK, SELECT version() in 5 ms) found ONE wrong noun: the connect status said "0 containers" (dbstudio's internal word for tables/collections) beside a connection sourced FROM a Docker container; DbEngine.containerNoun now speaks tables/collections/databases per engine, source-gated so the internal word never reaches the status line, mutation-proven ×2
- **v1.273.0**: ledger 75 closed — the narrow-width clip class, both instances: DB Studio's console toolbar wraps via new core.util.WrapLayout (FlowLayout wraps at layout time but LIES one-row in preferredLayoutSize — the exact lie that clipped the second row invisible) and the Infra property form tracks its viewport width so GridBag squeezes the editor column instead of growing a horizontal scrollbar; three behavior/gate tests, mutation-proven ×3
- **v1.272.0**: docs truth — plan.md carries the 2026-08-05 evening addendum (organize-sweep close, clicked-item-wins review + gate-scan lessons, the Infra walk + live proof, the verdict-grep law) (docs only)
- **v1.271.0**: the Infra Designer walk (the DevOps persona) — pressing the wire connector dot PANNED the canvas: the nub is painted centered on the node edge so half the dot lies outside the rect, and the press handler gated the nub test on inside-the-rect nodeAt (drop side identical: input-nub drops silently cancelled); the nub hit-test now owns the whole dot on both ends, illegal wires refuse OUT LOUD via Callbacks.wireRefused ("X doesn't wire into Y — a wire reads \"serves\""), and DB_*→DROPLET joined the rule table (the most common DO pairing was unrepresentable; planner-safe — provider loop reads only VPC/SSH_KEY, rules stay a DAG); FlowCanvasWireGestureTest drives the real listener headless, mutation-proven ×4; DEPLOY dry-run/destroy honesty verified exemplary; ledger 75 gains the property-panel clip as its second instance
- **v1.270.0**: the arc review (v1.266–v1.269) — every context menu in the product acted on the SELECTED item, not the CLICKED one (`setComponentPopupMenu` never moves the selection; every verb reads it): API Studio's tree Delete (confirm-less for requests, keychain-wiping) was silent data loss, NPM Explorer's Run Script spawned the wrong script, the address book copied/forgot the wrong row, history restored the wrong send; new core.util.Popups.selectOnTrigger (press+release trigger, empty-space clears) wired at all four sites, behavior-tested on real headless Swing incl. the JList closest-index trap, PopupTargetGateTest makes the pairing a build law, mutation-proven ×4; DBA-pass guards / v1.268 rename / v1.269 gestures verified CLEAN
- **v1.269.0**: the organize sweep closes its fourth surface — Contract Studio can forget: Remove Network… (Add Network… had no inverse since the network manager shipped, so a typo'd RPC URL lived forever and a SECRET network's keychain entry outlived every way to drop it — removal deletes the RpcSecrets entry off the EDT, LOCAL_ANVIL refuses before any dialog, safe default per v1.98.0) + Forget deployment in the Oversight address book (rows accumulated with no removal gesture; the dialog says the chain is untouched); both pure-Swing wirings pinned by OrganizeGesturesGateTest, mutation-proven ×4; the sweep (API v1.263.0 / DB v1.266.0 / Block v1.268.0 / Contract v1.269.0) now covers every studio persisting named user artifacts
- **v1.268.0**: Block Studio rename follows references — F2 on the root piece routed through the generic param editor, silently orphaning sibling Element references and allowing tag collisions (double customElements.define throws); BlockWorkspace.renameActive refuses invalid/taken tags with zero side effects, updates every sibling reference counted, and treats the rename as a patch boundary (v1.84.0); mutation-proven ×2
- **v1.267.0**: docs truth — plan.md carries the 2026-08-05 addendum + the persona-cadence method note (docs only)
- **v1.266.0**: the DBA pass — saved queries gain Manage… (rename/delete with safe defaults; rename collision refused), the console placeholder stops CONCATENATING into run text (the persisted `SELECT …;SELECT p.name` smoking gun), engine-aware Test wording; ledger 75 records the toolbar clip
- **v1.265.0**: the loading-guard law — the v1.263.0 data-loss class made structural: every raise of API Studio's `loading` flag must enter a try, every drop must live in a finally; LoadingGuardShapeTest fails a new bare raise by name
- **v1.264.0**: the beginner pass (David: "look through the eyes of a beginner, grant their wishes") — three grants from a fresh-userdir walk: the Vanilla Web template's pinned `-p 8080` made a new user's FIRST Run die in raw errno when the port was busy (http-server WITHOUT -p scans upward and prints what it bound — live-proven: busy 8080 → 8081 → ⇄ serving chip); EADDRINUSE now speaks human at CommandExecutor's pump where EVERY lane's output flows (the v1.11 humanizer only ever lived on the rack device's LCD, never the Run button) — live-proven: '↳ Port 8080 is already being used by another program…'; and learning space #89 "Your First Web Page" leads the catalog — 88 spaces existed and not one for someone who never wrote HTML (it started at Common Lisp). Mutation-proof lesson: `false && A || B` folds to B — a half-strength mutant survived redundant test inputs until javap exposed it; test each OR branch alone
- **v1.263.0**: renaming a request in API Studio WORKS — David's ask ("try the API studio with the Hacker News API") found that typing in the Name field threw IllegalStateException("Attempt to mutate in notification") on every keystroke: the listener's reload()+restoreSelection() re-entered the tree's selection listener → bindRequest → setText on the document being notified, so "HN top stories" became "N top storiesH" (first keystroke resets the caret to 0), the tree collapsed, and — worst — the aborted bindRequest left `loading` stuck true so the model stopped recording edits and persisted the name as just "H"; now one nodeChanged repaint (no selection event, no re-entry), method combo included; broken since API Studio itself (v1.19.0, 240+ releases). Live-proven against the real HN API: topstories 200/188ms/4KB, {{storyId}} item 200/38ms/1KB, Standards graded the live endpoint F crediting its real HSTS
- **v1.262.0**: ledger 63 closed — the deferral's condition (live repro of real proxy-log shapes) met: HPM 2.0.9 prints `[HPM] Proxy created: / -> http://localhost:3001` BEFORE the server banner (the hazard was real for the CRA-era stack) while HPM 4 / webpack-dev-server 5 print nothing; the general rule fell out of the corpus — no banner puts an arrow before its own URL, arrows point at DESTINATIONS — so firstLocalUrl skips arrow-preceded URLs and the real banner registers on a later line; one scan serves both consumers, mutation-proven, captured lines pinned verbatim
- **v1.261.0**: the update center crosses the runtime boundary, gauntleted headlessly — a stock v1.252.0 install (bundled Java 21.0.11) updated itself in-app to 1.260.0 via the real update center (11 modules digest-verified, originals in update/backup) and rebooted on the SAME Java 21 with all eleven 1.260.0 modules Enabled and zero module-system failure phrases: the behavioral proof of the target-stays-21 law across the exact boundary it protects; the gauntlet recipe is now fully scriptable (`--modules --update-all` + `-J-Dnetbeans.close=true` — without netbeans.close the CLI completes and the app sits in its GUI forever, a fake hang) (docs only)
- **v1.260.0**: the loopback+h2c sweep — the v1.175.0 consumer-2 question answered NO twice: java.net.http.HttpClient (API Studio/BEACON/HTTP console/CouchDB/JSON-RPC) ALSO dials only the first localhost address, and fixing that exposed the deeper twin — HttpClient defaults to HTTP/2, whose cleartext h2c upgrade esbuild accepts and never answers (bisected live: HTTP_2 timed out 5s, HTTP_1_1 answered in 54ms); LoopbackUrls promoted to core.http + NEW CleartextHttp.pinVersion (http→1.1, https untouched — ALPN h2 has no upgrade request), both wired at all six localhost-capable seams with source gates (un-wiring fails by name), JsonRpcClient's resolve inside the redacting try; remote-only clients deliberately out; live-proven: localhost:4321 went refused→hang→200/HTTP_1.1-in-153ms through the exact seam shape
- **v1.259.0**: the Browser speaks both loopbacks — the FX 26 Angular gauntlet found `http://localhost:4321` REFUSED in the in-app Browser while Chrome worked: Java resolves localhost to [127.0.0.1, ::1], the WebView loader dials only the FIRST address (no happy-eyeballs), and `ng serve` binds ONLY [::1] — the exact URL the CLI prints failed on the framework bet's front door; LoopbackUrls.resolve probes the stacks in the loader's own resolution order and rewrites only when the as-typed form would fail (nothing-answers stays as typed for an honest error; [::1]/127.0.0.1 both already local for save-to-reload, test-pinned), wired at the loadUrl choke point on a single-lane RP off the EDT; mutation-proven ×2, live-proven against real ng serve; PLUS the gauntlet itself green on the shipped Zulu 25 + FX 26 runtime — h2c fix holds against the real esbuild server, Angular 21.2.19 bootstraps, window.ng.getComponent answers, root component retrieved
- **v1.257.0**: the baseline-arc review (v1.250–v1.256) — one find, fixed: Re-run failed joined RAW failure names into the regex filters of jest/vitest/bun/node/go/forge, and real names carry metacharacters — measured live on node 22, the raw `applies discount (10%)` pattern SKIPPED the failing test of that exact name and reported PASS (a false green from the truth button); names are now backslash-escaped per metacharacter (Pattern.quote is Java-only \Q\E — wrong for JS/RE2/Rust regex engines), deno's --filter gains its /.../ regex form (bare substring 'a|b' could never match two names), pytest/cargo stay verbatim by design, both fixes mutation-proven; verified CLEAN: killTreeAndWait's rewrite, the runtime/packaging chain (already gauntleted in the shipped artifacts), the TAP tally anchors
- **v1.256.0**: the native-access grant — the live gauntlet on the shipped 1.255.0 app found the platform calling RESTRICTED METHODS on every boot (`com.sun.jna.Native` → `System::loadLibrary` from the classpath, plus javafx.graphics/javafx.web natives when the Browser opens), which JDK 25 warns about and a future JDK will BLOCK — the platform would stop booting and the Browser could no longer load WebKit; the base conf now grants `--enable-native-access=ALL-UNNAMED` (silent on Temurin 21 too, so the portable zip's BYO-Java range is untouched) while the NAMED javafx.graphics/javafx.web entries are written by whichever script INSTALLS the FX runtime (bundle-jre.sh + the windows lane) — naming an absent module prints "Unknown module" on every launch of the FX-less portable zip. Both rewrites EXTEND the single default_options line (the Windows .exe GREPS this file instead of sourcing it, so two assignments have no defined winner) and are ANCHORED to it — verification caught the first sed rewriting the explanatory comment too, shipping prose that contradicted its own code. Proven live: zero warnings with the flags, four without; 4 mutation proofs. **Timing law: the conf ships only in installers, never through the update center, so an install without the grant can never receive it later**
- **v1.255.0**: the jlinking lanes move to Zulu — v1.254.0 ALSO built zero assets, one layer deeper: Temurin 25 ships NEITHER jmods NOR a linkable run-time image, so jlink refuses outright ("This JDK does not contain packaged modules") and that distro simply cannot build a bundled runtime; windows was never immune (its one pass was an older Temurin patch with jmods). All three release jobs + the windows-installer check now use Zulu 25 (all 70 jmods); build-and-test stays on Temurin to prove the product still builds on the distro most users have; the JEP 493 branch stays as the safety net. Verified against a REAL Zulu 25.0.4 download: 78 modules, javafx.web@26.0.2, java.compiler present
- **v1.254.0**: the JDK 25 runtime lanes fixed — v1.253.0 built ZERO assets because linux+macos died on "need a JDK 21+ with jmods": a JDK's jmods dir is OPTIONAL since JDK 24 (JEP 493 linkable run-time images) and Temurin 25 on those runners ships none, while windows still does (hence exactly two of three lanes); bundle-jre.sh + the windows lane now add the JDK module path only when it exists, and the platform module list comes from `java --list-modules` rather than a hand-picked subset (which measured 39 modules vs the jmods build's 76, silently dropping java.compiler/jdk.jdi/jdk.attach) — proven end-to-end against a jmods-less JDK 25: 78 modules, javafx.web@26.0.2, parity confirmed
- **v1.253.0**: **the baseline advances** (David's call) — the bundled runtime is JDK 25 LTS and the Browser's engine is OpenJFX 26.0.2, all pins moved in lockstep (CI + 3 jlink jobs + windows check on 25; four fresh platform sha256s; ui pom + NOTICE via the four-home gate); `mvn verify` on JDK 25 is GREEN across all ten modules incl. SpotBugs/find-sec-bugs/JaCoCo (the dossier's biggest unknown), a workflow-identical full-dir jlink builds clean, the app boots zero-SEVERE, and the browser gauntlet passes on FX 26's WebKit (https, plain-http through the h2c-flagged loader, DevTools DOM). **maven.compiler.target STAYS 21**: the update center ships modules not runtimes, so class-69 bytecode would brick an in-app update on an install still running bundled Java 21 — law recorded at the property. Ledger 74 closed
- **v1.252.0**: VERITAS reads node's own test runner — a `node --test` project showed P:0 F:0 with no failure names (the tally knew jest/vitest/pytest/cargo/go/forge but not TAP, and node speaks TAP: `# pass 3`/`# fail 1`, `not ok 4 - name`), so the most dependency-free suite in the Node ecosystem was the one the harness couldn't read; both parse now through one tested tallyFrom seam, Re-run failed speaks `--test-name-pattern`, `node` joined the RUNNER knob per the append-only law, and the whole chain is live-proven (P:3 F:1, the failing name in the dialog, Re-run armed) — found by the QA persona on a purpose-built failing suite
- **v1.251.0**: the first CONTRIBUTOR persona + day docs truth — a fresh clone built the assembled app with CONTRIBUTING's verbatim command in 21s, zero friction; the only finds were CLAUDE.md's fossil JDK-17 Troubleshooting claims (corrected to the Java 21 baseline); plan.md carries the day-shift addendum (docs only)
- **v1.250.0**: the bundled-JDK decision gets its measured dossier (docs only) — JDK 25 LTS + FX 26 probed live on this box: jlink green, the app boots with zero SEVERE (the --add-opens set holds on 25), the Browser renders on FX 26's WebKit, and the v1.226.0 h2c flag survives in FX 26's javafx.web; docs/engineering/jdk25-fx26-dossier.md lists the honest GO-remainder (CI matrix on 25, win/linux probes, fresh sha pins, full gauntlet) — the decision is David's, the numbers are in
- **v1.249.0**: the day-arc review (v1.241–v1.248) — one find, fixed: the bundled OpenJFX runtime was the ONLY unattributed vendored component (jlinked into all five installers since v1.199.0, absent from NOTICE); attribution added and FxPinLockstepTest binds the FX version's four homes (ui pom ×2, bundle-jre.sh, release.yml, NOTICE) into build-failing lockstep, mutation-proven; verified CLEAN: the v1.246.0 wizard deletion is complete (dirs/iterator/dependabot entries/docs), the org.openjfx ignore survived with its reason, the v1.247.0 ceiling port is real
- **v1.248.0**: the browser engine catches up — OpenJFX 21.0.5 → 21.0.12 in full lockstep (ui pom ×2 + windows workflow pin + bundle-jre.sh's three platform sha256s), gauntleted live on the new WebKit (https render, plain-http through the h2c-flagged loader, DevTools bridge reading the DOM); ledger 74 now carries MEASURED FX/JDK floors (FX 24=JDK 22, FX 25=JDK 23, FX 26=JDK 24 — read from the jmods' class versions), so every FX major past 21 is chained to a bundled-JDK baseline decision and the FX 21 LTS line is what moves on Java 21
- **v1.24.0**: Learning Spaces — a real in-rack REPL (InteractiveProcess engine + REPL device), New Learning Space… launcher, data-driven 51-space catalog (learn-catalog.json) across languages/frameworks/libraries; spaces live under ~/.nmox/learn
- **v1.25.0**: the daily-driver polish — Welcome rebuilt as a launchpad (start/recent/tooling columns, stamped-version footer), Environment Doctor (Tools menu; live-probes ~32 external tools with versions/install hints), daily update check (quiet, opt-out, dev builds skip)
- **v1.25.1**: cleanup — REPL interpreters that buffer piped stdin gained their force-interactive flags (python3 -i -q, node -i, lua/julia -i, ts-node -i), Doctor probes go with `go version`, first ui test, docs truth pass
- **v1.26.0**: the complete-system release — the whole deferred debt ledger worked end-to-end: rack undo/redo (⌘Z), Quick Search into API Studio requests + infra nodes, multi-provider cloud sync (DO/Hetzner/Cloudflare with per-provider failure isolation), TAIL/TEMPO resurrection, 8 more Navigator outlines (43 mimes), Options update-check toggle, SOLDER→CI export; the refactor items re-audited with fresh evidence (#7 startup measured at 7s cold, palette not the bottleneck; #1/#2 firmed won't-fix — repeated idiom, not repeated values)
- **v1.27.0**: the coverage release — ~320 new unit tests across all eight code modules; JaCoCo coverage now measured on the *testable surface* (pure-Swing windows/dialogs/canvases excluded at the root pom, each named with a reason); floors raised on six modules and added to project + ui (all eight now gated); the extra tests surfaced and fixed a real latent bug — WorkspaceTrust stored trusted paths as one joined preference value that would overflow java.util.prefs' 8 KB cap and break trust on a long-lived install, now one entry per path with legacy migration + a 200-path regression test
- **v1.28.0**: the LAMP/LEMP release — PHP made a first-class citizen: ARTISAN Laravel console device (41st device; serve/test/migrate/fresh/queue/routes, composer.lock version currency, READY/URL/SERVING wiring), TYPEGUARD→phpstan + GLOSS→Pint PHP AUTO lanes, IGNITION docroot-aware `php -S`, Apache config grammar (.htaccess/httpd.conf/apache2.conf/.vhost; generic .conf deliberately unclaimed), PHPUnit in Run Focused Test (both test-method shapes), "PHP Web (LEMP)" template (guarded front controller/cli-server router, phpunit+phpstan+pint wired, nginx+php-fpm+MariaDB compose, LEMP cloud-init for droplets) + LAMP Bench preset, Dockerize PHP (composer:2 → php:8.3-fpm-alpine + nginx sidecar), Doctor probes composer/mysql/nginx/apachectl, Database Explorer (already shipping, 10 db modules) surfaced in docs; two template bugs fixed in passing (nested dockerize paths, README heading indentation); MySQL learning space skipped with reason (REPL model needs a live server)
- **v1.29.0**: DB Studio — a new dbstudio module: database management tab (⇧⌘7) for SQLite/PostgreSQL/MySQL/MariaDB/MongoDB/CouchDB with bundled drivers (MariaDB Connector/J LGPL serves MySQL too; PG BSD-2; SQLite+Mongo Apache-2; Couch driverless over the shared HttpClient), DbBackend abstraction (JDBC + document backends), Keyring-only passwords + .nmoxdb.json specs, SqlSplitter/DocumentGrid pure cores, kind-aware console (SQL kit / JSON), per-statement result grids + history + real cancel, ⌘I search into connections/tables, 150 module tests (real SQLite e2e in CI) + 0.73 measured floor; plus the discovery layout — all six suite tabs (Workbench→Rack→DB→Infra→API→Docker) open by default on first launch, boot smoke 6s unchanged
- **v1.23.0**: the completeness release — every half-shipped idea finished: Experiments manager (promote/discard UI), Hetzner drift/destroy/cloud-init + any-provider live deploys, VITALS gates all four Lighthouse categories, Ship Gate + Dev Intelligence presets, BEAM outline families, wizard validation, window shortcuts (API Studio ⇧⌘8, Infra ⇧⌘9)
- **v1.29.x**: DB Studio arc — v1.29.0 the suite (dbstudio module: 5+ engines with bundled drivers, Keyring passwords, kind-aware console, ⇧⌘7, all suite tabs open by default), v1.29.1 the live click-through polish (peek-on-double-click, balloon feedback, honest RUN gating, first-run guidance, save-style SQLite chooser), v1.29.2 dev-launch branding (APP_DOCK_NAME conf, Welcome TOOLING gains DB Studio)
- **v1.30.0**: the Services bridge — NetBeans Database Explorer connections (ConnectionManager) appear in DB Studio's tree and run in its console by wrapping NB's live java.sql.Connection (any registered driver incl. Derby/Oracle; FETCH FIRST peeks; NB owns credentials/lifecycle); JdbcCore extracted from DbClient (one proven statement loop, headless test through the real NB API); dev ⌘Tab/dock icon fixed (committed nmoxstudio.icns copied to target by the application build)
- **v1.30.1-v1.30.2**: NPM Explorer lists global packages (`npm ls -g`) when no project is aimed; `--app-only` bundle build closes the last dev-launch branding gap (⌘Tab switcher label)
- **v1.31.0**: REPL grows up — ENGINE knob (26 engines derived from the learning catalog, force-interactive commands + HINTS + per-OS install seeded; CUSTOM on manual edit), INSTALL button (missing interpreters install from the rack, streamed onto the REPL screen), learning-space picker probes interpreter availability before creating anything
- **v1.32.0**: DB Studio 2, the working-DBA sprint — in-grid row editing (EditGate single-table/PK gating with honest read-only reasons, EditSession dirty tracking, UpdateBuilder dialect-quoted UPDATEs with status-bar-ready refusals, Apply-with-SQL-preview then re-run-for-truth), CSV/JSON export on every grid, persistent history (cap 50) + saved queries in .nmoxdb.json, .env connection offers (DB_*/DATABASE_URL; password → Keyring only), EXPLAIN button (engine-native plan syntax); SqlDialect extracted as the one quoting seam (doubles embedded quotes); ReDoS finding fixed by idiom not exclusion; debt #9 closed; dbstudio 317 tests, 87.1% line coverage
- **v1.33.0**: the Web3 release — Contract Studio (new web3 module, ⇧⌘6, suite-tab default-open): Foundry/Hardhat artifact tree, ABI-driven Interact (CALL decoded returns, SEND receipt polling, revert/custom-error decode), Watch (2s block poller + Keccak-topic-matched event decode), Oversight (gas report, EIP-170 verdicts, address book); no-private-keys boundary test-pinned (devnet unlocked accounts only, secret RPC URLs Keyring-only, URL redaction); vector-pinned cores (hand-rolled Keccak-256 w/ OpenSSL-validated multi-block vectors, ABI codec on the Solidity spec examples, regex-free gas parser); Solidity editor citizenship (grammar 0.0.187 pin, outline, LSP entry, keywords, spellcheck); foundry.toml toolchain + forge lanes; ANVIL (42nd) + GOVERNOR (43rd) devices + Web3 Bench preset; solidity/chisel learning space (52nd); 7 Doctor probes; web3 245 tests @ 89.5% (floor 0.80), rack 585
- **v1.33.1**: fresh-launch fix — the rack no longer aims at `$HOME` on first run (Project Studio's tree + Project Explorer's detection walked it on the EDT, hitting macOS TCC-protected `~/Desktop`/`~/Downloads`/`~/Pictures` and firing a permission-prompt storm that also froze first paint); fresh launches aim at a created-on-first-run `~/NMOX` workspace (recent/resurrected project still wins), and every startup filesystem walk moved off the EDT (lazy FileTreePanel children, detectKinds on a RequestProcessor, + Docker panel/ROSETTA/package-manager handlers found by audit); rack 593 tests, project 14
- **v1.33.2**: fixed a fresh-launch startup hang that v1.33.1 unmasked — moving startup scans off the EDT exposed a self-sustaining event storm (ProjectExplorer ran a full refresh on every PROP_OPENED/PROP_ACTIVATED across all 10 default-open suite tabs, each fanning out per-row async detection; previously masked because the old synchronous scan blocked on $HOME File.list before a 2nd iteration); fix = RefreshCoalescer (burst → one refresh, 500→1 test-pinned) + idempotent row updates; also InfraGraph.clear() no longer fires on an empty graph (was writing a stray .nmoxinfra.json into fresh ~/NMOX every launch); rack 594 / project 17 / infra 166 tests
- **v1.33.3**: the last startup storm — FlowCanvas.fit()/selectNode() busy-re-posted invokeLater(self) while the canvas was 0×0 (Infra tab open-but-unselected on fresh launch → infinite loop, EDT pinned, first paint starved; pre-existing, unmasked like its v1.33.2 siblings); fix = deferUntilSized (arm ONE resize listener, run body once; regression tests proven to fail on the old code); repo-wide audit of all 116 invokeLater sites + paint/layout + listener paths found no more loops; live-verified fresh boot (window paints, EDT idle ~4%, ~/NMOX aim, zero prompts); infra 170 tests. Lesson pinned in memory: locked-screen sessions map no windows — check lock state before calling a clean boot hung
- **v1.33.4**: click-surface branding on both desktop OSes — macOS: build-dmg.sh ships the icns under BOTH names (nmox-studio.icns for Info.plist + nmoxstudio.icns for the launcher's -Xdock:icon, whose dangling path was overriding bundle attribution → default Java icon); Windows: BrandingArtGenerator emits a committed multi-res nmox-studio.ico, Inno Setup brands the setup exe + both shortcuts, release workflow rcedit-patches every launcher exe, new windows-installer-check workflow byte-verifies it all on windows-latest
- **v1.34.0**: the classic web release — script-tag sites open as STATIC projects (last-resort kind, suppressed by any real manifest; IGNITION serves them with READY/URL); bower.json/Gruntfile/gulpfile/webpack.config are manifests (30 total); FORGE config-file lanes + grunt/gulp knob positions; CRATE bower lane; DYNAMO device (44th — static-parses Gruntfile/gulpfile tasks onto a knob, GO runs via npx); LegacyWeb detection with jQuery 1.x/2.x EOL chips; Classic Kit wizard (vendored pinned jQuery 3.7.1/MooTools/Prototype/Backbone+Underscore/Knockout with sha256 NOTICE, or npm deps; webpack/grunt/gulp/bower generators, never-clobber .suggested siblings; HtmlWiring seam shared with PWA Kit); Classic Web (jQuery) template; CoffeeScript citizenship (48 grammars, 45 outline mimes); classic-library-aware completion (295 API entries, manifest-mtime-cached detector, dotted matcher); Doctor probes webpack/grunt/gulp/bower/coffee; rack 671/editor 341/tools 92/ui 39
- **v1.35.0**: the connections release — ServingRegistry (7 serve devices announce URLs; status-line ⇄ chip, ⌘I Live Servers, VITALS/BEACON auto-target, API Studio {{baseUrl}} offer, Contract Studio ANVIL auto-connect/grey) + ManifestPulse (16 manifest names; NPM-9000/DYNAMO/CRATE/HALO/ARTISAN re-sync on save, coalesced 10-writes→1, .env → status note + studio hooks); Contract Studio ArtifactPulse (out/ is in FileWatcher's skip-dirs — purpose-built poller), DB Studio Docker connection offers (DockerDbOffers.plan: image-name then port inference, cap 2/pass, once per container/session), all four .nmox*.json files external-edit reload with self-write-stamp discrimination + never-clobber dirty guards (infra holds its debounced save on conflict), "Open manifest" device context jumps, ServeUrls/scripts() dedup; every listener bounded+equality-guarded+storm-tested; rack 714/dbstudio 349/web3 271/infra 181/apiclient 91 (+133)
- **v1.35.1**: the finishing pass — PostgreSQL JDBC 42.7.5→42.7.11 (clears both HIGH Dependabot advisories); REPL INSTALL runs compound catalog commands via /bin/sh -lc when shell operators present (foundryup pipe+&&, PyTorch trailing-# entries; SOLDER deliberately keeps its no-shell stance for user-typed commands); php -S lane registers with the ServingRegistry; API Studio follows mid-session re-aims (force-save old → load new → re-point pulse; 100-event storm → 1 load, A→B→A bounce → 0); DB Studio Docker offers wait for tab visibility (probe on componentShowing, guard consumed only on displayed offers — the v1.35.0 live-demo finding); SelfWriteTracker promoted to core (web3+apiclient dedup); core 13/rack 719/dbstudio 353/apiclient 94/web3 268
- **v1.36.0**: the senior review release — six read-only audit lenses (platform architecture, device/process lifecycle, listener symmetry, timers, EDT+process hygiene, API quality) then fixes for everything proved, blessings-in-writing for the deliberate-but-odd; the house laws HELD (orphan guarantee, storm laws, Keyring boundaries, v1.35 listener symmetry). Fixed: Infra Designer's inverted listener lifecycle (close/reopen killed auto-save + cross-project write vector), DockerClient's fictional timeout (wedged daemon bricked Docker silently; regression test fails on old code), the EDT mutation half (New Project's 4 git spawns, Experiments discard/promote, file-tree recursive delete, 5s session-snapshot writes), corrupt workspace .bak-before-fallback ×4 studios, ghost-serving/disposed-device races (flag cleared on undo re-attach), JS context-menu layer collision + dup Editors merge + 4 ⌘I position collisions, last JOptionPane + all 15 printStackTrace downgrades, cloud tokens prefs→Keyring w/ delete-only-after-save migration. New core ProcessSupport.runBounded (waitFor-first, drains on threads) adopted at 5 sites. Deleted: tools.build service, CodeIndexService, sample module from product, dead deployment profile, ui wildcard export (net −3,000 lines). Ledger items 15–24 recorded with reasons. core 18/rack 734/tools 59/project 18/ui 39/editor 336/infra 193/apiclient 97/dbstudio 355/web3 271 = 2,120
- **v1.37.0**: the debugging release — JavaScript/TypeScript breakpoint debugging out of the box: vendored `js-debug` v1.117.0 (MIT, 2.5MB, nbmResources → InstalledFileLocator) + `DapProxy`, a DAP session multiplexer that answers js-debug's `startDebugging` reverse request, dials the child session, replays breakpoints and splices it in — the platform's stream-based DAP client can't dial a second socket, so without it the debuggee pauses forever (verified live before a line was written). `RealJsDebugIntegrationTest` drives the real adapter end-to-end and pins the transcript. `ProcessSupport.killTree` made public (descendants first: the debuggee is the adapter's child). Three bugs fixed, each with a test proven to fail on the old code — two found by the E2E click-through, one by ubuntu CI: **the proxy slammed the client's socket on `terminated`** (close() discarded bytes still unread in the peer's receive buffer, so a finished session could stay "live"; macOS's reader usually won the race, Linux never does — now a half-close, FIN queued behind the frames; reproduced in a Linux container, mutation-proven); **debugging bypassed Workspace Trust** (all three languages ran project code with no prompt — now gated, "Keep Safe" blocks before any spawn) and **IGNITION's static lane served silently** (python block-buffers its "Serving HTTP on" banner to stdout, so READY/URL/serving-chip/⌘I never fired; `python3 -u`; the old test injected the banner into onLine() and passed while reality failed). Ledger items 25–26: one child session per debug run (children run undebugged); the vendored adapter is invisible to Dependabot/SBOM (manual bumps, sha256 in NOTICE). core 19/rack 735/tools 59/project 18/ui 39/editor 355/infra 193/apiclient 97/dbstudio 355/web3 271 = 2,141
- **v1.38.0**: the startup-truth release — measured first (startup-log phases + JFR across cold/warm/dev-tree/installed boots), then fixed what the profile named. The v1.26-era "7s cold start" no longer reproduces (the v1.33.x storm fixes removed it): window paints in 1.4s (warm OS cache) to 2.7s (first boot), ~90% of it the platform module system scanning 519 cluster jars — the deliberate feature-set price. The real waste was hidden default-open tabs working at boot: **NPM Explorer spawned `npm ls -g` twice per boot** (the only boot-time processes in the whole IDE, per JFR), Contract Studio walked the artifact tree, the Docker panel ran its dockerize detect walk, and Project Explorer/Project Studio double-fired their scans (ctor + componentOpened). All gated on componentShowing (the DB Studio v1.35.1 idiom) or deduped; rack re-aims and build pulses behind hidden tabs coalesce to one deferred pass on first show. Pinned by source-gate tests (NPM one proven to fail on old code); live-verified — fresh boot = 0 ProcessStart events, each tab serves its deferred work on first click. Audit blessed the rest: no default-open tab hits the network at boot. plan.md's perf item struck with fresh numbers. core 19/rack 737/tools 61/project 19/ui 39/editor 355/infra 193/apiclient 97/dbstudio 355/web3 272 = 2,147
- **v1.38.1**: the DX pass — four senior-developer journeys walked E2E in the real app. The engines held (v1.37.0 debugging stopped a live HTTP request mid-flight in a real Node service; trust prompt named the right project root; Stop left zero orphans), and the bugs were all one keypress off the feature path: **four of seven advertised shortcuts opened the wrong window** (⌘0→Editor, ⇧⌘6→Tasks, ⇧⌘7→Properties, ⇧⌘8→Palette, and ⇧⌘O→Open Project, because Keymaps-profile registrations beat `Shortcuts/`-folder ones — studios moved to the unclaimed ⌥⌘6–9 / ⌥⌘0 / ⌥⌘O family, WindowShortcutsTest pins chords + Welcome labels, mutation-proven), **a debug session's Output never surfaced** (the debuggee's console existed but nothing opened it), **Open Folder started in $HOME** (now the ~/NMOX workspace). Ledger 27–28 record what we won't fix yet: the platform's Breakpoints window never lists DAP breakpoints (reproduced with Python, none of our code — the gutter is the manager, user guide says so) and Window-menu items show no accelerator. plan.md gains the failure pattern: *a UI affordance documented but never exercised is untested*. ui 42/dbstudio 355/web3 272/apiclient 97/infra 193/project 19/editor 355
- **v1.39.0**: the idiom release — five senior-RCP lenses (Lookup/services/actions, window system, FileSystems/DataObjects, threading/utilities, module wiring) audited the whole codebase; twelve cheap-and-right fixes shipped: **file CRUD through DataObject** (open editors followed deletes/renames for the first time; rename via locked FileObject.rename because DataObject.rename's extension handling is loader-dependent — proven by a failing test), **atomic workspace writes ×5** (core AtomicFiles: temp sibling + ATOMIC_MOVE; kills the torn-read/false-foreign race SelfWriteTracker existed to prevent), **Rack's per-instance shutdown-hook leak** → one static reaper over a live-set (RackReaperTest), **Docker Panel PERSISTENCE_NEVER+openAtStartup contradiction** (the one tab that ignored being closed), **ctor→componentOpened workspace loads ×3** (double-load fixed; DB Studio's open-time Docker probe caught trying to spawn behind a hidden tab and gated on isShowing — zero-boot-spawns held), **apiclient raw threads → RequestProcessor("API Studio")**, **NpmService off ForkJoinPool.commonPool + getInstance→getDefault**, **DockerClient @ServiceProvider**, **normalizeFile ×2** (dead hyperlinks on relative paths), **Utilities.isMac/isWindows ×3** (BaseUtilities in core — no util-ui dep). Ledger 29–36: rack-as-context-system (identity — migrate as its own release or never), LinkageError soft-deps, friend-less rack exports (tripwire: before any plugin SDK), DiagnosticsBus vs HintsController, studios in editor mode, ProgressHandle gaps, no @OnStop seam, FileTreePanel UI half. Blessed in writing: polling over FileObject listeners, RackBus, hooks-over-@OnStop for reaping, WorkspaceTrust global prefs (now documented in-class), modeless JDialog viewers. core 22/rack 743/tools 62/project 19/ui 42/editor 355/infra 194/apiclient 100/dbstudio 356/web3 274
- **v1.40.0**: the git surface — recon first (the ide cluster already ships 12 git modules; the gap was OUR chrome): status-line branch chip (GitFacts pure core: HEAD parse incl. gitdir indirection + detached heads, porcelain counting; GitChip model w/ boot-guard state machine, source-gate tested — branch is a file read, the git spawn fires only on aim/click/visible-30s-tick), one-click History via the git module exported API (reflection over the friend-restricted package, degrades to a status message). The chip menu ships ONLY what works selection-free: the platform Show Changes/Diff/Annotate are NodeActions reading the global selection no NMOX window publishes — three invocation strategies tried live (context lookup, node delegate, setActivatedNodes assist), all refused, so ledger 29 (the context-system migration) gains its first concrete customer and the Team menu keeps those verbs. Live E2E: real repo, chip main +-1, History shows real commits; API quirk pinned in a comment (openSearchHistory String arg is a commit-ish, not a path). core 31/rack 751 (+GitFacts 9, GitChip 8)
- **v1.41.0**: the accessibility release — the widget library (Knob/RackButton/ToggleSwitch/Led/LcdDisplay/VuMeter) implements Swing accessibility: roles (SLIDER/PUSH_BUTTON/TOGGLE_BUTTON/LABEL/PROGRESS_BAR), AccessibleValue on knobs+meters, state/text announcements (EDT-marshaled, guarded so zero cost without assistive tech), keyboard operation (arrows/Home/End on knobs through the same listeners as mouse drags; Space/Enter on buttons incl. dimmed-refusal parity), FOCUS_RING in RackStyle (color law reserves GO/STOP/MUTATE/QUERY for meaning). DeviceContractTest gained the name law — every control on every faceplate exposes a non-blank accessible name — which failed-first on 59 controls across 40 devices (1 CommandDevice fix covered ~26; 30 device-side names; LCD label-less class backstopped via param keys). Found+fixed: RackTopComponent's global Tab-swallow (rear flip) ate focus traversal — now yields when a control has focus. A11yContractTest 19 tests; rack 785 (+63)
- **v1.42.0**: the Windows lane — windows-latest joins the CI matrix as a blocking full-verify gate (tests+SpotBugs+floors+audit; green in 7m24s; probes stay POSIX, ledger 37). Getting green = 5 fixture fixes (canonical paths vs Git Bash /tmp mounts, CRLF folding in DeviceDocsTest, per-OS INSTALL expectation, two @TempDir cwd-lock cleanups incl. the js-debug E2E — which passes on Windows — and a real DB Services connection leak) + 3 product fixes with regression tests: **LanguageServerCatalog never detected any server on Windows** (canExecute on bare names; now probes .exe/.cmd like ToolLocator), **DapProxy disconnect race** (stop mid child-handshake left the debuggee alive on every OS — gate on childSocket, test freezes the dance), **ProcessSupport.killTreeAndWait** (destroyForcibly is async; Windows holds file locks until death — JsDebugServer.stop waits bounded). ONE disable with runner-proven evidence: Git Bash breaks the Windows PID chain, MSYS grandchildren invisible to descendants() (ledger 38: Job Objects). plan.md gap #1 closed + new @TempDir-process failure pattern
- **v1.43.0**: browser debugging — the v1.37.0 adapter's second face: "Debug in Chrome (breakpoints)" on text/html + JS/TS (popup 8100 behind Debug File), same WorkspaceTrust gate before any spawn. Recon-first (four preserved transcripts of the real adapter + headless Chrome): simple pages fire startDebugging ONCE on the parent link (DapProxy unchanged — worker targets fire on the CHILD link, already answered there), breakpoints verify late but hit with exact webRoot source mapping, and a client disconnect alone kills the whole browser in <3s (js-debug cleanUp=wholeBrowser; killTreeAndWait backstops). URL pick: exact-project ServingRegistry WEB serving → containing-dir serving (monorepo) → file:// for .html → honest status for bare scripts. BrowserLocator (Chrome→Edge→Chromium, per-OS candidates, ToolLocator idiom); headed Chrome on a throwaway profile under the userdir cache, deleted only after the tree is confirmed dead. Ledger 39 (the recon surprise): an answered-but-unattached WORKER target pauses forever — different from Node's run-undebugged; no browser autoAttachChildProcesses:false exists. RealChromeIntegrationTest (real adapter + real headless Chrome + in-JVM fixture server; disconnect-kills-Chrome asserted on live ProcessHandles; mutation-proven after a stale-compile false negative). editor 373 (+18)
- **v1.44.0**: the debt sweep — ledger 15/16/17/22/26/28 closed + 34 mostly, each with proving tests: async Stop All/switch (Rack.stopAllAsync on RequestProcessor("Rack Stop",4); EDT freeze measured at 1,513ms/device on old code by the regression test; switch swap callback-driven, dialog unchanged; shutdown reaper synchronous + source-gated; orphan guarantee proven on ProcessHandles), SaveLane ×4 (EDT snapshots + single-throughput named writers; write+stamp one task; classify() rides the lane so foreign-vs-own can't misfire; close/reload drain; DbWorkspaceIO was the last non-atomic writer — caught in passing), listener symmetry on RackPanel/RackTopComponent (mutation-proven lifecycle tests; two headless-safety touches), ProgressHandles on DB connect + cloud sync (honest no-cancel comments — no interrupt seams), Keymaps shadows give Window-menu items their accelerators (WindowShortcutsTest pins chord AND target instance — the v1.38.1 drift class structurally dead), userdir_root warning killed by an @OnStart setter (conf word-splits on "Application Support"), SBOM gains the vendored js-debug component. rack 827/apiclient 108/infra 202/dbstudio 364/web3 282/ui 43/project 19
- **v1.46.0**: the soft-dependency release — ledger 30+31, the same surgery: core exports org.nmox.studio.core.spi (ProjectAim: projectDir/aim/recentProjects + projectChanged/manifest listeners; LiveServings: snapshot + coarse listeners) with rack publishing thin @ServiceProvider adapters (pure delegation, wrapper-mapped listeners — double-add never double-delivers); 31 catch(LinkageError) sites converted to find()-and-null-branch across apiclient(6)/web3(6)/dbstudio(5)/project(6)/tools(4)/infra(2)/ui(1); apiclient/web3/infra dropped the rack Maven dep (RackSoftDependencyTest ×3 pins lookups-null + rack-unloadable); kept catches carry KEPT/why comments (dbstudio's FileWatcher/DockerClient, project's rack UI surface, platform-optional Keyring/notifications/kits); OpenIDE-Module-Friends on rack = editor/tools/project/ui/dbstudio exactly (manifestEntries — the nbm plugin has no friends param; byte-verified in the built jar); TrustGate deliberately not facaded (editor's hard dep stays for CommandExecutor). SoftDependencyGateTest mutation-proven; storm tests ride FakeLiveServings with the registry's real threading contract. core +4/rack +9/apiclient/web3/infra +2 each
- **v1.45.0**: the context release — ledger 29 as its own release: the OpenProjects bridge (findProject→open+setMainProject on RequestProcessor("nmox-project-bridge"); re-entrancy flag beats the WebProjectOpenedHook echo — proven by a hook-re-enter test asserting proceed runs EXACTLY once; passive aims source-gated + behaviorally proven processless; never-close source-gated), AimNodePublisher (off-EDT DataFolder-node resolve → EDT setActivatedNodes on Rack/ProjectStudio/Workbench, equality-guarded, componentShowing-gated), git chip verbs restored via createContextAwareInstance on the published node. LIVE: Team menu = full enabled git suite with only a project aimed (v1.40.0's disabled stub), chip Show Changes opens the real Git window on the real dirty file, fresh boot = 0 children. Ledger 29 remainder honest: Kit actions stay always-enabled (focus-keyed enablement would disable them in the editor — a regression masquerading as idiom), seven non-aim windows still lookup-less. rack 844/project 21 = 2,342 total
- **v1.47.0**: the spec-versions release — ledger 20 closed: module OpenIDE spec versions track the product version off one root `<spec.version>` property instead of the pom-derived 1.0 frozen since v0.1. Mechanism chosen by evidence: jar-plugin manifestEntries tried first and rejected (maven-archiver lets the nbm-generated manifestFile win on conflicting keys — tested; rack's Friends entry survives because nbm never emits that key), reactor-version schemes rejected to keep the tag the only version source; shipped = nbm's own sourceManifestFile seam (each module's src/main/nbm/manifest.mf declares ${spec.version}, a root filter-nbm-source-manifest execution interpolates it, conditionallyAddAttribute keeps it verbatim). Reactor ordering makes downstream Module-Dependencies read the real version off dependency jars (core > 1.47.0), so a module dropped into an older install is refused by the loader — the ledger-30 hardening. Release workflow stamps the property in the same three per-OS steps as branding's currentVersion (committed dev "NMOX Studio 1.0" untouched: Versions.extract reads 1.0 as dev and keeps dev builds out of the update check). SpecVersionGateTest (application, 3 tests, mutation-proven): all 11 shipped jar manifests equal the injected property, every source manifest carries the placeholder exactly once. Byte-verified across the assembled cluster; headless boot smoke clean in 4s
- **v1.48.0**: the remainder sprint — the small honest remainders of ledger 34 (now fully closed) and 29 (now Kit actions only). Project Studio's file tree publishes its selection: the selected file's DataObject node becomes the activated node (AimNodePublisher generalized to files — same off-EDT resolve + equality guard, new pinned law: a distinct-target storm on the busy lane resolves twice, in-flight + final, not N), cleared selection falls back to the aim node (never emptied out), hidden tabs still publish nothing. NPM Explorer publishes the found Node project's node and withdraws to a null opinion (not an empty array) when no project — the registry keeps the last real selection so the hand-read fallback (kept, its purpose written in place: aims with no Node project) still works, and projectFromNodes skips our own published node so a re-aim away can't echo the stale project back (guard test-pinned both ways). Contract Studio's artifact walk rides a finally-guarded indeterminate ProgressHandle through one scanWithProgress seam both rescan paths use — no Cancellable on purpose (uninterruptible Files.walk, the v1.44.0 idiom), source-gated so a bare scan call fails the build. rack 858/tools 64/web3 286
- **v1.49.0**: the diagnostics release — ledger 32 closed, recon-first: the item's premise was half wrong (RackSquiggler never hand-painted — it has rendered via HintsController since it shipped, so the squiggle half stays byte-identical and a new source-gate pins both halves to platform APIs); the real gap was the Task List. RackFindingsTaskScanner (editor/diagnostics, a PushTaskScanner under TaskList/Scanners in layer.xml — the framework instantiates it lazily on first scan, zero boot cost, the bus's late-subscriber replay catches it up): eslint/tsc/phpstan findings become Action Items rows — severity on the platform's nb-tasklist-error/warning groups, "[tool] " text matching the squiggle hover, File→FileObject via normalizeFile, scope-filtered, no EDT anywhere. Replace-per-run semantics live in the pure RackFindings core (the SPI's Callback is final with a package-private ctor, so the logic lives where plain tests reach it): a fresh batch answers every OLD-batch file (empty = clear), an all-clear run clears everything, one tool going clean never erases another's rows — mutation-proven (deleting the old-batch union fails 3 tests). DiagnosticsBus stays: transport, not debt. editor +12 (RackFindingsTest 8, RackDiagnosticsWiringTest 4)
- **v1.50.0**: the housekeeping release — ledger 19 and 23 closed, each sub-fix mutation-proven. Rack undo cluster (19): the load-bearing bug was undo BLEEDING across a preset/patch load — `RackIO.fromJson` ran its content-replacement (device removals + additions) with capture ON, so ⌘Z after a load peeled the just-loaded patch apart and, past that, resurrected the PREVIOUS patch's structure; fixed by clearing undo history at `fromJson`, THE single choke point every load routes through (Presets menu, Load Patch button, RackService autoload — RackService keeps its own clear for the no-patch project-switch case that never reaches fromJson). Plus `lastTriggerAt` entries leaked on device removal (removeDevice severed cables in bulk without dropping their cooldown entries — disconnect/removeCable did it per-cable; now removeDevice does too) and TAIL/TEMPO showed stale faceplates on undo re-attach (dispose() stopped the poll/clock but left the switch+LED+LCD "armed"; both now re-run their sync from an `onAttached()` override that fires on every re-attach, no-op on a fresh switch-off add). RackUndoTest +2, new RackReattachSyncTest +2, all rack-local, v1.44.0 listener-symmetry/async-panic tests intact. org.json (23): eight module copies STAY (classloader ClassCastException territory, ledger 3) but the version literal centralized to one root `<orgjson.version>` all eight poms reference — Dependabot bumps all in one PR; byte-verified all resolve to 20260522; OrgJsonVersionGateTest fails on any re-hardcode
- **v1.51.0**: the update center — ledger 21 closed, recon-first (registration attribute names decompiled from the shipped autoupdate-services jar, not folklore; the /latest/download 302 and its query-param tolerance curl-verified; the scheduler's default period read out of autoupdate-ui bytecode = EVERY_WEEK, first check ~500ms post-UI-ready). App side: `Services/AutoupdateType/org-nmox-studio-update-center.instance` in ui/layer.xml (AutoupdateCatalogFactory.createUpdateProvider; enabled, STANDARD, trusted — our own HTTPS channel; display name via the resources Bundle), pointing at `releases/latest/download/updates.xml`; ui gains the autoupdate-services module dep. Release side: `scripts/build-update-site.sh` (one mechanism for CI + local) runs `nbm:autoupdate` with `maven.nbm.customDistBase` pinning every NBM to `releases/download/v<tag>/` by absolute URL — chosen with evidence: AutoupdateCatalogParser resolves relative distributions against the PRE-redirect catalog URI, so relative URLs would mix a cached old catalog with newer /latest/ NBM bytes and fail the catalog's own SHA-512 digests. The aggregator walks getAllProjects() (ignores -pl; its updateSiteIncludes filter is nbm-application-only — both verified against the 14.5 mojo), so the script prunes the never-shipped sample module and gates the result (exactly 11 modules, no sample, all URLs pinned). updates.xml/.gz + 11 NBMs upload as ADDITIONAL release assets; the 6 existing names untouched; NBMs now embed the MIT license (root licenseName/licenseFile) so installs stop saying "License terms: Unknown". Root `<spec.version>` bumped 1.47.0→1.51.0 (sprint-open bump; same-version catalogs offer nothing, so dev builds and freshly-updated installs stay quiet — verified, not special-cased). UpdateCenterTest (ui, 5): registration shape + exact URL, Bundle key, workflow-ships-catalog (line-anchored after mutation showed `updates.xml.gz` masks a substring gate), script-pins-URLs, catalog-shape-when-generated; URL + workflow mutations proven to fail. UpdateCheck (the daily one-line notifier) unchanged — it notifies, the UC installs
- **v1.52.0**: ORACLE — AI assistance through the rack's metaphor (plan.md's one remaining AI direction, now chosen; the 45th device, OBSERVE category). Recon-first (a full design dossier, file:line for every claim): the bus to tap is FlightRecorder, not RackBus — `last()` where `kind==EXIT_FAIL` already folds the "error on the MONITOR bus" into structured, bounded context (command from the LAUNCH event, exit code, ≤5 sampled ERROR lines, device, duration); the analog to copy is BlackboxDevice (recorder consumer + QUERY-blue button + modeless popup), not a command device. `OracleClient`: plain HTTPS+JSON to `POST /v1/messages` over the shared `HttpClientFactory` behind a `Transport` seam (JsonRpcClient idiom) — prompt assembly is a pure `FailureContext→String` function, response parse (text/refusal/error/empty) socket-free unit-tested; the key rides `x-api-key` only, never URL/body/logs; `claude-haiku-4-5` default, `claude-sonnet-5` on the MODEL knob. `OracleKeys`: OS keychain only (RpcSecrets idiom, `nmox.oracle.apikey`, `keyringUsable` seam, in-memory fallback, warn-once) + env fallback chain keychain→ANTHROPIC_API_KEY→CLAUDE_API_KEY through a swappable `env` seam so the env path is hermetically tested; faceplate KEY… uses a JPasswordField dialog, never the plaintext-echoing LCD editor. `OracleConsent`: ORACLE's OWN one-time consent (WorkspaceTrust is an inward execution guard; sending output to an external API is an outward flow it does not cover) — names exactly what's sent and what isn't, grant in `java.util.prefs` (the WorkspaceTrust mechanism, headless-safe), headless auto-allow without persist. Laws held: zero boot cost (attach only registers a recorder change-listener), no network without the EXPLAIN press (`consult` the single API path; key gate AND consent gate both mutation-proven — removing either lets a spy transport catch the attempt), honest LCD degradation for every failure state, DialogDisplayer throughout, listener symmetric. Tests: OracleClientTest 14 / OracleKeysTest 9 / OracleConsentTest 4 / OracleDeviceTest 6; DeviceContractTest + DeviceDocsTest auto-cover; rack 902; SpotBugs/find-sec-bugs clean on the new HTTP/secret code
- **v1.53.0**: community Learning Spaces — a ~/.nmox/learn-catalog.d/ drop-in dir; every *.json there adds learning spaces to the picker, merged with the 52 built-ins, same schema (docs/learning-spaces.md), slug-match overrides a built-in, malformed files skipped-with-note never blocking the picker, read lazily on picker-open (not boot) behind a path+mtime+size cache. 9 mutation-proven tests. rack 880
- **v1.54.0**: the SPI pre-work release — the Device SPI decision made (shape B: small declarative contract in core.spi, rack-hosted, laws by construction — chosen over freezing RackDevice; recon dossier with file:line evidence) and every closed-world assumption fixed first, each mutation-proven: unknown type-ids in .nmoxrack.json kept as MISSING placeholders preserving state+cables verbatim (the old silent drop shifted cable indices and rewired the patch), per-device bus names (two same-title devices merged their FlightRecorder timelines/stats into one phantom — first instance keeps the bare title, later get ·2/·3, assigned once at attach), DeviceCatalog as the one registry behind palette/persistence/⌘I/how-to/CI-export (the three byId() sites unified), contract tests parameterized over the catalog not @EnumSource, CI-step capability moved from CiExporter's hardcoded 14-id set into the catalog. rack 921
- **v1.55.0**: the Device SPI — third-party rack devices, shape B built: core exports frozen org.nmox.studio.core.spi.device (DeviceExtension/@ServiceProvider, DeviceDescriptor with reverse-DNS ids + typed PortSpecs, DeviceFace builder with role-typed buttons/tones + mandatory labels + transport-column placement + fit-or-fail, DeviceLogic callbacks, DeviceServices with trust-gated exec/emit/serving); rack hosts via ExtensionDevice + DeviceCatalog Lookup merge with skip-with-note validation (shelf law, port lexicon, namespace, collisions); laws by construction — the API cannot express a red GO, an unlabeled control, or an ungated spawn; contract tests parameterize over the catalog so a fixture extension answers all 8 laws every build; gate/validation/merge mutation-proven; LIVE E2E: out-of-tree Maven NBM (dep core > 1.55.0) installed via Tools ▸ Plugins without restart, UPTIME on the shelf, mounted, trust prompt fired on the plugin's exec, real uptime on the LCD; docs/device-spi.md worked example; extensions excluded from CI export/custom painting/resurrection as deliberate v1 scope. rack 940
- **v1.56.0**: the third senior review — five read-only lenses over the v1.40–v1.55 surface (frozen-SPI quality, security, threading, docs+tests, platform idioms), then fixes for what they proved, deferrals blessed in writing (ledger 41–44). House laws held; the finds: cloud API tokens were written to PLAINTEXT prefs by the Options panel (the one keyring bypass v1.36 missed) → OS keychain off-EDT under CloudTokens' scheme, source-gated; the frozen Device SPI got its day-old corrections — DeviceLogic.onAttached(services) default hook so undo re-attach revives a plugin (closes the v1.50 TAIL/TEMPO bug class for third parties), build() guarded on all three mount paths (was: only drag-drop), onDispose() now runs after the process is really stopped (javadoc made true, mutation-proven with a live sleep), port count capped at validation before jacks paint off-plate; the daily update notifier and the platform's weekly autoupdate now converge on the in-app Plugin Manager; ProjectStudio's Open-as-project findProject moved off the EDT; six doc-truth fixes (device counts 44/43→45, -proc JDK 23+, core.spi.device in the module table, ledger 31 SDK-shipped, REPL drop-in provenance, SPI enum-lockstep). Deferred: the systemic RackDevice.exec EDT fork (pre-existing, all 50 devices, own release). rack 949/core (+onAttached)
- **v1.57.0**: the threading tail — ledger 41 closed (every device's RUN button forked + read dotenv on the EDT; now both ride a RequestProcessor lane while a synchronous PendingHandle keeps the observable contract identical — running-true-on-return so gates can't double-launch, second-exec-cancels-first, stop-before-spawn creates no process, panic bounded on an unspawned run, exit fires once per phase; AsyncExecTest 7 lane-seam-stepped, live-verified a SOLDER echo to green OK with the UI responsive and trust firing on the EDT before the deferred spawn) + the three v1.56 EDT touches (learn-catalog picker scan, rack Save Patch write, ORACLE keychain peek all off-EDT) + the Windows JsDebugServerTest @TempDir flake (self-managed retry-delete temp dir). rack 960
- **v1.58.0**: plugin-ecosystem groundwork + two bounded fixes — NBM signing wired as a secret-gated release pipeline (sign-nbms root-pom profile on -Dnbm.keystore + base64-keystore workflow secret decode; OFF by default = unsigned as before, ON by adding 3 repo secrets; jarsigner-verified with a throwaway keystore; docs/engineering/nbm-signing.md), the UPTIME Device SPI plugin committed as a buildable examples/uptime-device/ (was scratchpad-only), ledger 43 (gitdir: pointer canonicalized + confined to a .git dir — worktrees/submodules still resolve, arbitrary paths refused) and ledger 44 (a MissingDevice never matches session-resume, killing the dead-click balloon) both mutation-proven. core/rack tests +5
- **v1.59.0**: the expansion — Gleam first-class end to end (gleam.toml ProjectKind in the BEAM family, pinned vscode-gleam grammar #49, CSL language + comment toggle + keyword completion + spellcheck mimes, `gleam lsp` server entry, IGNITION/FORGE/VERITAS/CRATE lanes + IDE Run/Build/Test/Clean all speaking gleam, 53rd learning space via the honest `gleam shell`-into-Erlang story); Environment Doctor backfilled with the 10 toolchains the IDE drove but never probed (gleam/dotnet/dart/zig/sbt/stack/dune/crystal/swift/gradle, zig's no---version dialect handled); docs-truth: the stack lists now admit the long-shipping .NET/F#/Dart/Zig/Scala/Haskell/OCaml/Crystal support. Recon lesson recorded: the summary docs undersold the product — verify coverage in code before scoping 'new stack' work
- **v1.60.0**: the package-manager truth release — every Node AUTO lane speaks the project's own manager: `ProjectInspector.nodePackageManager` (corepack `packageManager` pin wins, then pnpm-lock.yaml, then yarn.lock, then npm; kindDir-aware, mtime-cached, unknown pins fall through) drives CRATE AUTO (incl. `yarn upgrade` verb), NPM-9000 (new `auto` ENGINE appended per the index law, default for new devices, pinned patches keep their engine), the IDE-native Run/Build/Test/Clean argv, and NpmService/NPM Explorer (old lockfile-only detection deleted in favor of delegation); pnpm-lock.yaml + yarn.lock joined ManifestPulse (18 names, gate updated); Doctor probes pnpm/yarn (59 rows); 3 mutation proofs (hardcode-npm ×2 consumers, delete-corepack-branch) each fail named tests
- **v1.61.0**: the Biome release — the respect-their-toolchain rule applied to lint/format: `ProjectInspector.hasBiome` (biome.json/biome.jsonc, Node-lane-aware) flips PURITY's new appended-`auto` LINTER default to `npx @biomejs/biome lint .` (FIX = `--write` vs `--fix`, block headers → DiagnosticsBus as `[biome]`, `Found N errors/warnings` → the E:/W: LCD) and GLOSS to `biome format`; PREFLIGHT counts biome.json as lint config; Doctor probes biome (60 rows); saved patches keep pinned linters (append law test-pinned); 2 mutation proofs
- **v1.62.0**: the journey polish — from a live senior-web-dev walk (create→install→serve→browser→stop all green): wizard first-run default unified to ~/NMOX (recent location still wins), wizard install manager-aware with the pre-trust blessing written in place; the Tailwind LSP was built, live-tested, and honestly pulled — the platform LSP client lacks client/registerCapability so the server can never answer (ledger 45, evidence + restore path recorded)
- **v1.63.0**: the workspaces release — WAYPOINT (50th device): Workspaces pure core (npm workspaces globs + pnpm-workspace.yaml union, bounded glob subset, 64-cap), Rack.workspaceOverride mirroring toolchainOverride, Node lanes re-root through the one CommandDevice.commandDir choke point (NPM-9000 scripts/manifest follow; non-Node lanes pinned untouched), knob persists by name, pnpm-workspace.yaml in ManifestPulse (19), ROSETTA dispose law + onAttached re-apply; 3 mutation proofs — the first M1 run not biting exposed and closed an untested base-lane path
- **v1.63.1**: saved dynamic-knob selections survive patch reload — Knob.pendingSelection remembers a by-name wish applied before async options land (the RackIO load order), granted at setOptions, superseded by any explicit dial; heals NPM-9000's SCRIPT + WAYPOINT's WORKSPACE restore (latent since v1.0); patch-load-order test + mutation proof
- **v1.63.2**: CI export portable separators — the review pass's WAYPOINT×export composition pin failed on the windows lane and exposed relativize()'s backslash paths in exported workflows; normalized to forward slashes; overnight-surface review otherwise blessed (steering mirrors, volatile visibility, docs counts)
- **v1.63.3**: night polish — Monorepo Lanes preset racks WAYPOINT beside ROSETTA (live-verified); wizard default location skips ~/.nmox internal homes (learning-space recents no longer hijack it; seam + 3 tests, mutation-proven)
- **v1.64.0**: ledger 36 closed — Project Studio's file tree became a platform citizen: `FileTreePanel` is an `ExplorerManager.Provider` over a `BeanTreeView` on the root's `DataFolder` node delegate, replacing the hand-rolled JTree (~230 lines lighter). Wins the old tree couldn't have: real DataObject file-type icons (JS/Markdown badges), the `[main]` git annotation, the full platform node menu (Open/Cut/Copy/Delete/Rename/Tools/Properties — a superset, Cut/Copy new) driven by the DataObject cookies, and lazy off-EDT children. Laws preserved with their incidents: root resolve OFF the EDT with newer-aim-wins (the v1.33.1 TCC storm, test-pinned); heavy dirs (node_modules/.git/dist/build/coverage) render childless — no triangle, no 100k-file misclick storm; external edits via FileUtil.refreshFor keep expansion state. HeavyAwareFilterNode/FileLeafNode/DarkNode give each node its right action set; the selection still feeds AimNodePublisher (ledger 29). Live-verified end to end (icons, git chip, childless heavy dirs, Open-opens-editor); RootResolver seam pins the off-EDT law + stale-resolve guard
- **v1.65.0**: VELOCITY — the Vite console (50th device), mirroring NEXUS/HALO for the tool under most modern React/Vue/Svelte/Solid SPAs: DEV (serve + URL→SCOPE, READY/SERVING), BUILD (production bundle), PREVIEW (serves the built dist), and vite-vs-registry version currency. DeviceType FRAMEWORKS entry, CI-step capable, FrameworkDeviceTest coverage, devices.md regenerated; IGNITION still serves Vite generically for the un-opinionated case
- **v1.65.1**: review fix — Project Studio's file tree survives a tab close/reopen (dispose no longer stops the reused panel's RequestProcessor, which had left the reopened tree stuck at "No project"; latent in the old tree too, now caught via the RootResolver seam, mutation-proven); same review added VELOCITY's missing onFinished so stopping the Vite server drops its serving registration (⇄ chip/⌘I/VITALS) instead of leaving a phantom entry
- **v1.66.0**: COSMOS — the Astro console (50th device): DEV (serve + URL→SCOPE, READY/SERVING), BUILD (static site), PREVIEW (serves dist), DIAG (astro check), astro-vs-registry version currency, onFinished serving-deregister built in; FRAMEWORKS palette + CI-step; FrameworkDeviceTest + ServingDevicesTest; devices.md regenerated
- **v1.67.0**: the meta-framework consoles — KINETIC (SvelteKit, 49th: vite dev/build/preview + svelte-check DIAG, @sveltejs/kit currency) and NIMBUS (Nuxt, 50th: nuxi dev/build/preview + typecheck, nuxt currency); both with serving-deregister-on-stop built in, FRAMEWORKS palette + CI-step, devices.md regenerated to 50 — the framework family (HALO/NEXUS/VELOCITY/COSMOS/KINETIC/NIMBUS/PHOENIX/ARTISAN) is complete
- **v1.67.1**: Modern Web preset (VELOCITY→SCOPE serve loop + REFLEX→VERITAS/PURITY save lanes) — the console-family discoverability capstone; COSMOS E2E-validated live against real Astro 4 (registry currency, trust gate, real dev spawn, serving chip, stop-deregister)
- **v1.68.0**: framework learning spaces — Astro/SvelteKit/Nuxt join the catalog (53→56), each a real minimal app + tutorial pointing at its console (COSMOS/KINETIC/NIMBUS), same run-driver shape as the Next.js space
- **v1.69.0**: the indie stacks — Julia completed (Project.toml ProjectKind + Pkg lanes on CRATE/FORGE/VERITAS; run greys honestly), Nim (\*.nimble glob, nim-secret REPL space, nimlangserver), D (dub.json/dub.sdl, serve-d, --missing-only install verb, no-REPL recorded honestly), Racket (info.rkt, raco lanes, racket -i space) — each a full vertical: pinned grammar + CSL + comments + keywords + spellcheck + outline + Doctor; 4 detection cases + 4-lane parameterized tests + 2 mutation proofs; Odin skipped honestly (no manifest)
- **v1.70.0**: the functional web — Elm (elm.json kind, elm reactor/make/elm-test lanes, elm-repl learning space, elm-language-server), ReScript (rescript.json + legacy bsconfig.json, rescript build/clean, no-test-runner greyed honestly), PureScript (spago.yaml/spago.dhall, spago run/build/test/install, Haskell-family outline) — NODE outranks all three beside a package.json (WEBPACK-family rule, test-pinned); Doctor +3; grammars sha256-pinned; 1 mutation proof
- **v1.71.0**: senior review of the v1.64–v1.70 surface (two read-only lenses over ~10 releases) — console lens clean (8 devices' serving parity/readyFired/version-package all correct); stack-lane lens fixed 3 mutation-proven bugs: Racket build compiled info.rkt (metadata) not main.rkt (the program); CRATE's CHECK silently ran install for 5 tools with no outdated verb (now greys); ReScript fell through to npm start/test instead of greying (build-only). NextDevice START parity + older outdated-fallthrough class blessed as pre-existing
- **v1.72.0**: V (vlang) full vertical — `v.mod` → `ProjectKind.VLANG`; pinned vscode-vlang grammar on `text/x-vlang` (`.v`/`.vsh`/`.vv`, sha256 in NOTICE), CSL VLanguage (`//`), mime threaded through typing/deletion/spellcheck/comments/keyword-completion/brace-outline; IGNITION `v run .` / FORGE `v .` / VERITAS `v test .` / CRATE `v install` (update `v update`, CHECK greys — no vpm outdated) / ROSETTA lists it / IDE-native Run/Build/Test speak `v` (no clean); `v-analyzer` LSP + catalog + Doctor probe; `v repl` learning space (60 spaces). Full coverage: detection + every AUTO lane + IDE lanes test-proven; the TestDevice AUTO-map's missing VLANG case was caught by the new lane test before ship
- **v1.73.0**: Fortran full vertical (fpm) — `fpm.toml` → `ProjectKind.FORTRAN`; pinned free-form vscode-fortran-support grammar on `text/x-fortran` (`.f90`/`.f95`/`.f03`/`.f08`/`.f18`, sha256 in NOTICE), CSL FortranLanguage (`!`), mime threaded through typing/deletion/spellcheck/comments/keyword-completion; a dedicated Navigator outline (program/module/subroutine/function/type; rejects `type(x) :: p` variable decls); IGNITION `fpm run` / FORGE `fpm build` / VERITAS `fpm test` / CRATE `fpm build` deps (update `fpm update`, CHECK greys) / ROSETTA lists it / IDE-native Run/Build/Test speak `fpm` (no clean); `fortls` LSP + catalog + Doctor probes; a run-driver `fpm run` learning space (61 spaces). Full coverage: detection + every AUTO lane + IDE lanes + the outline false-positive guard test-proven
- **v1.74.0**: the classics release — GNU Smalltalk (`.st`, gst REPL space, no-line-comment truth test-pinned), Prolog (`.pro`/`.prolog`/`.plt` — `.pl` stays Perl's, documented; swipl space), Tcl (`.tcl`/`.tk`, tclsh space), Scheme (`.scm`/`.ss`/`.sld`/`.sps`, guile space, outline rides the Racket extractor) — each a pinned MIT grammar (2 converted YAML→JSON, 1 plist→JSON) + CSL + keyword completion + spellcheck/typing + Doctor probe; honest scope: no manifests/LSPs exist, so editor citizenship + REPL spaces, no invented lanes; 67 grammars, 71 spaces
- **v1.75.0**: the systems-heritage release — Ada full vertical (`alire.toml` → ProjectKind.ADA, `alr run`/`alr build` lanes, VERITAS greys honestly with the ReScript-bug-class guard test, CRATE deps-at-build, ada_language_server LSP, textmate/ada.tmbundle grammar — AdaCore's is GPL-3.0, deliberately not used; `alr run` space) + Pascal (`.pas`/`.pp`/`.lpr`, instantfpc run-space) + Odin (`.odin`, ols LSP, brace outline, `odin run .` space; no manifest — honest editor-only scope) + COBOL (`.cob`/`.cbl`/`.cpy`, `cobc -xj` run-space); Doctor probes alr/odin/instantfpc/cobc; 67 grammars, 71 spaces
- **v1.76.0**: senior review of the v1.72–v1.75 surface — lane lens CLEAN (all reverse maps/greys/precedence/append-only held); registration lens found the Navigator outline dead for TEN languages (Structure panel registrations frozen at v1.34.0 while OutlineModel grew — Fortran/V/Scheme/Odin + the six v1.69/70 outlines unreachable; fixed + OutlineNavigatorGateTest drift-gates family() vs registrations, mutation-proven), Open Project missing 8 manifests (gleam/pubspec/sbt/stack/cabal/zig/dune/shards — test-pinned), Ada space missing its .gpr + phantom brew hint, Prolog `[family].` load bug (`['family.pro'].`), self-printing guile/gst/swipl snippets (piped-echo class), Doctor +v-analyzer/ada_language_server; ledger 46 (CiExporter setup steps) + 47 (INSPECTOR node default) recorded
- **v1.77.0**: the polyglot tail — Haxe (`.hx`, vshaxe grammar, brace outline, `haxe --interp` run-space) + Janet (`.janet`/`.jdn`, Clojure-family outline, janet-lsp, self-printing REPL space); Raku/Forth honestly skipped (no cleanly-licensed grammars, documented in NOTICE); the v1.76.0 outline drift-gate caught its first real regression mid-release (new family() mappings without Navigator registrations — build failed until fixed); Doctor +haxe/janet; 67 grammars, 71 spaces
- **v1.77.1**: ledger 47 closed — INSPECTOR's AUTO greys honestly ("NO DEBUGGER FOR <KIND> — DIAL TARGET", no spawn, no gate) for toolchains with no wired debugger; node default confined to the web family; explicit knob choice always wins; DebugDeviceGreyTest mutation-proven
- **v1.77.2**: every v1.72–v1.77 learning space live-verified with real toolchains (11/12 pass exactly as documented — run-spaces compile+print, REPLs echo all self-printing snippets; Ada untestable, alr has no brew formula); the one find — brew's V misses libgc (`-L$(brew --prefix)/lib` fixes) — documented as a Troubleshooting section in the V space tutorial
- **v1.78.0**: Block Studio — a Scratch-like studio tab (⌥⌘5) composing real Web Components from interlocking typed pieces: pure interlock-rule matrix + checked BlockDoc edits (illegal nestings refused, incl. on load), codegen to a self-contained custom element (shadow DOM, state fields, listener wiring, escape-proof template) with a per-block character-range map driving click-a-piece→highlight-its-lines in a live text/javascript code pane; Save Component writes src/components/<tag>.js atomic + never-clobber (marker-guarded); .nmoxblocks.json workspace follows re-aims; ⌘Z snapshot undo; palette drag-and-drop with legal-slot-only previews; zero boot cost, IO on a named RP, listener-symmetric, a11y-named (the lifecycle test caught the canvas lacking an AccessibleContext). v1 scope recorded: one-way blocks→code, no external-edit pulse, no ⌘I provider
- **v1.79.0**: the debt sprint — ledger 46 closed (CI export emits real setup actions for every post-v1.59 toolchain, npm-family deduped, honest NOTE comments for scala/swift; test-pinned) + Block Studio studio-law parity (external-edit pulse on .nmoxblocks.json with self-write discrimination + pending-save override said out loud; ⌘I reach with live tag/piece-count label at position 350) + docs truth (43/44 marked CLOSED per v1.58.0). Lesson: a hand Bundle.properties beside @NbBundle.Messages clobbers the generated bundle — category bundles live in a search subpackage
- **v1.80.0**: Block Studio v2, the live loop — in-memory loopback preview server (JDK httpserver, per-request codegen read, harness mounts bare + slotted + logs dispatched CustomEvents; ⇄/⌘I registration with stop-deregisters on Stop/close/re-aim) + three lawful pieces: SLOT (`<slot name>`), TIMER (connectedCallback/disconnectedCallback with clearInterval; toggle-class targets the host), DISPATCH (bubbling composed CustomEvent, state-interpolated escaped detail); ranges + validation + interlock matrix all pinned (BlocksV2Test 8)
- **v1.81.0**: Block Studio v3, the round trip — BlockParser (strict dialect-only parser, line-numbered refusals, no half-imports; in-dialect hand edits import fine), `generate(parse(code))` byte-identical (BlockRoundTripTest, 6-doc corpus incl. hostile escaping; ids ride data-b anchors + const vars), Open Component… toolbar action (IO-lane read+parse, undo snapshot, honest off-dialect dialog), and the PROP piece (observedAttributes + attributeChangedCallback re-render + `#prop_name()` private accessors carrying defaults; `{@name}` interpolation beside `{state}` in every context). rack +org-openide-filesystems-nb dep (FileChooserBuilder)
- **v1.82.0**: the Block Studio review — two read-only lenses (pure cores probe-proven standalone + UI/lifecycle) over v1.78–v1.81, 14 mutation/regression-proven fixes: reopen-reloads-for-current-aim (the inverted-lifecycle class), canonical host-grouped listener emission (the round-trip byte law now survives early listeners — corpus case added), validate() covers every generate() throw (ternary exprs, 20-digit timers, reserved data-b, newline params, leading-zero literals), .bak-before-fallback, lane-ordered self-write re-check, force-save-before-re-aim, stale lastResult/⌘I cleared, same-parent drag off-by-one, import-validates-first, parent-range highlight fallback, validated harness tag, daemon preview threads, drop-line cleanup, insert aliasing guard; ledger 48 (keyboard canvas) + 49 (exit deregister residue) recorded
- **v1.175.0**: AI-arc review — the re-aim/stale-Explain hazard closed in the second consumer too; one shared clearResultTabs()
- **v1.174.0**: DB Studio explains SQL errors — the seam's second consumer, tightest disclosure in the product (no rows exist to leak)
- **v1.173.0**: Explain gauntlet — live-proven vs the real API; consent now precedes the window (a decline opens nothing)
- **v1.172.0**: API-arc review — re-aim clears the armed response (cross-project disclosure path closed), neutral seam default; rest CLEAN
- **v1.171.0**: ORACLE explains API responses — core.spi.OracleAsk facade (text-only seam), redaction in the studio that owns the data, kind-scoped consent
- **v1.245.0**: docs truth — plan.md closes the 2026-08-02/03 night shift (twelve releases v1.233–v1.244: two arc reviews, the Angular truth arc, three generator homes gated, the OpenJFX lockstep refusal, the unreachable-platform-wizard finding queued for decision); docs only
- **v1.244.0**: the third generator home joins the proven line — the platform New Project wizard still scaffolded Create React App (react-scripts 5, dead upstream, cannot install beside the React 19 pin Dependabot kept fresh AROUND it) and its Vue template had marched to vite ^8 (the exact version the v1.237.0 proof watched refuse node 22.9) because this home had no gate; react is now the proven Vite set (the proof caught plugin-react 5 being ESM-only → type:module), vue clamped to vite ^6 + plugin-vue ^5.2, both npm-proven from the exact shipped files on default node 22.9, and WizardTemplateCeilingTest makes any future over-ceiling bump fail CI on the bump PR itself
- **v1.243.0**: deps housekeeping — snakeyaml 2.6 + MariaDB driver 3.5.10 through the gate; the grouped PR's OpenJFX 21→26 pom-only bump REFUSED structurally (provided-scope deps must move in lockstep with the release workflow's sha256-pinned jmods or the Browser compiles against an API it doesn't ship — skew no CI lane can see; dependabot.yml now ignores org.openjfx, ledger 74 owns the real gauntleted upgrade)
- **v1.242.0**: night docs truth + the angular space gauntleted in the shipped app — picker blurb, real-workspace scaffold, and app.component.html opening with Angular-template highlighting all verified live in the installed 1.241.0; plan.md carries the night's closing addendum incl. the pinned Angular engine facts and the law *a starter that was never installed is a claim, not a product* (docs only otherwise)
- **v1.241.0**: the Angular truth release — the framework bet's own starters PROVEN: the template's ^22 pin could never npm-install (Angular 22 requires TS 6; the TS-5 ceiling binds) and the angular learning space shipped Angular-18 sketch files with NO angular.json so its own START could not run; both now pin the live-proven ~21.2 + TS ~5.9 line, the space is a real zoneless workspace whose tutorial walks the whole Angular arc, both generators pin `@schematics/angular:component {type: component}` so ng generate emits the `.component.*` naming the IDE's template intelligence keys on (proven on Angular 21's otherwise-suffixless CLI), AngularSpacePinParityTest locks the two homes together (mutation-proven), and the template README states Angular's node floor (20.19+/22.12+/24+, CLI hard-refuses below); suffixless external projects → ledger 73
- **v1.240.0**: the night-tail review (v1.235–v1.239) — the CoalescerTest sleep-based timing assumption that blocked v1.239.0's macOS lane made deterministic (await-the-first-dispatch; the coalescing behavior it tripped over was CORRECT), and the schematic "one vocabulary, two surfaces" comment made structural (NgSchematicParityGateTest reads the rack source since HALO's field is private, mutation-proven); AimFollower's six wirings + the npx cross-OS path verified CLEAN; two kit-action LOWs blessed in writing
- **v1.239.0**: File ▸ New Angular Schematic… — ng generate as an IDE gesture (schematic combo = HALO's exact vocabulary, name + folder fields, cwd-resolved generation in place, output streamed); kit-action idiom (always enabled, honest no-angular.json refusal), trust-gated + ledger-classified, pure NgSchematic core with a traversal guard (../../etc dies before any spawn) unit-tested
- **v1.238.0**: night docs truth + the update-center gauntlet re-proven — a stock v1.230.0 portable updated itself in-app to 1.237.0, all 11 NBMs digest-verified, originals in update/backup, clean restart, byte-verified spec versions in all eleven installed jars (docs only otherwise; plan.md currency for the whole night arc)
- **v1.237.0**: template currency — React 19/Express 5/Vue 3.5/eslint 10/vitest 3 + the Vite line ^5.4→^6 with matching plugin majors, ALL SEVEN touched templates live-proven by real npm install+build from the template code before ship; two ceilings written at the pins: TypeScript stays 5.x (TS 7 = the Go port, no tsserverlibrary.js — the IDE's tsserver lane and ngserver both need 5), Vite stays ^6 (7+ demands node >=22.12; a starter must run on a learner's node — the live proof itself caught Vite 8's rolldown binding refusing node 22.9). Method: over-eager bumps to latest majors FAILED the npm proof, the empirical peer-set loop (fix one scratchpad package.json until green, then port) found the right line
- **v1.236.0**: the DevOps pass — what the product GENERATES is as current as what it runs: Export CI's action pins were 3 majors stale (checkout@v4 vs our own workflows' v7 — Dependabot reads YAML, not Java string literals; 9 bumps each verified against the action repo's latest release, 16 more verified current), Dockerize/LEMP bases moved to node:24-alpine/rust:1.89/golang:1.25/php:8.4/trixie-slim (every tag existence-checked on Docker Hub); NEW CiExportPinCurrencyTest — exporter pins must equal our workflows' pins for every SHARED action, so Dependabot's freshness now drags the generated workflows with it. The drift pattern named: *a version literal inside a generator is invisible to every bot that keeps the rest of the repo current*
- **v1.235.0**: the aim follows you — the aimed project's node becomes the ambient selection of Welcome/Browser/IRC/Docker Panel/Block Studio/DB Studio via the new rack.service.AimFollower (three hooks per window, the whole v1.45.0 discipline packaged: publish-only-while-showing, symmetric attach/detach, equality-guarded, reset-on-close), so Test Project ^F6 + the Team menu work wherever the user is — live-proven from the WELCOME tab (Test Project (cmake-demo) → ctest 1/1 Passed); apiclient/web3/infra stay selection-less by the v1.46.0 soft-dependency architecture → ledger 72 (core.spi aim-node facade when it earns a second consumer); Doctor probes cmake; PLUS the v1.233.0 lanes gauntleted in the shipped app — CMake configure→build→ctest behind the trust prompt, per-subdir CMakeLists staying ONE project
- **v1.234.0**: the night-arc review — two lenses over v1.226–v1.233, seven MEDs fixed, no HIGH: ~/.nimble (a DIRECTORY) made $HOME a platform project via the day-old glob recognition (both halves fixed — files-only + dotfiles-skipped + name-only File.list, the v1.33.1 TCC class disarmed again); CMakeLists.txt/Makefile chains collapse to the outermost member (F6 in src/ configured the wrong root); ⌘-hover's per-mouse-move full-document scans now cache by document edit-version; Compile to CSS moved its save+refresh off the EDT; eslint/stylelint refuse project-less lookups (both gates were skipped — the config is executable JS resolved from the FILE); the swatch layer moved to SHOW_OFF(430) placed by DECOMPILED anchors (warnings SHOW_OFF 420, selection SHOW_OFF 500 — TOP_RACK beat warnings but occluded selection and its comment claimed otherwise); save-to-reload checks the URI host not a substring; to-rgb() stops swatching as rgb(); WCAG refuses ANY translucency and truncates its displayed ratio so display never contradicts verdict; NEW second spawn ledger over ProcessSupport.builder callers (the sass spawn was gated but invisible to the CommandExecutor.run enumeration); bridge failures log at INFO not FINE. Method: a z-order comment is a claim about a total order — decompile the anchors
- **v1.233.0**: Run/Build/Test buttons work for every stack — DOTNET speaks dotnet run/build/test/clean (fell to default-null for six releases), TACT rides its package.json scripts, CMAKE follows plansOnlyWhatExists (configured build/ → cmake --build / ctest / clean; else the cmake -B configure step); the deeper find: WebProjectFactory never recognized the GLOB-detected kinds (.csproj/.fsproj/.sln/.nimble) or CMakeLists.txt/Makefile, so those checkouts were never platform projects at all — the lanes existed, the door didn't; the OpenProjects bridge now waits on openProjects().get(5s) before setMainProject (open() is async, the IllegalArgumentException was swallowed at FINE and F6 kept running the PREVIOUS project); NEW completeness gate: every ProjectKind resolves at least one IDE lane or is blessed with a written reason; live-proven — Open Project accepted a bare-.csproj dir, F6's trust prompt named the right root, dotnet run exit 0 through the rack; method lesson re-learned: a stale MODULE CACHE serves old bytecode after a jar swap — the broken org.json classpath in the dev install faked three product bugs before the cachedir wipe
- **v1.232.0**: the Senior CSS3 pass — stylelint findings arrive on their own: stylelint-lsp as a LanguageServerProvider across the whole css family (the v1.213.0 eslint arrangement for stylesheets), config-gated (ten spellings + package.json key) AND trust-gated (a stylelint config is executable JS — the v1.216.0 payload law), joining the mimes' other providers rather than replacing them; closes ledger 71's "hire the modern cop" path — live-proven with stylelint's own rule name in the editor tooltip ("Unexpected invalid hex color ... (color-no-invalid-hex)") stacked above the legacy parser's noise, invalid hex unswatched beside a swatched valid one; catalog names BOTH packages (stylelint-lsp + stylelint), Doctor probes it
- **v1.231.0**: the Junior CSS3 pass — the CSS Color 4 functions swatch and pick: oklch()/oklab()/lab()/lch()/hwb() paint as the colors they are (real conversions — Ottosson Oklab matrices, CIELAB D50 + Bradford, spec percent scaling — pinned on published anchors, oklch red within ±2/255) and the ⌘-click picker replaces each in its authored form via the inverse conversions; color-mix() swatches the COMPUTED mix spanning the whole recipe with component literals layered inside (balanced-paren walk, spec percentage normalization, srgb/srgb-linear in their own spaces, others honestly in Oklab); narrowest-span wins for inner clicks. The gauntlet also found the platform's legacy-grammar WARNING background painting over exactly the modern literals (color-mix, space-syntax hsl) — swatch layer moved to TOP rack so colors always show with the squiggle honest underneath; silencing the false warnings themselves proved externally blocked three ways (inert disabledErrorChecks, union ErrorFilter, additive ErrorsProvider) → ledger 71 with evidence. Persona recon surprise: the platform property DB is CURRENT (container-type completes with spec docs) — the pain is value-grammar only
- **v1.230.0**: the designer pass part 4 — Compile to CSS in the SCSS editor: the right-click gesture runs `sass --no-source-map` to the sibling .css AND arms recompile-on-save (the v1.212.0 arms-on-the-gesture law); binary resolution is the PrettierFormatter trust idiom (project-local .bin/sass only when trusted, else global), partials refuse honestly, sass errors land verbatim on the status bar; live-proven end to end — one ⌘S turned the served page tomato through recompile → theme.css → v1.228.0 save-to-reload, scss → pixels with no terminal. PLUS the gauntlet's find: css-prep resolves .scss/.less to text/scss+text/less BEFORE our x- resolver, so the v1.227.0 swatches, v1.229.0 picker, and the Prettier menu had NEVER reached real SCSS/Less files (only indented .sass) — all four surfaces now also ride the platform mimes, css-prep's own SCSS intelligence kept intact. Doctor probes sass
- **v1.229.0**: the designer pass part 3 — the click-a-color picker: ⌘-click a color literal in CSS/SCSS/Less opens a chooser seeded with that color, picking replaces the literal IN ITS AUTHORED FORM (hex→hex, rgb()→rgb(), hsl()→hsl() via the reverse conversion round-trip-pinned, named→hex) as ONE undo unit, with a stale-document re-scan refusing the edit honestly if the stylesheet changed under the chooser; PLUS the gauntlet's find fixed — v1.227.0's swatch layer sat in the SYNTAX racks and the CSL/TextMate coloring painted over every grammar-RECOGNIZED literal, so `tomato` and `#hex` never showed a swatch and only grammar-unknown names did; moved to SHOW_OFF_RACK, all forms paint (the failure pattern: a losing highlight merge fails silently and PARTIALLY — the feature works on exactly the tokens the grammar doesn't know)
- **v1.228.0**: the Senior Web Designer pass, part 2 — responsive viewport presets in the Browser (toolbar dropdown caps the WebView to real CSS-pixel device sizes centered on a backdrop; no UA games, media queries fire for real — live-proven with a max-width:500px breakpoint flipping on the iPhone preset) + save-to-reload (web-file saves reload LOCAL pages only, 400ms coalesced, listener attach/detach symmetric with the tab; live-proven — ⌘S turned the served page tomato with no manual reload, composing with the active preset). The designer's save → see loop is closed
- **v1.227.0**: the Senior Web Designer pass, part 1 — inline color swatches in CSS/SCSS/Less (every literal painted AS its color via a mime-registered HighlightsLayer, black/white text by WCAG luminance; hex/rgb()/hsl()/named forms, comments-are-prose and $red-dark-never-matches boundary rules, pure CssColors core) + a WCAG contrast verdict in the DevTools DOM pane (ratio + AA/AAA × normal/large from the element's computed colors, transparent-background honest refusal, pinned on the 21:1 and #767676-borderline reference values); both click-verified live (swatches on the seeded stylesheet; example.com body = 18.10:1 AA+AAA pass)
- **v1.226.0**: ledger 70 ROOT-CAUSED AND FIXED — the JFX WebView sends the RFC 7540 h2c cleartext-upgrade probe on every plain-HTTP request and Angular's esbuild dev server accepts the connection then never answers; proven headlessly (socket-logger capture + curl replay: hangs with the header, 200/5ms without, static server fine either way), fixed by com.sun.webkit.useHTTP2Loader=false at FxAvailability before any WebKit class loads (explicit -D still wins; https keeps HTTP/2 via ALPN). `ng serve` now renders in the Browser AND the DevTools Angular pane reads its live tree — the framework bet's dev loop, in-app, for the first time. Method: **capture the exact bytes, replay them headlessly — a UI-only hang becomes a one-header bisect**
- **v1.225.0**: the day-arc review + ledger 69 closed the day it mattered — clearForTest flips WorkspaceTrust into a scratch-node test mode so local mvn verify stops deleting the developer's REAL trust grants (bit five times on 2026-08-01 alone; sentinel regression-proven); RackTrustGateTest pins the facade wiring (a broken @ServiceProvider would silently remove the forge gates); v1.222–v1.224 lenses otherwise CLEAN with one LOW accepted in writing (project-local install decline reports FAILED, not a new enum value)
- **v1.224.0**: the spawn-site trust sweep — every CommandExecutor.run caller enumerated and classified in a BUILD-FAILING ledger (SpawnSiteTrustLedgerTest: GATED / GATED-BY-CALLER / BLESSED-with-reason; a new spawn site fails until classified — enumeration beats recollection, the v1.223.0 lesson made structural). Three ungated sites fixed: Contract Studio's forge build/test (ffi = arbitrary host commands; gated via the NEW core.spi.TrustGate soft-dependency facade + RackTrustGate @ServiceProvider, closing v1.46.0's "not facaded" deferral the moment it grew a consumer — live-proven, prompt names the right root and forge build runs on grant), Project Config's npm add/remove (installs run project lifecycle scripts), and the project-local ALS install (--save-dev runs workspace scripts; global installs stay blessed fixed-argv). Device GO path verified gated-by-caller (v1.93.0 trustCheck), New Project's pre-trust blessing confirmed real, Docker panel blessed (fixed verbs, container-scoped)
- **v1.223.0**: Run Focused Test speaks Angular + the missed trust gate — an Angular workspace (angular.json, no declared jest/vitest) now runs the CLI's own runner (`ng test --watch=false --browsers=ChromeHeadless --include=<spec>`, project-local .bin/ng preferred; Karma has no name filter so file-level focus is the honest ceiling); SECURITY: the action spawned project runners (npx jest / cargo test / phpunit / mix test) with NO WorkspaceTrust gate — the v1.102/v1.103 inward-execution class missed by BOTH sweeps, now requestTrust before every spawn, source-gated. The live proof found its own bug: Angular's src/index.html is a STATIC-kind manifest so the generic manifest walk stopped at src/ (trust prompt named ngdemo/src, branch fell to jest) — the Angular root is now located by walking up for angular.json itself, fixture carries the real layout; click-proven end to end (Keep Safe → no spawn; Trust → Chrome Headless → TOTAL: 3 SUCCESS, exit 0)
- **v1.222.0**: the DevTools Angular pane — Vue and Svelte each had one, the framework bet didn't: live component tree of an Angular dev build via window.ng.getComponent/getDirectives (class names, instance state with __ngContext__ filtered as plumbing, host directives, click-to-highlight), the two-step ng-version-then-window.ng detection making "production build, no component tree" its own honest answer distinct from "no Angular"; AngularSnapshotParser mirrors the Vue parser's hostile-input posture; live-proven in all three states against a real Angular 18.2 app (dev: AppComponent + title state + highlight; prod: the honest message with the version; HN: no Angular). Ledger 70 recorded: ng serve's esbuild dev server hangs the JFX WebView (load never commits) while the same bundle served statically works — measured, deferred with workaround
- **v1.221.0**: docs truth for the Angular arc (docs only) — README gains the "Angular, first-class" section (template mime + the Angular team's grammars, project-local ALS install with live template type-checking, ⌘B in templates, the component ↔ template switcher, HALO, the Angular project template; stale 78-grammar count corrected to 85) and user-guide ch. 5 gains "Angular templates, first-class" with the TypeScript-5 install note and the honest miss behavior — the framework bet was invisible in both documents
- **v1.220.0**: the Angular-arc review (v1.217–v1.219) — one find, fixed: the day-old switcher actions resolved ON THE EDT (Files.readString + a sibling .ts directory scan in actionPerformed — the v1.108.0 disk-on-EDT class); resolution now rides a named RP with an EDT hop only to open, source-gated. Verified CLEAN: LspLanguageIds' generic rule against all ~60 registered LSP mimes (the mime names are already VS-Code-aligned, the 4-entry exception table is complete; fortran-vs-FortranFreeForm accepted LOW), refusesCommand's absolute/relative split, the goto pair's span/delegation/chord registration; the template.let.ng internal-#ref TM4E warnings accepted as cosmetic in writing
- **v1.219.0**: the senior-Angular grants — ⌘B **Go to Declaration in templates** (two cooperating pieces, both measured necessary live: a mime HyperlinkProviderExt enabler claiming the identifier span and delegating the click to the platform LSP client's provider, + an `ng-goto-declaration` kit action bound via mime-scoped keybindings — on CSL panes the platform's global goto never consults the hyperlink chain, and a SAME-NAME mime action loses to the root registration, so the recipe is non-colliding-name + mime chord, chord test-pinned) and the **component ↔ template switcher** (Open Angular Template / Open Component Class popups; pure NgSwitch core — templateUrl wins resolved against the component's dir, sibling convention backstops, template→owner scans the dir's .ts excluding specs; honest status-line misses); the session's method lesson: **a stale cachedir masks NEW layer registrations from hot-swapped jars** while code edits to registered classes take — wipe the cachedir or a right registration reads as wrong
- **v1.218.0**: the Angular Language Service ANSWERS — the consumer-2 proof of v1.217.0's mime found the service dead twice over: (1) the platform LSP client sends the RAW MIME as didOpen's languageId unless a LanguageIdResolver rides the description's Lookup (decompiled LSPBindings) — ngserver only treats a doc as a template when it arrives as `html`; new LspLanguageIds (generic subtype rule + tiny exception table) at the launch() choke point serves all ~55 servers; (2) launch()'s missing-tool guard did `new File(pathDir, absolutePath)` — never resolves — so EVERY absolute command was refused: **the v1.216.0 project-local .bin/ngserver fix could never launch the server it found** (the payload-vs-gate class, third strike); refusal predicate extracted + test-pinned. Live proof in the shipped app: ngserver spawned from the project's .bin, and typing `user.logedIn` in a template produced the Angular compiler's own "Did you mean 'loggedIn'?" squiggle that cleared on revert — template type-checking against the component class, end to end, for the first time
- **v1.217.0**: Angular template awareness — `.component.html` resolves to text/x-ng-template (declarative resolver@250 beats the platform's html claim; ext+name-substring AND-compose, read from decompiled FileElement$Type.accept); the Angular team's 5 grammars vendored sha256-pinned, 4 registered as TextMate INJECTIONS into text.html.basic (first injection use; expression.ng include-only — injecting it made `<h1>` a TS operator), the html grammar drives the mime; @-block (13) + *-directive (7) completion gated on angular.json; **the mime lesson: a grammar alone does not make a mime** — no loader/kit means the PLAIN kit and every MimeLookup feature dead at once, so both text/x-ng-template AND text/x-http-request became CSL languages, which revealed **.http files had NEVER highlighted since v1.166.0** (fixed, click-verified); CSL kits SHADOW Actions-folder toggle-comment — configure CommentHandler instead; 85 grammars
- **v1.216.0**: arc review v1.209–v1.215 — the trust law's payload gap closed on both new LSP providers (ngserver executes the repo's node_modules via its probe locations; eslint flat configs are executable JS — both now trust-gated); the Angular install path made walkable (project-local .bin resolve, Install runs in the project not $HOME, hoisting-aware probe); ⌘P Go to File revived from THREE stacked kills (wrong tree, editor-consumed key, disabled-without-project) and click-verified live; per-run serving ids; STATIC lane announces (python -u relearned); nested-repository grammar gate; CJK search restored; ledger 63–67
- **v1.215.0**: the findability release — one term-based matcher for all eight search surfaces (phrases, plurals, word boundaries) + a controlled device vocabulary; **24 of 49 ordinary search terms found nothing before, 0 after**; docs/engineering/README.md stops calling its own live documents historical
- **v1.214.0**: the Angular Language Service — template intelligence; ngserver needs mandatory probe locations and TypeScript 5 (TS 7 dropped tsserverlibrary.js)
- **v1.213.0**: eslint findings arrive on their own — an LSP provider beside tsserver (the platform's lookupAll lets servers share a mime)
- **v1.212.0**: the mid-level sprint — the IDE's own Run button passed an EMPTY LAMBDA for output, so it never registered a serving; OpenOnServe arms on the gesture; package-manager-correct add/remove verbs
- **v1.211.0**: the Browser and IRC open by default for discovery — both build on first show, so boot stays free and IRC still never auto-connects
- **v1.210.0**: the junior-dev sprint — a shipping TM4E grammar crashed on a junior's FIRST project; gate the shape family, not the instance
- **v1.209.1**: the Windows lane's shell changed under us — ProcessSupportTest's sh fixtures gained a precondition probe (main went red on a docs-only commit)
- **v1.209.0**: docs truth, and the gauntlet that earned it
- **v1.208.0**: arc review v1.204–v1.207 — IRC TLS hostname verification (any CA-valid cert passed), transcript retention cap (the bounded-reads law stopped at the line), DevTools HTML-render vector (page text made the IDE's JVM fetch a URL), comment-toggle line-boundary corruption; + LICENSE/NOTICE in all five installers; live-gauntleted 4/4
- **v1.207.1**: the release survives a flaky CDN — both OpenJFX jmods fetches retry, sha256 still the gate; shipped as its own version (a published tag is never moved)
- **v1.207.0**: Svelte first-class (runes/blocks/directives completion, Vite+Svelte 5 template, svelteserver probe, DevTools __svelte_meta source mapping) + **relicensed MIT → Apache-2.0** (David's decision; NOTICE attributes the NetBeans Platform; gate covers docs)
- **v1.206.0**: Browser DevTools — own the WebView engine (no inspector exists); Console/DOM/Network/Storage/Vue panes; Vue prod builds honestly uninspectable
- **v1.205.1**: FakeIrcServer half-close — a bare close() sends RST on Windows and the reader misses EOF (caught by the main-green gate before a tag was cut)
- **v1.205.0**: IRC power-user tranche — SASL, IRCv3 caps, tab completion, highlights, clickable URLs→Browser, logging, /list, WHOIS, ignore, network editor, ⌘F; Escape dies in a docked TopComponent → Ctrl+U
- **v1.204.0**: an IRC client in the IDE (⌥⌘3, freenode default, keychain-only NickServ) + the Browser opens on Hacker News
- **v1.203.0**: coverage-and-comments tranche 2 — the rack and every studio, floors ratcheted across the board
- **v1.202.0**: coverage-and-comments tranche 1 — editor/tools/ui, floors ratcheted, 23 classes javadoc'd, the beginner's codebase guide
- **v1.201.0**: lazy keychain hydration — API Studio tokens load per request on first use
- **v1.200.0**: arc review over v1.194–v1.199
- **v1.199.0**: the in-app web browser — FX-lit platform WebKit, ⌥⌘4, SCOPE TARGET knob
- **v1.196.0–v1.198.0**: Prettier on demand, API Studio send history, response find-in-body + save-to-file
- **v1.194.0–v1.195.1**: the muscle-memory pack (⌘Enter/Duplicate/Rename), the .http editor gesture, smoke-test fixes
- **v1.190.0**: night docs truth — CLAUDE.md/plan.md carry the five-release night arc (docs only)
- **v1.189.0**: Insomnia review — base environment wins regardless of resources[] order, proven failing-first; "a comment claiming an ordering property is a test not yet written"
- **v1.188.0**: Insomnia v4 import — parentId chains → "Folder / Request" names, {{ _.templates }} translate, secrets law at the third border; import family complete
- **v1.187.0**: night gauntlet — Image Kit + Postman env import PASS in the installed app, byte-proven; brew-races-homebrew-job trap recorded (docs only)
- **v1.186.0**: Postman environment import — the v1.177.0 refusal becomes the feature; secret-typed values never enter the committable workspace file
- **v1.185.0**: night review — the Image Kit scan moves off the EDT (the v1.33.1 class in day-old code), source-gated
- **v1.184.0**: day docs truth — CLAUDE.md/README/plan.md carry the migration-complete story + Image Kit (docs only)
- **v1.183.0**: Image Kit (Web) — File ▸ Image Kit (Web)…: pure-Java JPEG press + downscale, cwebp WebP lane, kit laws on pixels, live-proven 17.8 MB→347 KB; Doctor +cwebp
- **v1.182.0**: migration gauntlet PASS in the installed app + Migrating-from-Postman tutorial (docs only)
- **v1.181.0**: import-arc review — captured opaque Authorization now follows the Cookie rule (drop-and-count); ### bodies can't split the .http export (proven failing-first); three blessings in writing
- **v1.180.0**: floors ratchet — ten JaCoCo minimums to measured-minus-margin, none lowered
- **v1.179.0**: .http export — the dialect both ways; auth deliberately not exported, round-trip law pinned with auth as the one written exception
- **v1.178.0**: HAR import — XHR/fetch kept, cookies dropped-and-counted, captured auth keychain-side, dupes collapsed, over-cap bodies refused
- **v1.177.0**: Postman Collection v2.x import — folders keep identity, {{vars}} verbatim, secrets law at the border, honest refusals
- **v1.176.0**: docs truth — the Explain-anything tutorial + studio tutorial updates + full CHANGELOG link block (217 versions)
- **v1.170.0**: OpenAPI 3 import — the trio complete (curl/.http/OpenAPI); update-center gauntlet PASS v1.160→v1.169 in-app
- **v1.169.0**: night docs truth — plan.md currency for the v1.164–v1.168 arc (docs only)
- **v1.168.0**: night-arc review — XML-body refusal fixed ("< path" is the file ref, not every "<"), .http read off-EDT; rest CLEAN
- **v1.167.0**: import gauntlet — live click-through PASS; name-dupe + toolbar-overflow finds fixed (Import… menu button)
- **v1.166.0**: .http files — Import .http… (REST Client dialect, near-lossless {{vars}}, secrets-law lift, honest < ./file refusal) + 78th grammar vendored MIT
- **v1.165.0**: API Studio curl import/export — CurlCodec pure core (secrets-law header lift, honest refusals, exact render), Import curl…/Copy curl UI; 17 tests
- **v1.164.0**: night review — five file-assuming preflight plans conditioned per plansOnlyWhatExists; parity fixtures carry conditioning files
- **v1.163.0**: the ship gate + IDE lanes speak every toolchain — PREFLIGHT plans for ~30 more kinds, nine null IDE lanes filled, PreflightLaneParityTest same-tool gate mutation-proven; v1.161 kinds live-gauntleted in-app
- **v1.162.0**: kind-lattice review — PreflightPlan starved by the precedence flip (Clarinet repos lost their npm ship checks) fixed + CLARITY/AIKEN plans; failure pattern recorded; plan.md currency
- **v1.161.0**: contract chains are project citizens — aiken.toml/Clarinet.toml/tact.config.json ProjectKinds with all lanes (aiken build/check, clarinet check + npm harness tests, npm-carried Tact); CLARITY-outranks-NODE precedence inversion mutation-proven; 58 manifests
- **v1.160.0**: the About screen wears the logo — branded about.png (icon mark + wordmark lockup) generated by BrandingArtGenerator, gated by BrandingResourcesTest, live-verified (branding only)
- **v1.159.0**: docs/tour.md — the tour in GFM, renders natively on GitHub; tour.html stays as the styled version (docs only)
- **v1.158.0**: Beginner’s Guide to Smart Contracts — five shapes, real refusal idioms, ten-minute first contract ×3 paths (docs only)
- **v1.157.0**: docs/tour.html — the visual tour in the repo, linked from README + user guide (docs only)
- **v1.156.0**: README leads with the tour — counts strip, four-frame tour with entry points, laws in the opening paragraph (docs only)
- **v1.155.0**: contract-language citizenship — Aiken/Tact/Clarity grammars vendored + CSL + comments + typing + spellcheck + keywords + brace outlines (Clarity outline skipped with reason); 77 grammars
- **v1.154.0**: night-arc review — npm-harness chains gain .gitignore (law test-pinned); rest CLEAN; plan.md current
- **v1.153.0**: TON/Tact in the Contract Kit (11th chain) + space #88 — npm-local loop, refusal-is-a-bounce asserted, live-proven raw+templated; FunC honestly out; 88 spaces
- **v1.152.0**: night docs truth — ten chains + 87 spaces + the AI arc in README/user-guide/plan.md
- **v1.151.0**: Cardano/Aiken in the Contract Kit (10th chain) + space #87 — vault validator with tested refusal path, live-proven raw+templated; parity gate reads aiken dependency tables; TON recon RED recorded; 87 spaces
- **v1.150.0**: Ask ORACLE model choice (Fast/Deep, remembered, fixed per conversation) + the night gauntlet: editor face click-verified live in the shipped app
- **v1.149.0**: AI-arc review — code-point-safe caps, per-conversation send lock + VIEW fronts-not-twins, and the surviving-mutant lesson (rendezvous test pins sent-body truth); rest CLEAN
- **v1.148.0**: EXPLAIN conversations — VIEW opens the diagnosis as a chat, seeded zero-call from the completed exchange; prompt-parity law mutation-proven; one gated engine path; live-proven follow-up
- **v1.147.0**: Ask ORACLE conversations — follow-ups on the same selection, full-history turns, fixed subject, honest ten-exchange cap, gates mutation-proven per send, live-proven multi-turn
- **v1.146.0**: Ask ORACLE in the editor — right-click any selection, own consent gate (never rides the failure-flow grant), capped disclosure, gates mutation-proven, live-proven vs the real API
- **v1.145.0**: docs truth — README/user-guide carry the nine-chain kit family + Aptos sniff; plan.md currency pass (53 devices, 74 grammars, 86 spaces)
- **v1.144.0**: kit-arc review (v1.139–v1.143) — space #86 install-hint pipeline bug + stale action javadoc fixed; sniff/casing/write-law/parity verified CLEAN; Clarity grammar recorded as premise
- **v1.143.0**: Clarity in the Contract Kit (9th chain) + learning space #86 — clarinet-proven counter with simnet tests; ASCII-only + Devnet.toml rules caught live; parity gate reads npm pins; Doctor +clarinet; 86 spaces
- **v1.142.0**: Move Aptos dialect — moveDialect sniff (AptosFramework in Move.toml) steers all five Move lanes to aptos move compile/test; live-proven with aptos 9.5.0 first; Doctor +aptos; mutation-proven
- **v1.141.0**: tech-debt sprint — kit write-law → one KitFiles home, toolOnPath reach-in → CommandDevice, dead Chain.tool wired, + KitCatalogParityTest gating kit↔catalog pin drift (mutation-proven)
- **v1.140.0**: Bitcoin in the Contract Kit (8th chain) + learning space #85 — spending conditions via Miniscript, a timelocked vault compiled to consensus Script, live-proven; regtest + Clarity/RSK pointers
- **v1.139.0**: Contract Kit (Web3) wizard — seven chains, live-proven templated scaffolds, never-clobber + idempotent mutation-proven, keys never touch the IDE
- **v1.138.0**: Web3 arc review #2 — surface CLEAN (first zero-bug arc review; checklist + gates held), INSPECTOR comment names cairo/move; the arc (v1.130–138) is complete
- **v1.137.0**: Move (Sui) full vertical — ProjectKind.MOVE + pinned MIT grammar + CSL/outline/LSP + all sui move lanes + space #84 (live-proven first); Sui-first, Aptos sniff documented; 74 grammars, 84 spaces
- **v1.136.0**: Web3 docs truth — README/user-guide/plan.md carry the multi-chain story (STELLAR/ANCHOR/Cairo/bench/keys-boundary)
- **v1.135.0**: Web3 arc review — STELLAR exit attribution captured at launch (knob-turn mid-run could fake LOCAL NET UP; the serving-truth class), rest CLEAN; third same-day review, third single-bug catch
- **v1.134.0**: Cairo (Starknet) full vertical — ProjectKind.CAIRO + pinned grammar + CSL/outline/LSP + all scarb lanes + space #83 (live-proven first); 73 grammars, 83 spaces
- **v1.133.0**: Multi-Chain Bench preset — STELLAR/ANCHOR/ANVIL on one MONITOR, the Web3 arc's discoverability capstone
- **v1.132.0**: Web3 tranche 3 — CosmWasm + ink! learning spaces (#81/#82), both live-proven on real crates (cosmwasm-std 3, ink 5); Cairo/Move recorded as own-language premises; 82 spaces
- **v1.131.0**: ANCHOR, the Solana console (53rd device) — solana-test-validator as a true long-runner with a truthful SERVING gate (banner pinned live vs Agave 4.1.2), anchor build/test lanes, crates currency; learning space #80 live-proven; Doctor +solana +anchor
- **v1.130.0**: STELLAR, the Soroban console (52nd device) — Stellar smart contracts in the rack: build/test/local-net lanes, crates currency, honest greys, no-private-keys by construction; learning space #79 live-proven (sdk 27, real WASM); Doctor +stellar
- **v1.129.0**: housekeeping — Dependabot maven group merged (org.json via the one root property), 169 CHANGELOG compare links regenerated (dead since v1.2.1), example SPI plugin re-proven against current core
- **v1.128.0**: floors ratchet — all ten JaCoCo minimums raised to measured-minus-margin, locking the night+day shifts' test gains
- **v1.127.0**: plan.md currency pass — 31 releases of drift + one internal contradiction fixed; the v1.96–v1.126 arc, empty-ledger state, and two new failure patterns recorded
- **v1.126.0**: the day review — v1.119–v1.125 under fresh lenses; infra opsInFlight depth counter (popup destroy could overlap the boolean lock and lift it mid-op), everything else CLEAN
- **v1.125.0**: forge v2 — DocsShots dialog-shot phase (action-invoke → await dialog → root-pane paint → dispose-with-nothing-created); Learning Space picker + Standards Kit wizard shots; 13/13 tutorials illustrated
- **v1.124.0**: ledger 56 closed — core.http.HttpBodies unifies the seven capped HTTP-read sites (cap + one probe byte, policy stays at call sites); per-module gates re-pinned + cross-module re-inline gate; no actionable ledger items remain
- **v1.123.0**: ledger 55 closed — the editor debug/format remainder, all six items: DapProxy loopback pair reaped on clean client EOF (FD leak per session), Prettier timeout kill-tree + daemon capped drain + refuse-past-8MB (truncated text never reaches the document), startDebugging parse-before-ack, loopback free-port, 200k completion scan window, Chrome-profile shutdown reaper; 3 mutation proofs, editor 442; corrects v1.122.0's overstated "ledger empty" claim (56 remains actionable)
- **v1.122.0**: CouchDB TLS opt-in (ledger 54's last item) — ConnectionSpec secure flag, compat ctor, no migration; dialog checkbox CouchDB-only; CouchBackend.baseUrl picks the scheme; 54 fully closed
- **v1.121.0**: infra canvas full structural lock during live cloud ops (ledger 53b, the chosen full-block design) — opInFlight + setLocked + disabled op buttons + red banner; deletes/wires/drops refuse; re-aims defer; 53 fully closed
- **v1.120.0**: infra ledger 53 c/d/e — boundDesignFile (re-aim force-saves the file the edits belong to), onModel EDT marshalling for every cloud-worker model write, deletedInCloud requires the real "HTTP 404:" prefix
- **v1.119.0**: DB Studio reload + .env offer off the EDT (ledger 54 M5) — RP read, newest-wins reloadSeq, EDT apply; mutation-proven
- **v1.118.0**: Project Explorer detection 1→4 lanes (ledger 61) — a hung mount can't wedge the Workbench sweep
- **v1.117.0**: DB Studio hardening (ledger 54 L3/L5) — allowLoadLocalInfile=false on MySQL/MariaDB connects; DbClient.close() zeroes its password clone
- **v1.116.0**: DB Studio LOB cap (ledger 54 M4/L4) — 64k cell ceiling, BLOBs render [N bytes], Mongo peek names via JSONObject.quote
- **v1.115.0**: tools LOW sweep (ledger 62) — wizard scaffolds off the EDT via AsynchronousInstantiatingIterator, display-name mtime cache, runCommand probes on the RP
- **v1.114.0**: first full tools review — KindCache (menu enablement stops walking disk on the EDT), NpmService routes through CommandExecutor (hung `npm run dev` can't pin the lane), Explorer re-aim storm coalescing; 3 LOWs → ledger 62
- **v1.113.0**: core LOW sweep (ledger 57/58/59) — atomic rewrites preserve POSIX perms, Versions.compare survives non-normalized input, GitFacts caps the HEAD read
- **v1.112.0**: CommandExecutor per-line cap (ledger 60) — readLineBounded; a single endless line can't OOM the pump
- **v1.111.0**: RecentFiles on its own RP lane — prefs writes off the EDT, pruneAsync, awaitIdle for tests
- **v1.110.0**: Lexers.find ThreadLocal cycle guard — a CSL grammar naming itself can't recurse the lexer to death; LexerIdiomGateTest
- **v1.109.0**: the docs screenshot forge — DocsShots (@OnStart, property-gated, zero boot cost) cycles suite tabs and paints the main window to 2x PNGs (Swing paint, no screen-recording permission needed); scripts/docs-shots.sh rebuilds first (stale-assembly lesson), throwaway userdir+cachedir, update checks suppressed (UpdateCheck gates on nmox.shots.dir; platform check via plugin.manager.check.updates=false); shot→tutorial mapping mutation-proven; 11/13 tutorials illustrated (8 curated staged shots incl. breakpoint-hit/ANVIL/postgres, 3 forge shots); learning-space Install sections point at the in-app INSTALL button (REPL spaces) or SOLDER (run spaces) instead of a shell — 'No terminal needed'
- **v1.108.0**: rack-panel mouse handlers are owner-scoped + Load/Export don't block the EDT (first dedicated rack UI-layer review) — RackPanel.installInteraction guarded with a bare `l instanceof DeviceMouse`, but DeviceMouse is one inner class shared across all RackPanel instances, so the guard matched a DIFFERENT panel's handler (the devices are shared + outlive the panel): a second RackPanel over the same rack would skip its own wiring (dead selection/reorder/patching) + route events into the stale panel, which every device would pin in memory (leak); guard now identity-aware (`dm.owner() == this`) + removeNotify detaches via new uninstallInteraction (attach/detach-symmetric); Load Patch (RackIO.load) + Export CI (createDirectories+writeString) ran blocking disk I/O on the EDT while the adjacent Save button used SAVE_RP — both now ride SAVE_RP, Load via the new RackIO.readDocument (read+parse+.bak off the EDT) then fromJson on the EDT (device components mutate on the paint thread); review verified CablePatchGesture (armed patch still can't connect a disposed device across rebuild/flip), RackTopComponent lifecycle, palette, and every control (Led/VuMeter/LcdDisplay/Knob/RackButton/ToggleSwitch — timers self-stop + stop on removeNotify, off-thread callers marshal to EDT, LCD paints a synchronized snapshot) CLEAN; source-gated
- **v1.107.0**: the flight recorder writes off the hot path + a corrupt patch is never clobbered (first dedicated rack-engine review — the engine/persistence/service core) — FlightRecorder.record() ran its per-event journal append (Files.writeString APPEND) + rotate (readAllLines+write, up to 1.5 MB) under `synchronized(this)`, the same monitor that gates every bus publish (all device output-pump threads funnel through it) AND every EDT reader (timeline/export/statistics), so one slow/full disk could stall every pump at once + block a UI read; append+rotate now ride a dedicated single-thread FIFO JOURNAL_RP off the monitor (only the in-memory add() stays locked), new awaitJournalIdle() barrier drains it for tests/shutdown, source-gated; RackIO.load parsed the JSON BEFORE fromJson cleared devices, so a corrupt .nmoxrack.json left the PREVIOUS project's devices mounted on a switch AND the untouched file was clobbered by the next atomic save — now load() .bak's the user's file (BlockStudio idiom) + resets to known-empty before rethrowing; the review verified CommandExecutor has NO OOM accumulator (line-by-line dispatch, no buffer), and Rack/RackDevice/WorkspaceTrust/RackService/FileWatcher/RackBus/Cable/AtomicFiles CLEAN; its one LOW (CommandExecutor readLine no per-line cap) → ledger 60; rack green
- **v1.106.0**: the process-probe is bounded in time AND memory (first ui review + the core-review HIGH it leaned on) — EnvironmentDoctor's per-probe timeout was illusory (it drained output to EOF via `transferTo(nullWriter())` BEFORE `waitFor(4s)`, so a tool holding its pipe open hung the whole Doctor sweep + leaked the reader thread; now delegates to `ProcessSupport.runBounded`, first non-blank of stdout then stderr for the nginx/apachectl holdouts; wedged-pipe test proves the leash fires); `ProcessSupport.runBounded`'s output accumulator was uncapped (HIGH: a runaway/hostile child flooding stdout — `yes`, a build reload loop — grows the heap without bound until the timeout → OOM kills the IDE; the shared primitive every module's spawns route through, and the Doctor fix now leans on it) — each capture now held at a 4 M char (~8 MB) ceiling while the drains keep reading to EOF (no pipe deadlock), `BoundedResult` gains a `truncated` flag, 20 MB-flood test mutation-proven; "Discard experiment" (irreversible tree delete) defaulted Enter/Space to YES — full `NotifyDescriptor` ctor with NO_OPTION initialValue (v1.98.0 idiom), `ManageExperimentsSafetyTest` source-gated; core review verified HttpClientFactory/JsonUtil/SelfWriteTracker/ToolLocator/AtomicFiles/TerminalPhosphor/Device-SPI CLEAN, its 3 LOW findings → ledger 56–58; core 11/rack + ui green
- **v1.105.0**: DYNAMO knob flake fixed — a device knob-change listener schedules an async reload on the single-threaded DEVICE_BG RP that the test settle didn't drain (EDT+router only); new RackDevice.awaitDeviceBgIdle() barrier + settle draining device→EDT→router closes the whole offEdt-race class; test-only, no production change; rack 1154 green, repeat-verified
- **v1.104.0**: The ofString sweep — a repo-wide hunt for the unbounded-HTTP-read bug (capped in apiclient/web3/dbstudio) found + fixed the last four sites: rack HttpDevice (1 MB), rack OracleClient (8 MB), infra DigitalOceanClient (8 MB), ui UpdateCheck (2 MB), each via ofInputStream+readNBytes; zero ofString remains repo-wide; source-gated per module
- **v1.103.0**: Tools RCE gates (the editor RCE class, tools half) — Run/Build/Test/Clean (WebProjectActionProvider→CommandExecutor.run) and NPM Explorer run-script (NpmService.runCommand) spawned a cloned repo's package.json scripts / make/cargo(build.rs) / npx node_modules binaries with no Workspace Trust gate; both now requestTrust before the spawn (fixed-tool npm ls -g/--version paths excluded), NpmService output capped; root cause pinned (CommandExecutor.run/ProcessSupport.builder are un-gated primitives, gating is the caller's job); mutation-proven, tools 78; a hand Bundle.properties missing the NPM Explorer @Messages keys fixed in passing
- **v1.102.0**: Editor security (first dedicated editor review, three HIGH) — LSP servers and Prettier-on-save ran a cloned repo's committed `node_modules/.bin/<tool>` with no Workspace Trust gate (RCE on file-open / on-save); the project-local binary is now used only when the workspace is trusted (silent `WorkspaceTrust.isTrusted`), else the user's global tool; DAP frame read capped at 64 MB (debuggee-output OOM) with malformed-Content-Length as a protocol error; `WorkspaceTrust.clearForTest` promoted public for the cross-module gate tests; mutation-proven, editor 434; ledger 55 records the proxy-socket-leak + Prettier-kill-tree + probe-port remainder
- **v1.101.0**: DB Studio robustness (first dedicated dbstudio review, seven fixes) — MySQL/MariaDB backslash-escape in UpdateBuilder (break-out closed; PG/SQLite keep quote-doubling), 8 MB capped CouchBackend read, Apply + Remove-connection dialogs default to the safe button (v1.98.0 idiom), EXPLAIN refuses multi-statement text (no trailing DELETE), Apply 0-row guard (no silent lost edit), CSV formula-injection neutralization; dbstudio 374, 3 mutation proofs; Keyring/quoting/atomicity/NB-bridge CLEAN; ledger 54 records EDT workspace read + LOB cap + cleartext-Couch remainder
- **v1.100.0**: Contract Studio robustness (first dedicated web3 review, all six findings fixed) — 8 MB capped RPC reads with redacted refusals, URI.create inside the redaction seam, WatchCursor pure core clamps the getLogs lane + generation guard kills the re-arm cursor race, SEND/Deploy safe-default confirmation on non-loopback endpoints (loopback devnets frictionless), reloadWorkspace off the EDT newest-wins; fixture-server tests + 3 mutation proofs; secrets/atomicity/lifecycle/no-private-keys verified CLEAN
- **v1.99.0**: API Studio robustness (ledger 52 closed) — capped streaming response reads (8 MB, truncation flagged, charset honored), pretty-parse off the EDT with size/SOE guards, dedicated interruptible send lane + real Cancel (hung sends can't wedge the housekeeping RP), close-save dirty guard (no-op open/close no longer drops a newer file's unknown fields), split-CSP union grading (HSTS keeps RFC 6797 first-field-wins); in-JVM fixture-server tests, 3 mutation proofs
- **v1.98.0**: Infra Designer destructive dialogs default to the safe button (first dedicated infra review, ledger 53a) — Destroy Stack/Resource + Deploy confirmations defaulted Enter/Space to the destructive option (NotifyDescriptor.Confirmation hard-codes initialValue=OK_OPTION; setValue never moves the default button); full-constructor NO_OPTION confirm + DialogDescriptor Cancel-initialValue deploy; a reflexive keypress can no longer delete billed cloud resources; DialogSafetyTest source-gate mutation-proven; ledger 53 remainder (live-canvas-during-op, re-aim edit loss, cross-thread props, drift 404-substring) recorded
- **v1.97.0**: API Studio auth tokens → OS keychain (first dedicated apiclient review's HIGH: plaintext tokens in the committable .nmoxapi.json broke the Keyring-only law) — stable request id + ApiSecrets (dbstudio.Passwords idiom), JPasswordField, WorkspaceIO never serializes the token, load-time migration drops legacy plaintext; basic-auth colon checked after {{var}} resolution (single-var credential no longer dropped); mutation-proven, apiclient 112; ledger 52 (apiclient response-cap/close-save/grader) + 53 (infra destructive-dialog defaults + live-canvas) recorded for follow-up
- **v1.96.0**: resurrection survives an unsaved rack — the night journey gauntlet (update center PASS across 12 releases of drift: v1.90.0 → 1.95.2 in-app with digest verify + restart; Device SPI PASS: example plugin compiled against current core, installed restart-free, trust-gated, real uptime on LCD) found kill -9 with a never-saved patch left a perfect snapshot matching nothing; unmatchedAgainst + click-time DeviceCatalog re-creation fix it, mutation-proven; ledger-44 types stay out
- **v1.95.2**: the seventh review (two lenses over v1.93–v1.95.1) — 4 mutation-proven fixes: armed click-patch canceled on rack flip (HIGH: invisible armed state silently patched cables) and on structural rebuilds (ghost cable to a disposed device); INSPECTOR's ENDPOINT/armed-LED moved inside the launch guard (the v1.93.0 bug shape on a DATA jack); launch() returns false on a disposed device; sticky preview follows the cursor over rails; ledger 51 (SPI exec launched-for-real signal, additive when needed)
- **v1.95.1**: docs-truth repair — CHANGELOG backfilled for the six-entry v1.92–v1.95 gap, CLAUDE.md/plan.md/README brought current, palette hint advertises click-to-click patching ("drag or click jacks"); records the ORACLE live proof (both consult paths ran against the real API in the shipped app, 2026-07-19)
- **v1.95.0**: click-to-click cable patching — CablePatchGesture pure state machine (click arms, preview follows the cursor across scrolls, click connects, empty/Escape cancels, same-side re-arms; classic drag rides the same machine); born from the rack being wider than a default window; every transition unit-tested
- **v1.94.0**: Block Studio jump-to-component — double-click/F3 on an Element naming a sibling component's tag switches there as a patch boundary; own/unknown tags fall through; Host.openComponentWithTag default-false seam; the v5 idea backlog emptied
- **v1.93.1**: rear jack pitch compresses for jack-heavy devices instead of colliding INPUTS/OUTPUTS (the v1.90.0 "ENABSERVING" observation); relayout-on-add since pitch depends on final port count; catalog-wide ≥44px clear-air gate proven failing-first (named HALO)
- **v1.93.0**: the serving gate never lies through the trust prompt — serve verbs raised SERVING/RUNNING/LIVE before launch(), whose Keep Safe early-return left a phantom high gate; launch()/launchWithEnv() → boolean launched-for-real, 18 emitters across 14 devices guarded, trustCheck seam, ServingTruthTest failing-first
- **v1.92.1**: sixth review (one lens over v1.90–v1.92) — vendor tripwire covers all eight Classic Kit libraries with a full-file scan (Alpine's version marker sits past 21KB; wrong-version mutation fails 3 checks)
- **v1.92.0**: the modern lightweights — Solid/Preact/Qwik/Lit/Alpine/htmx learning spaces (78 total, all six live-proven with real installs), Vite+Solid template (14th), Alpine 3.14.9 + htmx 2.0.4 vendored sha256-pinned (Classic Kit 8 libs, detection + 22 completion entries, script-tag law loosened to attributes-allowed), ember-cli-build.js/remix.config.js manifests (53)
- **v1.91.0**: ORACLE auto-explain by cable — EXPLAIN trigger in + OUT data out; cable path never prompts (consent gate mutation-proven), 30s consult cooldown, recorder-race grace wait; OUT emission proven through a real rack router; OracleDeviceTest 10, rack 1136
- **v1.90.0**: ledger 50 closed — VELOCITY/COSMOS/NIMBUS/KINETIC/SPECTER receive() overrides (serve/stop/enableGate, NEXUS shape; SPECTER ENABLE runs-while-high so VELOCITY SERVING → SPECTER ENABLE kills the suite with the server); ConsoleJackContractTest catalog-wide dead-jack gate, failing-first proven on VELOCITY + NIMBUS
- **v1.89.0**: the fifth senior review (v1.83–v1.88, two lenses) — 8 fixes: SPECTER ciCommand always-headless (HIGH: --headed leaked to CI), RECORD self-target skip, monorepo kindDir config resolution, readyFired reset on finish, preview library volatile-snapshot (CME on HTTP thread), leading-space TEXT validate refusal, invalid-active ⌘I clear, insertKind null-doc guard; ledger 50 (inert console STOP/ENABLE jacks, family-wide) + serving-gate no-op blessed; rack 1131
- **v1.88.0**: Block Studio v5 tail — TEXT-with-< round-trips (entity escape in generate, inverse in parse at the TEXT site only; LOG/attrs untouched; literal "&lt;" survives; markupishText corpus doc; unescape mutation fails 18 checks) + Save All (every valid component via never-clobber, honest per-outcome status); Block tests 71
- **v1.87.0**: the E2E capstone — E2E Loop preset (VELOCITY READY → SPECTER RUN, SCOPE follows app-then-report URLs, MONITOR tap), Playwright learning space (72nd; data:-page starter tests, SPECTER-aware tutorial), menu-position collisions fixed (Learning Space File/119→112 vs Classic Kit; Block Studio Window/259→257 vs Contract Studio) and verified gone on a throwaway-userdir boot
- **v1.86.0**: SPECTER — the E2E console (51st device): ENGINE=auto (playwright config → cypress config → deps, explicit knob wins), RUN/HEADED (`playwright test [--headed]`, `cypress run`/`open`), REPORT serves the Playwright HTML report (URL/READY/SERVING + ⇄/⌘I + deregister-on-stop; cypress greys), RECORD launches codegen at the ServingRegistry's live WEB serving (blank when none; cypress greys to HEADED), BROWSERS installs runtimes, @playwright/test|cypress registry currency, CI-step; faceplate fit + CI-pin gates failed first, then passed; SpecterDeviceTest 5, rack 1123
- **v1.85.0**: Block Studio v5 — the composition loop: the preview serves the whole workspace (harness imports every other valid component from /lib/<tag>.js read live per request; nested sibling tags upgrade instead of sitting inert; active tag excluded — double define throws, mutation-proven; invalid components skipped, unknown lib tags 404); user guide + README describe the composed preview
- **v1.84.0**: Block Studio v4 — multi-component workspaces: BlockWorkspace pure core (never-empty list + active slot, unique-tag add, v1-doc JSON wraps verbatim / saves as v2 — forward-only migration), toolbar switcher with +/−, switch-as-patch-boundary (force-save + undo.clear, ⌘Z cannot cross components; undo restore swaps the active slot so persist tells the truth), Open Component… replace-same-tag-or-append, status "· component k/n", docs truth (user guide §6 + README gain Block Studio, absent since v1.78.0); BlockWorkspaceTest 6 + BlockMultiComponentTest 3, three mutations proven fatal
- **v1.83.0**: keyboard-operable Block Studio canvas (ledger 48 closed, the v1.82.0 review's own-release finding) — arrows traverse (Left parent/Right child), Alt+Up/Down reorder, Enter/Shift+Enter legal-kind insert menus, F2 edit, Delete, Escape; same doc paths as mouse so undo/persist identical; pieces exposed to AT as LIST_ITEM children with kind+face, level, position, live SELECTED state; keys named in the accessible description; BlockCanvasKeyboardTest drives the real handler with synthesized events
- See CHANGELOG.md for the full record; tagging vX.Y.Z triggers the release workflow (6 installer/SBOM assets + updates.xml/.gz + 11 module NBMs for the in-app update center)

## Documentation

- `docs/user-guide.md` - The user manual: install → first launch → projects → rack → studios → wizards → safety nets. Part of the docs truth pass — keep it accurate every ship
- `docs/making-a-smart-contract.md` - Worked tutorial: a real escrow contract built the Contract Studio way (code, tests, gas gate, live ANVIL loop) — all output real
- `docs/devices.md` - The 53-device reference (GENERATED from DeviceType by DeviceDocsTest; CI fails on drift)
- `docs/engineering/tech-debt.md` - The CURRENT debt ledger: open items with deferral reasons, closed items by version
- `docs/engineering/codebase-guide.md` - A beginner's guide to the codebase: the five RCP ideas everything rides on, four traced flows (boot / open-a-file / press-GO / send-a-request) with real file links, and the house laws with their origins — start here before reading code
- `docs/engineering/plan.md` - The CURRENT plan: where the project stands, honest gaps, ranked opportunities, the working method + house laws + failure patterns — read this first when deciding what to do next
- `CONTRIBUTING.md` - Contribution guidelines
- `docs/hack/` and `docs/product/` and most of `docs/engineering/` - v0.x-era documents kept for archaeology; every one carries a "Historical document" banner and none describes the shipping product

## Troubleshooting

**Build fails with "invalid target release: 21"**:
- Ensure Java 21+ is active: `java -version`
- Set JAVA_HOME: `export JAVA_HOME=$(/usr/libexec/java_home -v 21)`
- Or specify Java explicitly: `JAVA_HOME=/path/to/jdk21 mvn clean package`

**"Cannot run on older versions of Java" error**:
- Use `--jdkhome` flag when launching: `./bin/nmoxstudio --jdkhome /path/to/jdk21`
- Or set in config: edit `application/target/nmoxstudio/etc/nmoxstudio.conf` and set `jdkhome` property
- The launcher requires Java 21+ (the installers bundle their own runtime; only source builds and the portable zip need a host JDK)

**IDE won't start**:
- Check `application/target/nmoxstudio/` exists
- Try rebuilding: `mvn clean package -DskipTests`
- Check logs in `~/Library/Application Support/nmoxstudio/dev/var/log/`
- Verify Java 21+ with: `java -version`

**Java module access errors (InaccessibleObjectException)**:
- Fixed: Java module `--add-opens` flags are now automatically configured
- These are required for NetBeans Platform to work with Java 9+
- Configuration is in `application/src/main/resources/nmoxstudio.conf`

**Tests fail**:
- Ensure Java 21+ is used for testing
- Some tests may require GUI environment (use `./gui-test.sh`)

**Module not found errors**:
- Rebuild from root: `mvn clean install`
- Check module is listed in root `pom.xml` `<modules>` section

**"CodeCache is full" warning**:
- Increase code cache size: add `-J-XX:ReservedCodeCacheSize=256m` to `default_options` in conf file
- This is normal for large NetBeans Platform applications

## External Resources

- [NetBeans Platform Developer Guide](https://netbeans.apache.org/kb/docs/platform/)
- [NetBeans API Documentation](https://bits.netbeans.org/22/javadoc/)
- [Maven NetBeans Plugin](https://netbeans.apache.org/wiki/DevFaqActionAddProjectCustomizer)
- [FlatLaf Look and Feel](https://www.formdev.com/flatlaf/)
