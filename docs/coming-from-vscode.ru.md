# Переход с VS Code

<!-- languages -->
[English](coming-from-vscode.md) · [Español](coming-from-vscode.es.md) · [Français](coming-from-vscode.fr.md) · [Deutsch](coming-from-vscode.de.md) · **Русский** · [Українська](coming-from-vscode.uk.md) · [Polski](coming-from-vscode.pl.md) · [Português (Brasil)](coming-from-vscode.pt.md) · [Bahasa Indonesia](coming-from-vscode.id.md) · [Filipino](coming-from-vscode.tl.md) · [Tiếng Việt](coming-from-vscode.vi.md) · [简体中文](coming-from-vscode.zh.md) · [हिन्दी](coming-from-vscode.hi.md) · [עברית](coming-from-vscode.he.md) · [العربية](coming-from-vscode.ar.md)
<!-- /languages -->

Ваши руки уже знают, где что лежит. Эта страница — карта от этих
привычек к NMOX Studio: сначала сочетания клавиш, потом где живёт каждая
идея VS Code, потом что здесь честно по-другому.

Первые четыре сочетания, которые нажимает пользователь VS Code, делают
то, чего он ждёт: **⇧⌘P** открывает палитру команд, **⇧⌘E** — дерево
файлов, **⇧⌘X** — плагины, а **⌃\`** — терминал. Они записаны во всех
пяти профилях раскладки, которые поставляет платформа, и проверка при
сборке разрешает каждое из них через собранную раскладку в macOS, Windows
и Linux, чтобы вместо него не сработало ничто другое.

<a id="the-chords"></a>
## Сочетания клавиш

Столбцы macOS используют знаки из строки меню (⌃ Control, ⌥ Option, ⇧ Shift,
⌘ Command); столбцы Windows и Linux — то же сочетание на клавиатуре PC.

| Что нужно | VS Code, macOS | NMOX, macOS | VS Code, Win/Linux | NMOX, Win/Linux |
|---|---|---|---|---|
| Палитра команд | ⇧⌘P | **⇧⌘P** (или ⌘I) — Быстрый поиск | Ctrl+Shift+P | **Ctrl+Shift+P** (или Ctrl+I) |
| Открыть файл по имени | ⌘P | **⌘P** — Перейти к файлу | Ctrl+P | **Ctrl+P** |
| Дерево файлов | ⇧⌘E | **⇧⌘E** — Студия проекта | Ctrl+Shift+E | **Ctrl+Shift+E** |
| Расширения | ⇧⌘X | **⇧⌘X** — Сервис ▸ Плагины | Ctrl+Shift+X | **Ctrl+Shift+X** |
| Терминал в папке проекта | ⌃\` | **⌃\`** | Ctrl+\` | **Ctrl+\`** |
| Открыть недавний проект | ⌃R | **⌥⌘P** — Сменить проект… | Ctrl+R | **Ctrl+Alt+P** |
| Перейти к символу в проекте | ⌘T | **⌥⇧⌘O** | Ctrl+T | **Ctrl+Alt+Shift+O** |
| Перейти к определению | F12 | **⌘B** | F12 | **Ctrl+B** |
| Переименовать символ | F2 | **⌃R** | F2 | **Ctrl+R** |
| Перейти к строке | ⌃G | **⌃G** | Ctrl+G | **Ctrl+G** |
| Закомментировать строку | ⌘/ | **⌘/** | Ctrl+/ | **Ctrl+/** |
| Показать подсказки | ⌃Space | **⌃Space** | Ctrl+Space | **Ctrl+Space** |
| Добавить в выделение следующее вхождение | ⌘D | **⌘J** | Ctrl+D | **Ctrl+J** |
| Выделить все вхождения | ⇧⌘L | **⌃⇧⌘J** | Ctrl+Shift+L | **Ctrl+Alt+Shift+J** |
| Добавить курсор выше / ниже | ⌥⌘↑ / ⌥⌘↓ | **⌥⌘↑ / ⌥⌘↓** | Ctrl+Alt+↑ / ↓ | **Alt+Shift+[ / ]** |
| Переместить строку вверх / вниз | ⌥↑ / ⌥↓ | **⌃⇧↑ / ⌃⇧↓** | Alt+↑ / ↓ | **Alt+Shift+↑ / ↓** |
| Скопировать строку вниз | ⇧⌥↓ | **⌥⇧↓** | Shift+Alt+↓ | **Ctrl+Shift+↓** |
| Удалить строку | ⇧⌘K | **⌘E** | Ctrl+Shift+K | **Ctrl+E** |
| Отформатировать документ | ⇧⌥F | **⌃⇧F** | Shift+Alt+F | **Alt+Shift+F** |
| Закрыть вкладку редактора | ⌘W | **⌘W** | Ctrl+W | **Ctrl+W** |
| Панель проблем | ⇧⌘M | **⌘6** — Задачи (здесь ⇧⌘M ставит или снимает закладку) | Ctrl+Shift+M | **Ctrl+6** |
| Поставить точку останова | F9 | **⌘F8** | F9 | **Ctrl+F8** |
| Начать отладку | F5 | **⇧⌘F5** — Отладить файл | F5 | **Ctrl+Shift+F5** |
| Запустить без отладки | ⌃F5 | **F6** — Запустить проект | Ctrl+F5 | **F6** |
| Настройки | ⌘, | **⌘,** — NMOX Studio ▸ Settings… | Ctrl+, | Сервис ▸ Настройки (без сочетания) |

Каждое сочетание NMOX в таблице (⌘, — собственное сочетание меню
приложения в macOS) прочитано из поставляемой раскладки, а не по памяти.
Несколько вещей таблица не может сказать в одной клетке:

- **F5 во время отладки занята.** Здесь она значит *Продолжить*, как в
  любой IDE семейства NetBeans, поэтому отладочный запуск начинается с
  **⇧⌘F5** (Ctrl+Shift+F5) и продолжается по F5.
- **⌃R здесь — «Переименовать»**, поэтому *Сменить проект* живёт на ⌥⌘P,
  а не на сочетании Open Recent из VS Code. Переименование работает там,
  где его поддерживает язык, стоящий за файлом.
- **Ctrl+, в Windows и Linux** возвращает назад по истории правок, как
  всегда было в NetBeans; настройки находятся в Сервис ▸ Настройки
  (в macOS — **Settings…** в меню приложения, ⌘,).

**Справка ▸ Сочетания клавиш…** перечисляет каждое сочетание NMOX в вашей
активной раскладке, включая четыре сочетания VS Code; список читается из
работающей раскладки, поэтому не может разойтись с тем, что делают клавиши.


<a id="from-the-terminal"></a>
## Из терминала

`code .` — это `nmox .`:

```bash
cd ~/code/my-app
nmox .          # open this folder (manifest or not) and aim the IDE at it
nmox src/app.ts # open one file
nmox            # just start the IDE
```

Команда возвращается сразу, а второй `nmox` передаёт свою папку уже
работающей IDE. Homebrew, установщик Windows (*Add "nmox" to PATH*) и
пакеты Linux ставят её в PATH; для установки из DMG
[руководство пользователя](user-guide.ru.md#2-first-launch) показывает
ссылку в одну строку.

<a id="where-each-vs-code-idea-lives"></a>
## Где живёт каждая идея VS Code

| В VS Code | В NMOX Studio |
|---|---|
| **Explorer** | **Студия проекта** (⇧⌘E) — дерево файлов, шаблоны и редактор `package.json` проекта. **Рабочее место** (⌥⌘0) — домашняя база: открытые файлы, недавние файлы, недавние проекты и всё, что работает. |
| **Command Palette** | **Быстрый поиск** (⇧⌘P или ⌘I) — действия, файлы, недавние проекты, устройства стойки, активные серверы, запросы Студии API, символы. |
| **Extensions** | **Сервис ▸ Плагины** устанавливает и обновляет модули, включая собственные обновления NMOX. Многое из того, что в VS Code добавляет расширение, здесь — **устройство стойки**, и его можно написать файлом JSON в `~/.nmox/devices.d` ([файлы устройств](device-files.md)). |
| **`tasks.json`** | Собственные скрипты вашего проекта, запускаемые так, как они написаны: Запуск / Сборка / Тесты на панели инструментов (F6, F11, ⌃F6), **Запустить скрипт** на строке раздела scripts в `package.json`, **Обозреватель NPM** и **Стойка задач** (⌘9), где задачи — это устройства, которые вы соединяете. |
| **`launch.json`** | **Отладить файл** (⇧⌘F5) и кнопка отладки на панели инструментов сами понимают, что запускать, по самому проекту — точка входа скрипта `start`, `main`, `index.js`, — а устройство стойки **INSPECTOR** запускает отладчик как шаг конвейера. |
| **Integrated terminal** | Окно **Терминал** (⌃\`): первое нажатие запускает оболочку в папке проекта, следующие возвращают к ней. |
| **`settings.json`** | Сервис ▸ Настройки (в macOS — NMOX Studio ▸ Settings…). Файл `.editorconfig` вашего проекта применяется, пока вы печатаете и когда сохраняете. |
| **Problems panel** | **Задачи** (⌘6): ошибки и предупреждения языковых серверов, а также находки линтера и проверки типов от устройств стойки PURITY и TYPEGUARD. Как и в VS Code, некоторые серверы сообщают только об открытых файлах; gopls сообщает обо всём пакете. |
| **Outline** | **Навигатор** (⌘7). |
| **Source Control** | Значок git в строке состояния (ветка и изменения, один щелчок до истории) и меню **Команда**. |
| **Workspace Trust** | Та же идея, соблюдаемая до того, как запустится что-либо, выбранное репозиторием: открытие клонированного проекта не запускает ничего, пока вы ему не доверитесь. |
| **Keyboard Shortcuts editor** | Сервис ▸ Настройки ▸ Сочетания клавиш (в macOS — Settings… ▸ Сочетания клавиш) — измените любое сочетание или переключите весь профиль на Eclipse, Emacs или IntelliJ. |

<a id="what-is-honestly-different"></a>
## Что здесь честно по-другому

- **⌘D здесь не мультикурсор.** Тот же жест — **⌘J** (Ctrl+J); сама ⌘D
  ничем не занята. Назначьте её в разделе настроек «Сочетания клавиш», если пальцы настаивают.
- **⌃\` открывает Терминал и переводит на него фокус, но не прячет его.**
  А пока фокус в Терминале, клавиши принадлежат вашей оболочке, поэтому
  второе нажатие достаётся оболочке, а не возвращает вас в редактор.
- **`.vscode/tasks.json` и `launch.json` не читаются.** Задача — это
  команда, выбранная репозиторием, и её чтение заслуживает собственного
  замысла вокруг доверия рабочей области; до тех пор эту работу делают
  собственные скрипты проекта и правила точки входа для отладки, описанные
  выше.
- **Профиля раскладки «VS Code» нет.** Сочетания выше записаны в профиль
  по умолчанию и в четыре остальных. Одно намеренное исключение: в профиле
  **Eclipse** ⇧⌘E остаётся собственным *Switch to Editor* Eclipse, а
  внутри редактора ⇧⌘P и ⇧⌘X сохраняют значения Eclipse (парная скобка,
  верхний регистр) — выбравший Eclipse ждёт Eclipse.
- **В Linux Ctrl+\` открывает Терминал, а не переключатель окон.**
  Платформа держала там второй переключатель для рабочих столов (KDE),
  которые перехватывают Ctrl+Tab; переключатель находится на Ctrl+Tab.
- **Сочетания с Ctrl+Alt могут столкнуться с AltGr.** В Windows раскладки,
  которые печатают символы через AltGr (например, польская), посылают вместо
  него Ctrl+Alt. Если Ctrl+Alt+P или Ctrl+Alt+K печатает вам символ,
  перенесите *Сменить проект* или сочетания экспериментов в разделе настроек «Сочетания клавиш».
- **Расширения VS Code здесь не устанавливаются.** Языковой интеллект
  берётся из языковых серверов, которые знает NMOX (Диагностика окружения
  перечисляет, чего не хватает и как это установить), из собственных
  грамматик редактора и из плагинов, созданных для платформы NetBeans.
