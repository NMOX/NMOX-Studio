# v2.144.0: the menu rows whose names live in action CODE, not in the
# layer, so only a walk finds them and only a grep by VALUE places them.
# Where a module holds two names for one action, both are here: the menu
# reads one and the Keyboard Shortcuts sheet the other. The mnemonic
# mirrors English, whose own assignment does not collide.
# Gated by MenuRowsSpeakTest and CodeNamedMenuRowsTest.
CTL_LocalHistoryMenuName=السجل
CTL_LocalHistoryMenuNameLoc=السجل(&T)
CTL_MenuItem_ShowTextAnnotations=عرض تسميات إدارة الإصدارات(&V)
Menu/Versioning=الفريق(&M)

# 3.2.0: the Team menu, whose rows are named in CODE: the git module's
# Annotator and its DynamicMenus, VersioningMainMenu, local history's
# actions. No layer names them, so the walk of a German build found
# them English and bytecode placed each key. Gated by
# CodeNamedMenuRowsTest and MenuRowsSpeakTest.
CTL_DisconnectAction.name=\u0642\u0637\u0639 \u0627\u0644\u0627\u062a\u0635\u0627\u0644(&O)\u2026
CTL_OtherVCS.menu=VCS \u062a\u0627\u0646\u064a(&C)
