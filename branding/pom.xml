<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
      <groupId>org.nmox</groupId>
      <artifactId>NMOX-Studio-parent</artifactId>
      <version>1.0-SNAPSHOT</version>
  </parent>

  <artifactId>NMOX-Studio-branding</artifactId>
  <packaging>nbm</packaging>

  <name>NMOX-Studio-branding</name>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
  </properties>

  <dependencies>
    <dependency>
      <groupId>org.netbeans.api</groupId>
      <artifactId>org-netbeans-api-annotations-common</artifactId>
      <version>${netbeans.version}</version>
    </dependency>
    <!-- JUnit 5 for unit tests -->
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <version>6.1.3</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
      <plugins>
            <plugin>
                <groupId>org.apache.netbeans.utilities</groupId>
                <artifactId>nbm-maven-plugin</artifactId>
                <executions>
                    <!-- The Cyrillic mnemonic tables (after 3.2.0, ledger 122).
                         The platform's Mnemonics reads them with a plain
                         ResourceBundle.getBundle, which finds only an
                         UNBRANDED locale jar (org-openide-awt_ru.jar), and
                         the default branding execution can only write
                         org-openide-awt_nmoxstudio_ru.jar. The goal names a
                         jar brandingToken + "_" + the file's locale, so a
                         file with NO locale suffix under a token that IS the
                         locale gives exactly the platform's name. Bound
                         before package so the cluster and the NBM carry
                         them; CyrillicMnemonicsTest holds the result. -->
                    <execution>
                        <id>mnemonics-ru</id>
                        <phase>prepare-package</phase>
                        <goals>
                            <goal>branding</goal>
                        </goals>
                        <configuration>
                            <brandingToken>ru</brandingToken>
                            <brandingSources>${basedir}/src/main/nbm-mnemonics/ru</brandingSources>
                        </configuration>
                    </execution>
                    <execution>
                        <id>mnemonics-uk</id>
                        <phase>prepare-package</phase>
                        <goals>
                            <goal>branding</goal>
                        </goals>
                        <configuration>
                            <brandingToken>uk</brandingToken>
                            <brandingSources>${basedir}/src/main/nbm-mnemonics/uk</brandingSources>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <configuration>
                    <!-- to have the jar plugin pickup the nbm generated manifest -->
                    <archive>
                        <manifestFile>${project.build.outputDirectory}/META-INF/MANIFEST.MF</manifestFile>
                    </archive>
                </configuration>
            </plugin>
      </plugins>
  </build>
</project>
