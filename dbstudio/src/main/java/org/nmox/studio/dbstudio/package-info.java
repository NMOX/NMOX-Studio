/**
 * DB Studio — the database management suite. A first-class tab alongside the
 * Rack, the Infra Designer, and API Studio: define connections
 * (MySQL/MariaDB, PostgreSQL, SQLite — drivers bundled), browse schemas,
 * run SQL, read results in grids. Connection specs persist per project in
 * {@code .nmoxdb.json}; passwords live only in the OS keychain via the
 * platform Keyring.
 */
package org.nmox.studio.dbstudio;
